# V7.4

## 修改目标

V7.4 基于 V7.3，仅修改 attempt2 中夹爪松开结束附近的 H30 监督，训练样本起点和其他过滤规则保持不变。

设 `G=gripper_motion_stop`、`A=arm_adjustment_start`、H30 起点为 `s`：

- 对 `s∈[G-29,G]`，保留 `[s,G]` 的真实 action。
- 跳过严格位于 `G` 和 `A` 之间的静止 action。
- 从 `A` 开始继续取真实 action，直到重新组成 30 帧。
- `s=G-29` 的原 H30 恰好结束于 `G`，因此不需要改写。
- 不使用 terminal hold；构建时检查压缩后的 H30 不得跨越 `rexecution_frame`。
- execution 样本、V7.3 的 action 起点过滤规则和 V4 norm stats 均保持不变。

例如 `s=G-10` 时，监督帧为：

```text
[s, ..., G] + [A, ..., A+18]
```

## 生成 training index

```bash
openpi/.venv/bin/python scripts/prepare_v7_4_adjustment_index.py
```

默认输出：

```text
/data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_4_adjustment/
├── action_phase_manifest.jsonl
├── filter_summary.json
├── v7_4_prompt_training_index.json
└── artifact_hashes.json
```

## 训练

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 \
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/train_vla_stage_a_openpi.py \
  --dataset-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/lerobot_data/tactile_vla_rotation_v4 \
  --index-file /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_4_adjustment/v7_4_prompt_training_index.json \
  --norm-stats-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats \
  --output-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action \
  --run-name pi05_delta_tac_rotation_phase_v7_4_no_history \
  --data-profile rotation_phase_v7_4_adjustment \
  --prompt-profile phase_v2 \
  --experiment-kind phase_prompt_h30_v7_3_pre_adjustment_idle_compressed \
  --checkpoint /home/qxh/.cache/openpi/openpi-assets/checkpoints/pi05_base/params \
  --batch-size 8 \
  --num-steps 15000 \
  --action-horizon 30 \
  --action-dim 32 \
  --no-use-state-history \
  --state-history-len 0 \
  --state-history-dim 7 \
  --history-hidden-dim 0 \
  --paligemma-variant gemma_2b_lora \
  --action-expert-variant gemma_300m_lora \
  --train-lora-only \
  --fsdp-devices 2 \
  --seed 42
```
