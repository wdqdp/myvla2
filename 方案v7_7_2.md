# V7.7.2：新环境五任务联合训练

## 版本身份与输入

V7.7.2 保持 [V7.7](方案v7_7.md) 的五任务目标、phase/plan/action prompt、H100/11、五任务训练循环、推理输出及异步部署状态机。它从 V7.4.2 新环境 Stage A 的 `10000` checkpoint 初始化，不使用 V7.4 或 V7.6 的模型权重：

```text
/data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action/pi05_delta_tac_rotation_phase_v7_4_2_no_history/10000
```

数据身份固定为原始 episode 261–350，LeRobot 数据集 `tactile_vla_rotation_v4_1_new_env`，V4 索引及 norm stats 位于 `outputs/rotation_v4_1_new_env/`。Action 直接重放 `outputs/rotation_v7_4_2_adjustment/v7_4_2_prompt_training_index.json`，使用原始连续 H30；不做静止帧压缩、terminal hold 或跨 `arm_adjustment_stop` 的 adjustment chunk。构建器校验 checkpoint、V4 selection、V7.4.2 Action 索引、边界、源文件及 manifest 哈希。

## 数据差异

- adjustment 使用 80 个 attempt2 的离线 `A=arm_adjustment_start`、`S=arm_adjustment_stop`。事实样本 `[A,S-11]` 为 false、`[S-10,S+10]` 为 true。`S` 本身仍是 adjustment Action 最后一帧，`S+1` 开始 execution Action；`S+1..S+10` 的 assessment 正样本用于 V7.7 一致的结束窗口训练。
- 反事实仍为 `moderately→slightly` 的 `S_s=floor((A+S)/2)` 和 `slightly→moderately` 全 false。物理图像、触觉、qpos 与 H100 历史在事实/反事实 pair 内相同，只更改 plan 和 label。
- H100/11 使用 episode 内真实连续时间线，允许跨 attempt，不跨 episode；新环境不做 V7.6 的 idle 删除或扰动。新 norm stats 把 11x7 qpos 离散为 256-bin 文本。
- `need_recovery` 沿用失败活跃帧正样本和三类 3:1 负样本。成功恢复 execution 负样本从 `S+1` 取到 attempt 结束，不依赖缺失的 `gripper_close_start`。
- `failure_reason`、`recovery_plan` 沿用新环境 V4 的真实 observation、目标和 `rotation_distance_v1` memory。val/test manifest 保留 15 帧，训练/主评估流只使用 `F+14`。

新环境的 adjustment 运动更长：train 的事实样本与 `moderately→slightly` 反事实已经有 7,950 负 / 3,220 正，旧版只缩减 `slightly→moderately` 的办法无法同时保持 1:2。V7.7.2 正式策略已确认，默认 `--adjustment-sampling-policy ratio_1_to_2`：

按旧版每条 source-slightly 轨迹的保留比例重算困难反事实数量；保留全部边界附近困难反事实和 `moderately→slightly` pair，只对未被选中反事实引用的早期 `slightly` 事实负样本作确定性均匀缩减。所有保留的反事实仍有事实 pair，train 达到 6,440 负 / 3,220 正。`retain_all` 仅保留为诊断对照，不用于 V7.7.2 正式 run。

不能把旧版固定的 975 个反事实样本或旧版各 split 样本总数复制到新环境。

## 构建与训练入口

数据构建：

```bash
openpi/.venv/bin/python scripts/build_v7_7_2_multitask_data.py
```

写入 `outputs/rotation_v7_7_2_multitask/` 的四个 manifest、统一 `v7_7_2_multitask_training_index.json`、`summary.json` 和 `artifact_hashes.json`。先用 `--dry-run` 可只读检查计数。

训练入口：

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 OPENPI_DATA_HOME=/home/qxh/.cache/openpi USE_TF=0 \
  openpi/.venv/bin/python scripts/train_vla_multitask_v7_7_2.py \
    --num-steps 5000 \
    --eval-interval 1000 \
    --save-interval 1000 \
    --keep-period 1000 \
    --eval-max-need-samples 4096 \
    --fsdp-devices 2
```

默认 run 为 `outputs/multitask_v7_7_2/pi05_rotation_v7_7_2_five_task_h100_no_history`；默认训练 20,000 step、批量 8，每 `action → adjustment → need → failure → plan` 循环一次，每任务 4,000 次更新；每 5,000 step 保存。新两个分类头随机初始化，仅训练 PaliGemma LoRA 与分类头，Action Expert 和 PaliGemma 非 LoRA 冻结。正式 checkpoint 导出 `delta_params` 和部署用的 `full_params`；继续同 run 使用 `--resume`。

推理服务使用 `scripts/serve_tactile_vla_v7_7_2.py --checkpoint <step> --norm-stats-dir <新环境 norm_stats>`；客户端使用 `openpi/inference/agilex/inference/agilex_inference_v7_7_2_asyn.py`。两者复用 V7.7 的分步 `phase_decision` / `failure_reason` 协议、KV-cache 续写、安全 hold、generation 丢弃与 H100 rolling buffer，并检查 V7.7.2 的 data profile。在线阈值需在新环境 val split 校准并写入 checkpoint metadata，不应直接把旧环境阈值当成已验证值。

## 当前验证状态

已按确认的 1:2 策略生成正式索引 `outputs/rotation_v7_7_2_multitask/v7_7_2_multitask_training_index.json`，`training_data_hash=86b5f69ba4b3d915859d28cce3fb0f03090ae533f777f27a246ef0f0f2822395`。train 样本数：Action 66,397、adjustment 9,660（6,440 负 / 3,220 正）、need 27,432、failure 960、plan 1,920；val/test 的 adjustment 分别为 1,696/1,872。困难反事实保留 433 条，其中近边界 352 条；早期未配对事实负样本缩减 1,943 条。

正式索引已通过源哈希校验和训练加载器 dry-run，五个 batch 均完成 tokenization/transform，Action target 为 `[batch,30,32]`。相关测试 21 项通过。当前 JAX 只发现 CPU，GPU 训练尚未启动。离线边界随机十例的人工复核仍为 `pending`。
