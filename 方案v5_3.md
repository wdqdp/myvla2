# V5.3：Adjustment End 自动阶段切换

> 2026-08-31 联合训练修订：最初“冻结 Stage A、只训练分类头”的
> 4000-step 实验已经完成，但分类头难以理解 `qpos_h30` 的时序含义。当前后续
> 实验改为在 V5.2.1 Stage A 上共享 PaliGemma LoRA，按
> `action -> adjustment_end` 的固定 1:1 周期训练。本文后面关于“只训练 head”、
> “backbone 必须不变”、`recall>=80%` 硬门槛、action loss 退化硬门槛和 H30
> 硬门槛的旧描述只用于记录首轮实验，不再约束联合训练修订。数据标签
> `[R-5,R]`、prompt、1:3 分类采样和真机状态机均保持不变。

## 0. 当前联合训练修订

当前训练任务只有两个：

```text
step 0: action
step 1: adjustment_end
step 2: action
step 3: adjustment_end
...
```

固定总更新数为 8000，因此 action 和 `adjustment_end` 各更新 4000 次；两个
loss 权重均为 1.0。action stream 完整复用 V5.2 的 action indices、phase_v2
prompt、H30 terminal hold、norm stats 和均匀采样分布；分类 stream 保持
`adjustment_end=true:false = 1:3`。不修改 `[R-5,R]` 正标签窗口。

`eval_interval` 不属于固定实验参数，通过训练命令的
`--eval-interval <正整数>` 设置；未显式设置时默认 500。它只控制训练期间
计算 val AUROC/AUPRC 的频率，不改变 task cycle、训练步数、保存频率或最终
step 8000 的完整评估。

可训练参数只有：

```text
shared PaliGemma LoRA
adjustment_end_head
```

action expert（包括其 LoRA）、state-history GRU、action projection 和
PaliGemma 非 LoRA 参数全部冻结。action batch 的 loss 只更新共享 PaliGemma
LoRA；分类 batch 同时更新共享 PaliGemma LoRA 和 `adjustment_end_head`。

本轮不增加动作回归验收门槛，也不要求 recall 达到 80%。最终 threshold
只使用 val 集，并按以下唯一规则确定：

```text
early FPR <= 1% 的全部候选中，选择 recall 最大者；
recall 相同时选择数值更高的 threshold。
```

这里 `early FPR = FP/(FP+TN)`，负样本严格为 `frame < R-5`。test 只使用
val 选出的 threshold，不重新调参。动作 loss、H30 端点仿真继续可以作为
诊断信息，但都不是阻止 checkpoint 输出的 gate。

最终必须额外输出 `P(adjustment_end=true)` 在 `R-30` 到 `R` 的平均值：

```text
[R-30,R-26], [R-25,R-21], [R-20,R-16],
[R-15,R-11], [R-10,R-6], [R-5,R]
```

前五个区间各 5 帧；最后一个区间是保持不变的 6 帧闭区间正标签窗口。
每个区间同时报告样本加权均值、episode 等权均值、样本数和 episode 数，
并分别输出 val/test 结果。

## 1. 目标与版本边界

V5.3 在已训练完成的 V5.2 Stage A 动作模型上增加
`adjustment_end` 二分类头，用于在 `adjustment` 阶段判断当前 H30 chunk
执行完成后是否应在下一个 chunk 切换到 `execution`。

本版本只训练和验证 `adjustment_end`：

- 不重新训练 V5.2 Stage A；
- 不修改 V5.2 action indices、H30 target、terminal hold 和 norm stats；
- 暂不训练新的 `need_recovery`；
- `execution` 阶段不调用 `adjustment_end`；
- `adjustment` 阶段不调用 `need_recovery`，不启动异步失败检测；
- 推理代码以 V5.2 的
  `openpi/inference/agilex/inference/agilex_inference_forced_phase_anlation.py`
  为基线，重新编写成新文件
  `openpi/inference/agilex/inference/agilex_inference_forced_phase_anlation_5_3.py`；
- 不修改 V5.2 原推理文件，V5.3 的所有新推理逻辑只写入
  `agilex_inference_forced_phase_anlation_5_3.py`。

版本标识固定为：

```text
data_profile=rotation_phase_v5_adjustment_end_v1
prompt_profile=phase_change_v1
experiment_kind=adjustment_end_qpos_h30_text
```

V5.3 为未来的 `need_recovery` 保留与 `adjustment_end` 相同的输入格式和
同一个 PaliGemma backbone。未来只增加并列的 `need_recovery_head`，不再发明
另一套输入表示。

### 1.1 V5.3 独立代码文件

V5.3 的数据、模型、训练、服务和推理都使用独立文件：

```text
src/tactile_vla/vla/v5_3_phase_change.py
src/tactile_vla/vla/v5_3_adjustment_end_data.py
src/tactile_vla/vla/v5_3_adjustment_end_model.py
scripts/build_v5_3_adjustment_end_data.py
scripts/train_vla_adjustment_end_v5_3.py
scripts/train_vla_adjustment_end_multitask_v5_3.py
scripts/serve_tactile_vla_v5_3.py
openpi/inference/agilex/inference/agilex_inference_forced_phase_anlation_5_3.py
```

职责固定为：

- `v5_3_phase_change.py`：评估 prompt、H30→H10 采样、qpos 归一化和
  离散化的唯一共享 helper；
- `v5_3_adjustment_end_data.py`：manifest/index 构建及身份、时序、标签和
  hash 校验；
- `v5_3_adjustment_end_model.py`：V5.2 backbone 和 `adjustment_end_head` wrapper；
- `build_v5_3_adjustment_end_data.py`：版本化数据构建 CLI；
- `train_vla_adjustment_end_v5_3.py`：只训练分类头并执行离线评估；
- `train_vla_adjustment_end_multitask_v5_3.py`：当前联合训练入口，交替训练
  V5.2 action replay 与 `adjustment_end`，更新共享 PaliGemma LoRA 和分类头；
- `serve_tactile_vla_v5_3.py`：同一模型对象同时提供 `mode=execution` 和
  `mode=adjustment_end`；
- `agilex_inference_forced_phase_anlation_5_3.py`：V5.3 真机状态机。

V5.2 已有数据构建、训练、服务和推理文件均保持不变。

## 2. 不修改 V5.2 Stage A 动作输入

V5.2 Stage A 继续使用已训练的 `phase_v2` action prompt：

```text
Mode: execution. Task: <instruction>.

Mode: adjustment. Task: <instruction>.
Put the object back, and follow this recovery plan: <plan>.
```

Stage A 的 WebSocket 动作请求仍使用：

```text
mode=execution
```

动作阶段只由 action prompt 表达。V5.3 不将触觉 caption 或
`qpos_h30` 加入 Stage A action prompt，否则会产生 Stage A 训练/推理不一致。

## 3. 统一的阶段评估 Prompt

`adjustment_end` 和未来的 `need_recovery` 使用同一个共享 prompt builder。
当前 `adjustment_end` 的唯一原始格式为：

```text
Mode: adjustment.
Task: <instruction>.
Tac: <tactile caption>
Recovery plan: <input_recovery_plan>.
qpos_h30:[[...],[...],...]
```

格式规则固定为：

- 每个字段独占一行；
- `Mode`、`Task` 和 `Recovery plan` 保证以一个英文句号结束，
  输入已有句号时不重复添加；
- `Tac:` 后使用一个空格；
- `qpos_h30:` 后不加空格，紧接紧凑 JSON；
- JSON 每组恰好 7 个整数，共 10 组，不输出浮点数；
- 训练、离线评估、replay 和真机必须调用同一个 builder。

未来训练 `need_recovery` 时改为：

```text
Mode: execution.
```

只替换第一行，其他字段的名称、顺序、标点和 `qpos_h30`
序列化方式不得改变。紧凑 JSON 示例：

```text
qpos_h30:[[12,31,55,78,91,108,220],[13,32,55,77,92,109,219],...]
```

真机 `adjustment_end` prompt 中的 `input_recovery_plan` 必须与当前
adjustment action prompt 使用的 forced recovery plan 完全一致；不得使用
空 plan、初始 plan 或上一个 attempt 的过期 plan。

π0.5 的 transform 仍会将当前帧 7 维 qpos 归一化和离散化，自动追加为：

```text
State: <current_qpos_discrete>;
Action:
```

因此分类头的 prefix 同时包含：

- front 图像；
- left wrist 图像；
- `Mode`、instruction、触觉 caption 和 recovery plan；
- 文本形式的过去 H30 qpos；
- π0.5 自动添加的当前帧 qpos。

阶段切换分类请求使用独立的：

```text
phase_change_max_token_len=512
```

该名称同时适用于当前的 `adjustment_end` 和未来的 `need_recovery`，
不使用 assessment 命名，避免与后续的 `Mode: tactile assessment.`
混淆。V5.2 Stage A action 请求仍使用 `action_max_token_len=200`。

数据构建时必须使用与训练/推理相同的 π0.5 tokenizer 统计完整
阶段切换 prompt（包括自动追加的当前 qpos）的 token 长度。任何样本
超过 `phase_change_max_token_len=512` 必须报错，不得静默截断。

## 4. qpos_h30 的时间定义

`current_qpos` 和 `qpos_h30` 是两个独立输入。对当前帧 `t`：

```text
current_qpos = qpos[t]
qpos_h30    = qpos[t-30:t]
```

`qpos_h30` 是 `t-30 ... t-1` 的 30 帧，不包含当前帧 `t`。

### 4.1 允许跨 attempt，禁止跨物理 episode

对 attempt2 开始处不做 padding。只有在同一物理 episode 中存在
attempt1 且能提供完整历史时，才保留 attempt2 的前 30 帧分类样本，
并从 attempt1 尾部取历史：

```text
attempt2/frame0:
    history = attempt1 最后 30 帧
    current = attempt2/frame0

attempt2/frame1:
    history = attempt1 最后 29 帧 + attempt2/frame0
    current = attempt2/frame1

attempt2/frame10:
    history = attempt1 最后 20 帧 + attempt2/frame0..9
    current = attempt2/frame10

attempt2/frame30:
    history = attempt2/frame0..29
    current = attempt2/frame30
```

这里的 episode 指 raw/V5 identity 中的物理 `episode_id`，不能用可能按 attempt
拆分的 LeRobot `episode_index`。

数据构建不能直接使用 LeRobot loader 在 attempt 边界 padding 后的
`observation.state_history`。必须依据 V5.2 的 global/frame identity 建立：

```text
physical_episode_timeline = attempt1 frames + attempt2 frames
```

再从当前帧在该 timeline 中的位置向前取 30 帧。每个样本必须验证：

```text
history length == 30
history 与 current 的 physical episode_id 相同
history global indices 严格递增
history 最后一帧早于 current
history_crosses_episode == false
```

允许 `history_crosses_attempt == true`。若 attempt1 存在但长度不足 30，
则 attempt2 开头仍无法组成 30 帧同 episode 历史的样本也不进入分类训练；
等同 episode 累计历史满 30 帧后立即恢复使用。不得跨到上一个物理
episode。

真实数据中 episode 361--440 共 80 个 episode 只有 attempt2，没有 attempt1。
对这 80 个 attempt2，取消 `frame_index=0..29` 的分类样本，从 frame 30
开始使用 attempt2 frame 0--29 作为完整历史。不得 padding 或跨 physical
episode 取历史。被取消的 2400 帧仍保留在 manifest 中用于审计，固定记录：

```text
history_available=false
history_unavailable_reason=physical_episode_has_no_attempt1
classification_sample_valid=false
```

若 attempt1 存在但不足以补齐当前帧的 30 帧历史，同样记录
`history_available=false` 和 `classification_sample_valid=false`，但原因记为
`insufficient_same_episode_history`。

其余拥有 attempt1 的 attempt2，`frame_index=0..29` 继续使用 attempt1 尾部
历史作为有效的 adjustment 早期负样本。manifest 额外记录：

```text
history_crosses_attempt=true
runtime_reachable_endpoint=false
```

对当前确认的真机 `s=0` 对齐，该字段的唯一定义为：

```python
runtime_reachable_endpoint = frame_index >= 30 and frame_index % 30 == 0
```

因此不仅前 30 帧为 `false`，所有非 30 倍数帧也为 `false`。
30 种偏移仿真依据各自的 `s` 动态生成端点，不复用这个只表示
真机 `s=0` 对齐的 manifest 字段。

val/test 逐帧指标只使用 `classification_sample_valid=true` 的样本，并分层报告
有历史的 attempt2 前 30 帧、
`frame_index>=30` 和实际 H30 端点。H30 端点验收只使用
`runtime_reachable_endpoint=true` 的实际可调用端点。

### 4.2 均匀采样为 10 帧

从 30 帧中固定采样：

```python
sample_offsets = np.rint(np.linspace(0, 29, 10)).astype(int)
```

固定结果为：

```text
[0, 3, 6, 10, 13, 16, 19, 23, 26, 29]
```

训练、离线评估、replay 和真机推理必须共享同一个 helper，不得各自
重新实现采样规则。

### 4.3 归一化和离散化

每帧 7 维 qpos 使用 V5.2 沿用的 V4 state q01/q99：

```python
q_norm = (qpos - q01) / (q99 - q01 + 1e-6) * 2.0 - 1.0
```

过去 H30 qpos 必须严格沿用 π0.5 对当前 state 的离散方式：
使用相同的 q01/q99 归一化、相同的 256 个 bin 边界和相同的
`np.digitize(...)-1` 逻辑，不额外增加 clip：

```python
bins = np.linspace(-1.0, 1.0, 256 + 1)[:-1]
q_bin = np.digitize(q_norm, bins=bins) - 1
q_bin = q_bin.astype(np.int32)
```

这意味着常规归一化范围内的离散值为 `0..255`；若归一化值低于
`-1`，与现有 π0.5 tokenizer 一样可能产生 `-1`。对非有限 qpos 必须报错。
训练、离线评估、replay 和真机必须调用同一个共享 helper，不得复制
一份“近似”实现。

离散化 helper 必须版本化，并将 bin 数、无额外 clip、sample offsets 和
norm stats SHA 写入数据产物及 checkpoint metadata。

## 5. 训练数据和标签

只使用 V5.2 的 408 个 attempt2，并逐项复用 V5.2 的 physical episode selection
和 train/val/test 划分：

```text
train: 328 attempt2
val:    44 attempt2
test:   36 attempt2
```

不加入 attempt1，不加入 `one_success` 的单 attempt 样本，不加入 `no_grasp`、
`small_grasp` 或其他扰动。caption 使用 V4 LeRobot 中当前帧的
`tactile_caption`，instruction 和 `input_recovery_plan` 也从同一帧取得。
V4 caption 的固定来源为：

```text
checkpoint=/data1/outputs/tactile_captioner/tcn_v3_w30_rotation_head/best.pt
window_size=30
```

V5.3 数据 identity、分类头 checkpoint 和 server metadata 必须记录该
captioner 的 SHA256 及 `window_size=30`。

对每个 attempt2，令：

```text
R = rexecution_frame
```

`R` 直接读取 V5.2 `phase_boundaries.jsonl` 中对应 attempt2 的
`rexecution_frame`，定义为 attempt2 内部从 0 开始的局部帧索引。
`R` 不是 raw timestamp，也不是 LeRobot global index。V5.3 不重新执行
rexecution 检测，也不重新根据 timestamp 映射边界。

必须验证：

```text
5 <= R < attempt2_frame_count
(episode_id, attempt_id=2, frame_index=R) 唯一映射到 current_global_index
```

`R<5`、越界、identity 缺失或映射不唯一时必须直接报错。

标签定义为：

```python
adjustment_end = R - 5 <= frame_index <= R
```

即 `R` 及其前 5 帧，共 6 帧为 `true`，其他帧的 label 均为 `false`。
同时使用 `adjustment_end_valid` 表达该头是否在当前阶段有效：

```python
adjustment_end_valid = frame_index <= R
```

因此：

| 帧范围 | `adjustment_end` | `adjustment_end_valid` | 进入 loss |
|---|---:|---:|---:|
| attempt1 | false | false | 否 |
| attempt2 `0 <= t < R-5` 且历史可用 | false | true | 是 |
| 缺少 attempt1 的 attempt2 `0 <= t < 30` | false | true | 否 |
| attempt2 `R-5 <= t <= R` | true | true | 是 |
| attempt2 `t > R` | false | false | 否 |

V5.2 将 `R` 定义为 execution 首帧，但 V5.3 将它作为边界容差正样本。
`R` 之后的 execution 帧仍在 manifest 中保留 `false` label，但由于分类头在
execution 无效，它们不参与 loss 和分类评估。

理论正样本数为：

```text
train: 1968
val:    264
test:   216
total:  2448
```

取消缺失 attempt1 的 2400 个早期负样本后，training index 的自然分布固定为：

```text
train: 72840 = 1968 positive + 70872 negative
val:    9464 =  264 positive +  9200 negative
test:   8010 =  216 positive +  7794 negative
total: 90314 = 2448 positive + 87866 negative
```

数据 manifest 保留全部有效负样本。训练 DataLoader 固定使用 `1:3`
正负采样；batch size 8 时每个 batch 恰好包含：

```text
2 x adjustment_end=true
6 x adjustment_end=false
```

采样规则为：

- 只从 `adjustment_end_valid=true` 且 `classification_sample_valid=true` 的
  train 样本采样；
- 每个 epoch 使用全部正样本一次；
- 负样本按 seed 确定性打乱，每个 epoch 无放回选取正样本数量的 3 倍；
- 后续 epoch 继续轮换尚未覆盖的负样本，负样本池耗尽后才用
  下一个确定性 permutation；
- batch 内 8 个样本的顺序再次确定性打乱；
- loss 不叠加 class weight；
- val/test 使用全部有效样本的自然分布，不做重采样。

## 6. 数据产物

新版本不覆盖 V5.2：

```text
/data1/outputs/vla/rotation_v5_adjustment_end_v1/
├── adjustment_end_training_index.json
├── adjustment_end_manifest.jsonl
├── adjustment_end_summary.json
└── artifact_hashes.json
```

`adjustment_end_manifest.jsonl` 保留 408 个 attempt2 的全部帧，包括
`R` 之后 `adjustment_end_valid=false` 的 execution 帧，用于身份、边界和
标签审计。`adjustment_end_training_index.json` 只索引
`adjustment_end_valid=true` 且 `classification_sample_valid=true` 的帧。
缺少 attempt1 的 80 个 attempt2 从 frame 30 开始进入 index。

train/val/test 都在 training index 中按 V5.2 split 分开记录。DataLoader 只能
通过 training index 读取分类样本；train 再执行固定 `2:6` batch 采样，
val/test 读取 index 中的完整自然分布。attempt1 不写入 manifest 或
training index，只作为 attempt2 开头 `qpos_h30` 的历史数据源。

manifest 至少记录：

```json
{
  "episode_id": 45,
  "attempt_id": 2,
  "frame_index": 3,
  "current_global_index": 123456,
  "rexecution_frame": 214,
  "split": "train",
  "history_global_indices": [],
  "history_attempt_ids": [],
  "history_crosses_attempt": true,
  "history_crosses_episode": false,
  "history_available": true,
  "history_unavailable_reason": null,
  "runtime_reachable_endpoint": false,
  "qpos_h30_sample_offsets": [0, 3, 6, 10, 13, 16, 19, 23, 26, 29],
  "qpos_h10_discrete": [],
  "adjustment_end": false,
  "adjustment_end_valid": true,
  "classification_sample_valid": true,
  "prompt": "..."
}
```

index 和 summary 必须完整记录：

- V5.2 selection/index/boundary/manifest SHA；
- V4 LeRobot dataset identity 和 norm stats SHA；
- caption 源字段和数据 SHA；
- qpos 归一化、离散化和采样 helper 版本/SHA；
- prompt schema 和 prompt token length 分布；
- 每个 split 的样本数、有效数、正负数和跨 attempt 数；
- 所有产物的 SHA256。

## 7. 模型结构与训练

以已训练完成的 V5.2 Stage A 15000 checkpoint 作为唯一 backbone：

```text
/data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_phase_v5_2_1/15000
```

必须校验其配置为：

```text
data_profile=rotation_phase_v5_adjustment_v2
prompt_profile=phase_v2
experiment_kind=phase_prompt_h30_terminal_hold
num_steps=15000
action_horizon=30
action_dim=32
state_history_len=60
state_history_dim=7
seed=42
```

数据产物、分类头 checkpoint 和 server metadata 都必须记录该路径、
V5.2 `config.json` SHA 和恢复后的 backbone 参数树 SHA。不得自动回退到
10000/5000 checkpoint 或基础 π0.5 参数。任何 identity 不一致都必须报错。

新 wrapper 在同一个 π0.5 模型上增加二分类头：

```text
V5.2 Pi0 backbone
├── PaliGemma image/language prefix
├── action expert
├── V5.2 state-history GRU
└── adjustment_end_head
```

分类特征沿用 V3 needRecovery 的 prefix pooling 方式：

```python
prefix_output, prefix_mask, _ = encode_prefix(backbone, observation)
mask = prefix_mask[..., None].astype(prefix_output.dtype)
features = (prefix_output * mask).sum(axis=1) / mask.sum(axis=1).clip(min=1)
logits = adjustment_end_head(features)
```

`adjustment_end_head` 严格沿用 V3 `NeedRecoveryHead` 结构，只修改模块名称：

```text
PaliGemma prefix masked-mean pooling
  -> LayerNorm
  -> Linear(PaliGemma width, 512)
  -> GELU
  -> Dropout(0.1)
  -> Linear(512, 2)
  -> false/true logits
```

固定 `adjustment_end_hidden_dim=512`、`adjustment_end_dropout=0.1`，
不得以未记录的命令行覆盖修改。

第一版不调用 `encode_state_history()` 生成分类特征，因为 H30 已按要求以
`qpos_h30` 文本进入 prefix。为保持 V5.2 observation schema，分类 payload 仍必须
携带 60×7 continuous history 及 mask，但 `adjustment_end_head` 不得调用
state-history GRU 或使用该 GRU 输出。

第一版冻结：

- PaliGemma 参数和现有 LoRA；
- action expert；
- V5.2 state-history GRU；
- action output projection；
- 其他 Stage A 参数。

只训练 `adjustment_end_head`的 LayerNorm、MLP 和二类输出层。Stage A 参数在训练前后必须
逐项不变，并记录 backbone SHA。

训练协议固定为：

```text
batch_size=8
num_steps=4000
lr=1e-4
optimizer=AdamW
beta1=0.9
beta2=0.95
eps=1e-8
weight_decay=1e-4
grad_clip=1.0
seed=42
eval_interval=500
save_interval=1000
precision=auto
loss=softmax_cross_entropy
lr_schedule=constant
```

不使用 warmup 或 cosine schedule。训练不与 Stage A action loss 联合，
只计算 `adjustment_end` 二分类交叉熵并只更新新分类头。
每 500 steps 的 val 只用于训练监控，不在早期 checkpoint 中挑选“最优”模型。
唯一官方分类头为固定训练完成的 `step=4000`；threshold 选择、H30 仿真和
锁定 test 均只使用 step 4000。若最终 checkpoint 验收失败，不自动回退
到早期 step。

训练 checkpoint 只保存 head 参数、恢复训练所需的 head optimizer state、
step 和完整 metadata，不重复保存冻结的 V5.2 backbone 参数。服务时先加载
固定 V5.2 Stage A checkpoint，验证参数树 SHA，再加载 head 参数，组成一个
共享 backbone 的模型对象。

## 8. 服务端协议

新建的 `scripts/serve_tactile_vla_v5_3.py` 以 V5.2 action server 为基线，
但不修改 V5.2 server 原文件。V5.3 server 在保留 action 能力的同时新增：

```text
mode=adjustment_end
```

请求结构仍为：

```python
{
    "mode": "adjustment_end",
    "observation/image": current_front,
    "observation/wrist_image": current_left,
    "observation/state": current_qpos,
    "observation/state_history": continuous_history_60x7,
    "observation/state_history_mask": history_mask,
    "prompt": phase_change_prompt,
}
```

返回：

```json
{
  "adjustment_end": true,
  "adjustment_end_probs": [0.08, 0.92],
  "policy_timing": {"infer_ms": 35.2}
}
```

server metadata 增加：

```json
{
  "supports_adjustment_end": true,
  "adjustment_end_threshold": 0.5,
  "phase_change_prompt_profile": "phase_change_v1",
  "phase_change_max_token_len": 512,
  "qpos_h30_sample_offsets": [0, 3, 6, 10, 13, 16, 19, 23, 26, 29],
  "qpos_bin_count": 256,
  "qpos_discretization_extra_clip": false,
  "captioner_checkpoint_sha256": "<sha256>",
  "captioner_window_size": 30,
  "phase_change_timeout_seconds": 10.0
}
```

threshold 由自然分布的 val 集确定。候选集固定为 val 上所有唯一的
`P(adjustment_end=true)` 值与 `0.0/1.0`，按数值升序做确定性遍历，
判定统一使用 `P(true) >= threshold`。定义：

```text
positive_window = R-5 <= frame_index <= R
early_region    = frame_index < R-5
```

候选 threshold 必须同时满足：

```text
positive-window recall >= 80%
early false-positive rate <= 1%
```

在全部合格候选中选择数值最高的 threshold，使阶段切换尽量保守。
若没有任何 threshold 同时满足两个条件，则该模型验收失败，不做
真机测试。选定后将 threshold 写入 checkpoint/server metadata，test 和
真机不得重新调参。
服务端是 threshold 的唯一执行方：服务端使用 metadata 中的
`adjustment_end_threshold` 将 probability 转换为最终布尔值。
客户端以响应中的 `adjustment_end` 为唯一状态切换依据，
`adjustment_end_probs` 只用于日志和离线分析。客户端不得重新应用 threshold，
也不提供 threshold 覆盖参数。启动时必须读取并记录 server metadata
中的 threshold。

V5.3 server 在宣布 ready 之前必须分别预热一次 `mode=execution` 和
`mode=adjustment_end`，避免将首次 JAX 编译耗时带入真机超时。
预热完成后，真机阶段切换请求固定超时为 10 秒；超时按本方案的
fail-closed 规则安全停止。

## 9. Forced-phase 真机验证

以 V5.2 推理脚本为基线：

```text
/home/test/qxh/workspace/myvla2/openpi/inference/agilex/inference/
agilex_inference_forced_phase_anlation.py
```

不在原文件上继续修改，而是复制并重新实现为独立的 V5.3 推理脚本：

```text
/home/test/qxh/workspace/myvla2/openpi/inference/agilex/inference/
agilex_inference_forced_phase_anlation_5_3.py
```

V5.2 的 `agilex_inference_forced_phase_anlation.py` 必须保持不变，用于回归对比。
V5.3 本节后续的按键、状态机、`qpos_h30` 采集和 `adjustment_end`
请求逻辑均在新脚本中实现。

保留：

- 确定性 action noise 规则；
- 方向/幅度的 forced recovery plan；
- 第 7 维夹爪安全下限；
- `s` 在当前阶段执行下一个 chunk；
- SPACE 停止 execution 当前 chunk、开始 attempt2 并切换到 adjustment；
- q 安全停止。

不再使用 `1/2/3` 或其他按键人工选择 execution/adjustment。

### 9.1 专用 H30 实际反馈缓存

每个 raw action 发布后，读取一次机器人实际 qpos 反馈：

```python
chunk_feedback_qpos.append(actual_qpos)
chunk_feedback_timestamps.append(actual_qpos_timestamp)
```

该缓存保存的不是：

- 模型预测 action；
- 发布命令值；
- 普通 60 帧 state-history snapshot；
- chunk 结束后等待期间的静止 qpos。

验证配置固定 `chunk_size=30`。只有完整执行 30 个 raw action 时才允许
调用 `adjustment_end`。如果启用 action interpolation，每个 raw action 只保留该 raw
action 的最后一个插值命令执行后反馈。chunk 被按键中断或反馈不足 30 条时
不做分类。

30 条反馈时间戳必须严格递增。第 30 条 action 反馈记录完成后，
必须等待一条更新的机器人状态，将其作为分类时的
`current_qpos`。必须满足：

```text
feedback_timestamp[0]
  < ...
  < feedback_timestamp[29]
  < current_qpos_timestamp
```

`qpos_h30` 只由这 30 条 feedback qpos 构成，`current_qpos` 不得复用
第 30 条 feedback。出现重复/逆序时间戳、反馈不足 30 条，或无法
取得比最后一条 feedback 更新的 current qpos 时，不得调用
`adjustment_end`；保持 adjustment，停止动作并等待人工处理。

真机分类用的专用 feedback deque 是单 chunk 缓存：每个 adjustment
chunk 开始前清空，结束时必须恰好包含该 chunk 的 30 条 feedback。它不得
跨 chunk 或跨 attempt。训练数据在 attempt2 开头允许 qpos 历史跨
attempt1→attempt2，是为了保留早期逐帧负样本；真机第一次分类发生在
完整第一个 adjustment H30 之后，因此不需要跨 attempt feedback。

V5.2 Stage A 的普通 60 帧 history 仍按原有方案在新 attempt 时重置，
与单 chunk feedback deque 不得混用。

### 9.2 同步判断和状态机

adjustment H30 完整执行后：

1. 确认 30 条 action feedback qpos 及时间戳完整、严格递增；
2. 等待一条时间戳严格晚于最后 action feedback 的 current qpos；
3. 另外获取一帧最新同步 front/left 图像；
4. 获取当前触觉 caption；
5. 将刚执行的 30 条实际 qpos 反馈均匀采样成 10 条；
6. 用相同 norm stats 离散化并构造 `Mode: adjustment` 评估 prompt；
7. 同步发送 `mode=adjustment_end`；
8. 只在结果返回后才决定下一个 action chunk。

front image、left image 和 current qpos 必须由 V5.3 同步观测函数一次取得。
三者时间戳都必须严格晚于最后一条 action feedback，并满足：

```text
max(front_timestamp, left_timestamp, current_qpos_timestamp)
- min(front_timestamp, left_timestamp, current_qpos_timestamp)
<= 0.05 seconds
```

超过 50 ms 时继续等待新观测，不得用旧图像或将不同时刻的字段拼接后
发送分类请求。

真机 caption 必须使用与 V4 标签相同的：

```text
/data1/outputs/tactile_captioner/tcn_v3_w30_rotation_head/best.pt
window_size=30
```

V5.3 `adjustment_end` 运行禁止 `--no-captioner`。当 ROS 触觉消息没有 header 时，
以 V5.3 客户端收齐构成一帧所需的六个触觉 topic 时的本机 ROS 时间作为
该触觉帧时间戳。caption 窗口末帧与 current qpos 的时间差也必须
不超过 50 ms。captioner checkpoint/SHA 不匹配、窗口不足 30 帧或触觉
时间不同步均视为分类输入异常，保持 adjustment 并进入只接受 `q`
的安全停止状态。

状态机：

```text
EXECUTION
    |
    | SPACE
    v
ADJUSTMENT
    |
    | execute complete H30
    v
ADJUSTMENT_END classification
    | false -> ADJUSTMENT, do not reset history
    | true  -> EXECUTION,  do not reset history
```

按键和 chunk 调度的确定行为为：

- 启动时状态为 `execution`；
- 按 `s` 时才获取实时观测，并使用当前状态的 prompt 生成、执行一个 H30 chunk；
- execution chunk 结束后暂停，不调用 `adjustment_end`，等待下一次 `s`；
- 在 execution 中按 SPACE 时切换到 `adjustment`；
- adjustment chunk 完整执行后同步调用一次 `adjustment_end`，再暂停；
- `adjustment_end=false` 时保持 adjustment，下一次按 `s` 再执行下一个 adjustment chunk；
- `adjustment_end=true` 时自动把阶段状态切换为 execution，但不自动执行 action，等待下一次 `s`；
- adjustment 中按 SPACE 不改变阶段，只给出当前已在 adjustment 的提示；
- 只有 `adjustment_end=true` 能将 adjustment 切换回 execution，不保留人工阶段切换键。

`execution` 阶段不发送 `adjustment_end` 请求。`adjustment` 阶段不发送异步
`need_recovery`。adjustment 到 execution 切换不重置 Stage A history，下一个 action
chunk 使用新获取的实时观测。

普通 Stage A `state_history` 和分类专用 `qpos_h30` 是两个独立缓存，
不得混用。每个 action chunk 发布完最后一个 raw action 后：

- Stage A 60×7 `state_history` 继续采集 1 秒实际 qpos，然后冻结；
- 等待用户按 `s` 期间，冻结的 Stage A history 不再刷新；
- 按 `s` 后，front/left 图像、current qpos 和触觉 caption 使用按键后的
  实时数据，Stage A action 请求使用上一个 chunk 结束 1 秒后冻结的
  60 帧 history；
- 开始执行新 chunk 时恢复 Stage A history 更新；
- 分类专用 `qpos_h30` 只接收每个 raw action 执行后的一条反馈，
  不记录 chunk 结束后的 1 秒、分类请求耗时或人工等待期间的 qpos。

不再保留“人工选择 phase”步骤；阶段仍由 SPACE 和
`adjustment_end` 状态机唯一决定。

客户端不增加 `--adjustment-end-threshold`；threshold 只由服务端从
checkpoint metadata 加载和执行。
同时不设置 `max_adjustment_chunks` 或其他 adjustment chunk 数量上限。
每个 adjustment chunk 都必须由用户再次按 `s` 才会执行；只要
`adjustment_end=false`，就保持 adjustment，直到后续某次判断为 `true`
或用户按 `q` 停止。

`adjustment_end` 请求采用 fail-closed 安全规则。以下任一情况均视为
分类服务失败：

- WebSocket 超时、断开或服务端报错；
- 响应缺少 `adjustment_end` 或布尔类型非法；
- `adjustment_end_probs` 不是两个有限数、不在 `[0,1]` 或和不近似 1；
- server metadata、head checkpoint 或 prompt/qpos 配置不一致。

失败不得映射为 `false` 或 `true`，不自动重试，也不再执行下一个
action chunk。客户端必须保持 adjustment，停止动作发布，记录完整
请求输入、原始响应和错误，并进入只接受 `q` 退出的安全停止状态。
排除服务问题后必须重新启动验证；绝不因分类服务异常切换到
execution。

每次判断记录：

```json
{
  "event": "adjustment_end_classification",
  "phase": "adjustment",
  "phase_index": 3,
  "completed_raw_actions": 30,
  "feedback_qpos_h30": [],
  "feedback_qpos_timestamps": [],
  "sampled_qpos_h10": [],
  "discrete_qpos_h10": [],
  "current_qpos": [],
  "current_qpos_timestamp": 1785658883.897981,
  "prompt": "...",
  "adjustment_end": true,
  "adjustment_end_probs": [0.08, 0.92],
  "threshold": 0.5
}
```

## 10. 离线评估和验收

### 10.1 数据验收

- 只有 V5.2 选中的 408 个 attempt2；
- physical episode selection 及 train/val/test 与 V5.2 逐项相同；
- 有 attempt1 的 attempt2 第 0 帧使用同 episode attempt1 的完整 30 帧历史；
- 缺少 attempt1 的 80 个 attempt2 取消 frame 0--29，frame 30 起历史完整；
- 每个 history 严格 30×7，采样后严格 10×7；
- history 可跨 attempt，但跨 physical episode 数必须为 0；
- 离散化结果与现有 π0.5 state tokenizer 逐项一致，不得额外 clip；
- 归一化值不低于 `-1` 时离散值为 `0..255`，低于 `-1` 时允许与
  π0.5 一致的 `-1`，非有限 qpos 数量必须为 0；
- 正样本严格是 `[R-5,R]`；
- `R` 之后的 execution 帧 `adjustment_end_valid=false`；
- prompt token 截断数为 0；
- V5.2 index、boundary、norm stats、caption 源和新产物哈希完整。

### 10.2 模型验收

- 只有 `adjustment_end_head` 参数改变；
- Stage A backbone 在训练前后逐项一致；
- 固定 seed 时采样、batch 和 loss 可复现；
- val 集选定 threshold，test 集只做一次锁定评估；
- val threshold 同时满足 positive-window recall 不低于 80% 和 early
  false-positive rate 不高于 1%，合格候选中取最高值；
- 无合格 threshold 时直接验收失败，不进入真机测试；
- 报告 precision、recall、F1、AUROC、AUPRC 和 early false-positive rate；
- 不以高度不平衡数据上的 overall accuracy 作为主要指标。

### 10.3 H30 端点仿真

标签正窗口只有 6 帧，而真机只在每个完整 H30 结束后判断一次。即使逐帧
F1 较高，chunk 终点也可能跳过 `[R-5,R]`。

因此必须对 30 种 chunk 起点偏移做离线仿真，报告：

- 每个 attempt2 是否能在 adjustment 期间首次触发；
- 首次触发相对 `R` 的提前/延迟帧数；
- episode-level transition recall；
- 过早切换率；
- 未触发率。

端点索引必须与训练的 `qpos[t-30:t]` 定义完全一致。对离线轨迹中
从帧 `s` 开始的 chunk，第一次分类定义为：

```text
t = s + 30
qpos_h30     = qpos[s:s+30]   # s ... s+29
current_qpos = qpos[s+30]
```

后续分类端点为 `s+60, s+90, ...`，不得使用 `s+29, s+59, ...`。
30 种起点偏移固定为 `s=0..29`。训练、端点仿真和 replay 测试必须
对这一 off-by-one 规则做显式断言。

H30 端点仿真必须作为真机实验前的硬门槛。本版本先严格保留
`[R-5,R]` 的 6 帧正标签；如果仿真证明 H30 终点大量错过该窗口，再单独评审
是否改标签窗口或缩短 adjustment 发布 chunk，不在数据构建时静默改动。

该结构存在已知上限：对单个 attempt 的 30 种 chunk 起点偏移，
最多只有 6 种偏移的端点能落入 6 帧正窗口；对严格按逐帧标签输出的
理想分类器，30 偏移全体窗口命中率上限为 `6/30=20%`。因此：

- 保持原实验设计，不为提高指标暗中扩展正标签窗口；
- 保持每个完整 H30 后只判断一次；
- 30 偏移仿真分开报告“存在端点落入正窗口的可命中偏移”和
  “结构上跳过正窗口的不可命中偏移”；
- 同时单独报告真机实际起点对齐规则下的 episode-level transition recall；
- 真机实际起点规则固定为 attempt2 从 `s=0` 开始，在帧 30 首次判断，
  后续在帧 60、90…判断；
- `s=0` 对齐的 val H30 仿真必须同时满足：

```text
episode-level transition recall >= 80%
early-transition episode rate <= 5%
miss rate <= 20%
```

- transition recall 表示在正确结束窗口触发切换的 attempt 比例；
- early transition 表示在 `R-5` 之前触发的 attempt 比例；
- miss 表示整个有效 adjustment 区间都未触发的 attempt 比例；
- 任一门槛不满足时，V5.3 验收失败并停止，不进入真机测试；
  改标签窗口或提高判断频率必须另立新版本。

### 10.4 推理逻辑测试

- execution 阶段从不调用 `adjustment_end`；
- 每个完整 adjustment H30 恰好调用一次；
- 中断或不足 30 个 raw action 时不调用；
- 30 条 feedback 时间戳严格递增，且 current qpos 时间戳严格晚于
  最后 feedback；
- front/left/current qpos 都晚于最后 feedback 且相互时间差不超过 50 ms；
- 必须使用 V4 同款 rotation captioner 和 30 帧窗口，caption 窗口末帧与
  current qpos 的时间差不超过 50 ms；
- captioner 未就绪、禁用或不同步时安全停止，不发送分类请求；
- 时间戳非法或无法获取更新 current qpos 时保持 adjustment 并安全暂停；
- 评估 prompt 使用 30 条实际反馈，不是预测 action；
- `false` 保持 adjustment，`true` 切换 execution；
- adjustment 到 execution 不重置 Stage A history；
- action chunk 结束后 Stage A history 继续采集 1 秒再冻结，等待 `s`
  期间不刷新；
- 按 `s` 后 action 请求使用实时图像/current qpos/触觉 caption 和冻结的
  Stage A 60 帧 history；
- Stage A 的 1 秒延迟冻结和人工等待数据不得写入分类专用
  `qpos_h30`；
- 相同输入和 threshold 产生相同分类结果；
- 客户端不重新根据 probability 计算布尔值，不允许覆盖服务端
  threshold；
- 服务请求或响应异常时保持 adjustment、不重试、不再发布 action，
  并进入只接受 `q` 的安全停止状态；
- 不存在 adjustment chunk 数量上限；`false` 后只有再次按 `s` 才执行
  下一 chunk，`q` 始终可安全停止。

## 11. 后续 needRecovery

只有 adjustment_end 通过离线仿真和真机验证后，才开始训练新的
`need_recovery_head`。它将使用同样的：

```text
front image
left wrist image
current qpos
past H30 qpos -> uniform H10 -> discrete text
tactile caption
recovery plan
```

只将 prompt 的 mode 改为 `execution`。最终模型包含共享的 PaliGemma backbone
和两个并列头：

```text
shared PaliGemma prefix
├── need_recovery_head       # 只在 execution 有效
└── adjustment_end_head      # 只在 adjustment 有效
```

推理状态机将在后续版本再扩展为：

```text
EXECUTION --need_recovery=true--> ADJUSTMENT
ADJUSTMENT --adjustment_end=true--> EXECUTION
```

## 12. 实现文件与运行命令

### 12.0 当前联合训练命令

当前实现入口是：

```text
scripts/train_vla_adjustment_end_multitask_v5_3.py
```

先做不初始化模型的真实数据 dry-run；该命令会完整验证两个 index、manifest、
两个 loader、action H30 形状和分类 batch 的 2 正/6 负组成：

```bash
PYTHONWARNINGS='ignore:The video decoding and encoding capabilities of torchvision are deprecated:UserWarning:torchvision.io._video_deprecation_warning' \
USE_TF=0 openpi/.venv/bin/python \
  scripts/train_vla_adjustment_end_multitask_v5_3.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_rotation_v4 \
  --action-index-file /data1/outputs/vla/rotation_v5_adjustment_v2/v5_prompt_training_index.json \
  --index-file /data1/outputs/vla/rotation_v5_adjustment_end_v1/adjustment_end_training_index.json \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --stage-a-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_phase_v5_2_1/15000 \
  --output-dir /data1/outputs/vla/adjustment_end_v5_3 \
  --run-name pi05_adjustment_end_rotation_v5_3_multitask \
  --dry-run
```

正式训练：

```bash
PYTHONWARNINGS='ignore:The video decoding and encoding capabilities of torchvision are deprecated:UserWarning:torchvision.io._video_deprecation_warning' \
USE_TF=0 openpi/.venv/bin/python \
  scripts/train_vla_adjustment_end_multitask_v5_3.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_rotation_v4 \
  --action-index-file /data1/outputs/vla/rotation_v5_adjustment_v2/v5_prompt_training_index.json \
  --index-file /data1/outputs/vla/rotation_v5_adjustment_end_v1/adjustment_end_training_index.json \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --stage-a-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_phase_v5_2_1/15000 \
  --output-dir /data1/outputs/vla/adjustment_end_v5_3 \
  --run-name pi05_adjustment_end_rotation_v5_3_multitask \
  --eval-interval 1000 \
  --resume
```

从同步保存的 checkpoint 恢复时追加 `--resume`。最终固定使用 step 8000，
阈值及 `R-30..R` 分段输出位于：

```text
/data1/outputs/vla/adjustment_end_v5_3/
  pi05_adjustment_end_rotation_v5_3_multitask/8000/
    adjustment_end_metadata.json
    relative_probability_profile.json
    val_predictions.jsonl
    test_predictions.jsonl
    delta_params/
```

联合 checkpoint 的服务命令：

```bash
USE_TF=0 openpi/.venv/bin/python \
  scripts/serve_tactile_vla_v5_3.py \
  --stage-a-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_phase_v5_2_1/15000 \
  --adjustment-end-checkpoint /data1/outputs/vla/adjustment_end_v5_3/pi05_adjustment_end_rotation_v5_3_multitask/8000 \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --adjustment-end-threshold-override 0.7 \
  --host 0.0.0.0 \
  --port 8000
```

服务端先加载 V5.2.1 Stage A，再覆盖联合 checkpoint 中的 PaliGemma LoRA
和 `adjustment_end_head`；因此 action 与分类必须使用同一个更新后的 wrapper，
不能继续让 action 请求走未覆盖 LoRA 的旧 Stage A 对象。

`--adjustment-end-threshold-override 0.7` 只覆盖本次服务进程使用的 threshold，
不会改写 step 8000 checkpoint 或训练评估 metadata。服务端 metadata 会明确记录
checkpoint threshold `0.9696840643882751`、runtime threshold `0.7`、
`threshold_policy=manual_robot_probe_override` 和
`adjustment_end_experimental_override=true`。因此真机客户端必须追加
`--allow-experimental-adjustment-end`。

离线 val 回放显示 threshold 0.7 的逐帧 recall 为 `84.47%`，early FPR 为
`5.77%`；按真机 `s=0` H30 端点模拟，44 个 episode 中正确窗口触发 6 个、
过早触发 18 个、未触发 20 个。该阈值具有明显的提前切换风险，只用于人工
逐 chunk 观察的真机实验；分类为 true 后仍暂停，不自动执行 execution chunk。

V5.3 使用以下新增文件，不覆盖 V5.2：

```text
src/tactile_vla/vla/v5_3_phase_change.py
src/tactile_vla/vla/v5_3_adjustment_end_data.py
src/tactile_vla/vla/v5_3_adjustment_end_model.py
src/tactile_vla/vla/v5_3_adjustment_end_checkpoint.py
scripts/build_v5_3_adjustment_end_data.py
scripts/train_vla_adjustment_end_v5_3.py
scripts/train_vla_adjustment_end_multitask_v5_3.py
scripts/serve_tactile_vla_v5_3.py
openpi/inference/agilex/inference/agilex_inference_forced_phase_anlation_5_3.py
```

数据构建：

```bash
USE_TF=0 openpi/.venv/bin/python \
  scripts/build_v5_3_adjustment_end_data.py
```

以下为已经完成的旧版 head-only 4000-step 实验命令，仅用于复现历史结果，
不用于当前联合训练：

```bash
PYTHONWARNINGS='ignore:The video decoding and encoding capabilities of torchvision are deprecated:UserWarning:torchvision.io._video_deprecation_warning' \
USE_TF=0 openpi/.venv/bin/python \
  scripts/train_vla_adjustment_end_v5_3.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_rotation_v4 \
  --index-file /data1/outputs/vla/rotation_v5_adjustment_end_v1/adjustment_end_training_index.json \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --stage-a-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_phase_v5_2_1/15000 \
  --output-dir /data1/outputs/vla/adjustment_end_v5_3 \
  --run-name pi05_adjustment_end_rotation_v5_3 \
  --resume
```

如果 step 4000 没有 threshold 能同时满足正式验收门槛，但需要在
人工逐 chunk 确认的前提下进行一次非正式真机 probe，不重新训练，
只恢复 step 4000 并跑一次 val：

```bash
PYTHONWARNINGS='ignore:The video decoding and encoding capabilities of torchvision are deprecated:UserWarning:torchvision.io._video_deprecation_warning' \
USE_TF=0 openpi/.venv/bin/python \
  scripts/train_vla_adjustment_end_v5_3.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_rotation_v4 \
  --index-file /data1/outputs/vla/rotation_v5_adjustment_end_v1/adjustment_end_training_index.json \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --stage-a-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_phase_v5_2_1/15000 \
  --output-dir /data1/outputs/vla/adjustment_end_v5_3 \
  --run-name pi05_adjustment_end_rotation_v5_3 \
  --resume \
  --eval-only \
  --export-experimental-robot-probe
```

该命令不改动原始 `4000/`，固定导出到同级
`4000_robot_probe/`。实验 threshold 在 `early FPR <= 1%` 的候选中选
recall 最高者，recall 相同时选更高的 threshold。metadata 必须保持
`accepted_for_robot=false`、`experimental_override=true`，不得将该产物冒充
正式验收通过的 checkpoint。

服务端同时提供 V5.2 action 和 V5.3 `adjustment_end`：

```bash
USE_TF=0 openpi/.venv/bin/python \
  scripts/serve_tactile_vla_v5_3.py \
  --stage-a-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_phase_v5_2_1/15000 \
  --adjustment-end-checkpoint /data1/outputs/vla/adjustment_end_v5_3/pi05_adjustment_end_rotation_v5_3/4000 \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --host 0.0.0.0 \
  --port 8000
```

真机客户端示例：

```bash
USE_TF=0 \
LD_PRELOAD=/lib/x86_64-linux-gnu/libffi.so.7 \
python inference/agilex/inference/agilex_inference_forced_phase_anlation_5_3.py \
  --host localhost \
  --port 8000 \
  --rotation-direction right \
  --rotation-magnitude moderately \
  --noise-seed 42 \
  --gripper-min 0.024 \
  --allow-experimental-adjustment-end
```

`--allow-experimental-adjustment-end` 仅在 server metadata 标记为非正式
probe 时需要。分类头输出 true 后仍先暂停，人工确认控制台已显示
`[EXECUTION]` 且物体位置正确后才能按 `s`。


#### 异步运行
修改了推理代码，无需每个chunk按s，只要切换调整时按空格。
adjustment_end 头异步运行，频率最高7hz。实际2-5hz

USE_TF=0 \
LD_PRELOAD=/lib/x86_64-linux-gnu/libffi.so.7 \
python inference/agilex/inference/agilex_inference_forced_phase_anlation_5_3_asyn.py \
    --host localhost \
    --port 8000 \
    --rotation-direction right \
    --rotation-magnitude moderately \
    --noise-seed 42 \
    --gripper-min 0.024 \
    --adjustment-end-rate-hz 7 \
    --allow-experimental-adjustment-end \
    --publish_rate 25

## 2026-09-01 正标签窗口扩展

本轮将正标签改为闭区间 `R-10 <= frame <= R+5`，分类有效区间同步延长到
`frame <= R+5`；其他联合训练设置不变。新数据使用
`data_profile=rotation_phase_v5_adjustment_end_v2`，输出到
`/data1/outputs/vla/rotation_v5_adjustment_end_v2`，不覆盖 v1。

train/val/test 正样本数分别为 5248/704/576，有效样本数分别为
74480/9684/8190。预测与评估必须显式携带原始 `rexecution_frame`；H30 回放
以 `R-10` 前为 early、`[R-10,R+5]` 为正确切换窗口，并统计 `R-30..R+5`。

联合训练默认新 run name 为
`pi05_adjustment_end_rotation_v5_3_multitask_r10_p5`。从 V5.2.1 Stage A
重新训练，不对旧 `[R-5,R]` checkpoint 使用 `--resume`。

训练：
```bash
PYTHONWARNINGS='ignore:The video decoding and encoding capabilities of torchvision are
deprecated:UserWarning:torchvision.io._video_deprecation_warning' \
USE_TF=0 \
python scripts/train_vla_adjustment_end_multitask_v5_3.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_rotation_v4 \
  --action-index-file /data1/outputs/vla/rotation_v5_adjustment_v2/v5_prompt_training_index.json \
  --index-file /data1/outputs/vla/rotation_v5_adjustment_end_v2/adjustment_end_training_index.json \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --stage-a-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_phase_v5_2_1/15000 \
  --output-dir /data1/outputs/vla/adjustment_end_v5_3 \
  --run-name pi05_adjustment_end_rotation_v5_3_multitask_r10_p5 \
  --eval-interval 1000
```
  ### 部署服务命令

  训练到 8000 步后，只需把 adjustment-end checkpoint 改为新 run：
```bash
USE_TF=0 openpi/.venv/bin/python \
  scripts/serve_tactile_vla_v5_3.py \
  --stage-a-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_phase_v5_2_1/15000 \
  --adjustment-end-checkpoint /data1/outputs/vla/adjustment_end_v5_3/pi05_adjustment_end_rotation_v5_3_multitask_r10_p5/8000 \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --host 0.0.0.0 \
  --port 8000 \
  --adjustment-end-threshold-override 0.7
```
先不要沿用旧模型的：

--adjustment-end-threshold-override 0.7

默认会使用新模型根据 val 集选出的阈值。等查看新模型评估结果后，再决定是否需要人工覆盖阈值。

真机异步客户端命令不需要因标签窗口变化而修改。

OPENPI_DATA_HOME=/home/qxh/.cache/openpi \