# V7.6

## 目标

在 V7.4 Stage-A 动作模型上继续 multitask 训练 action replay 和 `adjustment_end` 分类头。V7.6 通过反事实 recovery plan，让分类头学习 `slightly` 与 `moderately` 对应不同目标行程，而不是仅按已移动距离判断结束。

Action Expert 仍不使用连续 state history；分类 prompt 继续使用 V7.5 的 H100/11 点离散 qpos。

## 数据规则

记 `A=arm_adjustment_start`、`S=arm_adjustment_stop`、当前帧为 `p`。

原始样本不变：

- `p∈[A,S-11]`：false。
- `p∈[S-10,S+10]`：true。

反事实样本与原始样本共享完全相同的图像、触觉描述、当前 qpos、H100 采样帧及静止帧扰动；只修改 recovery plan 中的水平移动程度：

- 原 `moderately` 改成 `slightly`：令 `S_s=floor((A+S)/2)`，`p∈[A,S_s-1]` 为 false，`p∈[S_s,S]` 为 true。
- 原 `slightly` 改成 `moderately`：`p∈[A,S]` 全部为 false。

静止区间的随机保留帧数 `k=0:1:2` 按 17:2:1 做确定性最大余数分配，仍要求历史重叠长度 `l>k×10`，随机种子为 42。分配以物理历史为单位，之后再复制反事实 prompt，保证成对样本的历史完全一致。

训练集的 `slightly→moderately` 负样本从 4620 条缩减到 975 条：

- 所有 `p∈[S-10,S]` 的 880 条困难负样本均保留。
- 再从 `[A,S-11]` 按 episode 可用容量做最大余数分配，使用 seed 42 的稳定哈希保留 95 条。
- 验证集和测试集保留全部反事实样本，用于完整评估。

最终数量：

| split | false | true | total | 正负比 |
|---|---:|---:|---:|---:|
| train | 10490 | 5245 | 15735 | 1:2 |
| val | 1950 | 660 | 2610 | 1:2.95 |
| test | 1565 | 629 | 2194 | 1:2.49 |

训练分类 batch 不再强制 1:3，而是对缩减后的训练 manifest 做确定性自然 shuffle；不足一个 batch 的 epoch 尾部接到下一 epoch，不丢样本。长期有效比例与训练清单一致，为 1:2。

## 实现文件

- `src/tactile_vla/vla/v7_6_adjustment_end_data.py`：反事实标注、负样本缩减、17:2:1 历史扰动、manifest/index 及自然采样器。
- `src/tactile_vla/vla/v7_6_adjustment_end_evaluation.py`：事实样本时序统计和成对 prompt 敏感性评估。
- `scripts/build_v7_6_adjustment_end_data.py`：数据构建入口。
- `scripts/train_vla_adjustment_end_multitask_v7_6.py`：V7.4 action replay + V7.6 分类训练。
- `tests/test_v7_6_adjustment_end.py`：边界、比例、缩减策略与可恢复采样测试。

## 构建数据

```bash
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/build_v7_6_adjustment_end_data.py
```

默认输出：

```text
/data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_6_adjustment_end_counterfactual_h100/
├── adjustment_end_manifest.jsonl
├── adjustment_end_training_index.json
├── adjustment_end_summary.json
└── artifact_hashes.json
```

## 训练

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 \
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/train_vla_adjustment_end_multitask_v7_6.py \
  --dataset-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/lerobot_data/tactile_vla_rotation_v4 \
  --action-index-file /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_4_adjustment/v7_4_prompt_training_index.json \
  --index-file /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_6_adjustment_end_counterfactual_h100/adjustment_end_training_index.json \
  --norm-stats-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats \
  --stage-a-checkpoint /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action/pi05_delta_tac_rotation_phase_v7_4_no_history/15000 \
  --output-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/adjustment_end_v7_6 \
  --run-name pi05_adjustment_end_rotation_v7_6_counterfactual_h100_no_history \
  --batch-size 8 \
  --num-steps 8000 \
  --no-use-state-history \
  --state-history-len 0 \
  --state-history-dim 7 \
  --history-hidden-dim 0 \
  --paligemma-variant gemma_2b_lora \
  --action-expert-variant gemma_300m_lora \
  --fsdp-devices 2 \
  --eval-interval 4000 \
  --save-interval 4000 \
  --seed 42
```

训练仍为 action 与 adjustment-end 各一个 update 交替；动作专家冻结，训练 PaliGemma LoRA 和分类头。最终 checkpoint 导出 `delta_params` 和用于部署的 `full_params`。验证输出额外记录每种样本 variant 的概率分布，以及同一图像/历史仅改变 recovery plan 后的成对排序准确率。

## 异步部署

V7.6 继续使用与 V7.5 相同的在线 H100/11 点 prompt。下面的 server 和方向按键客户端文件名保留 V7.5，以兼容现有启动流程，但内部会严格校验 V7.6 data profile、checkpoint format、反事实策略和 1:2 采样配置。

Server：

```bash
CUDA_VISIBLE_DEVICES=0 \
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/serve_tactile_vla_v7_5.py \
  --adjustment-end-checkpoint /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/adjustment_end_v7_6/pi05_adjustment_end_rotation_v7_6_counterfactual_h100_no_history/8000 \
  --norm-stats-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats \
  --captioner-checkpoint-sha256 10818320f415bfc27b2965496fa1c9be8498f13c17300e528f245398266a4118 \
  --port 8000
```

方向按键异步客户端：

```bash
USE_TF=0 \
LD_PRELOAD=/lib/x86_64-linux-gnu/libffi.so.7 \
python openpi/inference/agilex/inference/agilex_inference_forced_phase_anlation_v7_5_asyn_direction_keys.py \
  --host localhost \
  --port 8000 \
  --instruction "Pick up and transfer the object stably." \
  --noise-seed 42 \
  --gripper-min 0.035 \
  --adjustment-end-rate-hz 3 \
  --publish_rate 30 \
  --captioner_checkpoint /data1/outputs/tactile_captioner/tcn_v3_w30_rotation_head/best.pt \
  --norm-stats-file /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats/norm_stats.json \
  --classification-gripper-open-threshold 0.0985 \
  --classification-gripper-open-value 0.0995
```

如果 server 使用 `--adjustment-end-threshold-override` 手动覆盖阈值，客户端还必须增加 `--allow-experimental-adjustment-end`。
