# V5.2：Adjustment 两阶段与 H30 边界隔离

## 1. 目标与版本边界

V5.2 解决 V5 在松开夹爪后切换 `adjustment` 时倾向立即下降和闭合的问题。数据实验改为两个 prompt 阶段：

```text
attempt1: execution
attempt2 [0, rexecution_frame): adjustment
attempt2 [rexecution_frame, end]: execution
```

`adjustment` 包含完整的“放回物体→松开→抬起→水平移动”序列，`execution` 从水平移动完成、即将再次下降/闭合时开始。

新版本不覆盖旧 V5 数据和 checkpoint：

```text
data_profile=rotation_phase_v5_adjustment_v2
prompt_profile=phase_v2
experiment_kind=phase_prompt_h30_terminal_hold
output=/data1/outputs/vla/rotation_v5_adjustment_v2
```

本轮只构建 Stage A 数据和 loader，不训练 Stage B，不实现自动阶段头，不包含 `no_grasp`、`small_grasp` 或其他非旋转扰动。

## 2. 数据范围与不变量

完全沿用 V4 的：

- 448 个 episode、776 个 attempt；
- `one_success`、`moderate_lift`、`slightly_lift` 及 V4 旋转排除规则；
- train/val/test 划分和 405,105 个 action start；
- H30、action dim 32、60×7@30 Hz history 和采样顺序；
- V4 norm stats，SHA256 必须保持为 `ce228149f658f75461e06deee2106d23fc3ec07ee0a57dfd456fe82ad82a568e`。

V5.2 不重建 LeRobot，不增删 action start。唯一 target 变化是跨越 `rexecution_frame` 的 adjustment H30 尾部 terminal hold。

## 3. rexecution_frame 标注

标签源同时读取并哈希：

- `puppetRight[:,0:7]`；
- `masterRight[:,6]`；
- 左右 `force_resultant[:,2]`；
- raw timestamp；
- Piper URDF。

帧数、timestamp、episode、attempt 和 LeRobot global/frame identity 必须严格一致。

release/regrasp 仍按 V5 的夹爪+Fz 自适应规则计算，仅用于限定恢复搜索窗口和审计，不再直接决定 prompt phase。现有 17 条人工 release/regrasp 修正数值原样迁移到 V2 override schema。

`rexecution_frame` 自动检测：

1. 解析版本化 Piper URDF，由 `puppetRight[:,0:6]` 做 FK 得到末端 XYZ。
2. 对 XYZ 做 9 帧平滑。
3. 在 release/regrasp 窗口内定位松开后的最高 Z 帧（apex）。
4. 从 apex 向后搜索：未来 10 帧累计下降至少 10 mm，且其中至少 80% 帧的垂直速度不高于 -5 mm/s。
5. 取第一个满足持续下降条件的窗口首帧作为 `rexecution_frame`，其前一帧为最后一帧 adjustment。
6. regrasp 前后的水平中位数仅用于审计净水平位移，不再决定阶段边界。

水平净位移小于 10 mm、release 后抬升小于 20 mm、timestamp 不严格递增、未找到持续下降窗口、边界逆序或无法可靠分离时必须拒绝自动标签，通过 `rexecution_frame` override 修正；不退化为旧水平稳定端点、regrasp 或固定关节阈值。override 字段同时支持 attempt 内局部帧号和近似 raw timestamp。

## 4. Prompt 与 H30

Prompt 只有两种，均不包含触觉描述：

```text
Mode: execution. Task: <instruction>.
Mode: adjustment. Task: <instruction>.
Put the object back, and follow this recovery plan: <plan>.
```

构建器需正确处理句末标点，不得生成重复句号，`phase_v2` 不接受 `reposition`。

观测帧 `f` 的 H30 target 原本是 `f..f+29`。若 adjustment chunk 跨越 `R=rexecution_frame`：

```text
f .. R-1  : 保留真实 action
R .. f+29 : 重复 R-1 的 action
```

terminal hold 在读取原始 action 后、delta action 和 normalization 之前执行。execution 和不跨界 adjustment target 与 V4 完全相同。保留边界前真实的“放回物体”下降和水平/下降重叠，但 adjustment H30 不得引用任何 `R` 及之后的真实下降或闭合。

Manifest 记录：

```text
phase
rexecution_frame
raw_chunk_phase_pure
effective_chunk_phase_pure
terminal_hold_from_offset
effective_h30_modified
```

需计算真实 effective H30 内容 SHA，并单独保留 V4 H30 source/content identity。两者必须不同，但 action indices 和 V4 norm SHA 必须相同。

## 5. 输出与验收

```text
/data1/outputs/vla/rotation_v5_adjustment_v2/
├── phase_boundaries.jsonl
├── action_phase_manifest.jsonl
├── phase_boundary_overrides.json
├── phase_label_summary.json
└── v5_prompt_training_index.json
```

硬性验收：

- 精确覆盖 448 episode、776 attempt，408 个 attempt2 全部有 `rexecution_frame`；
- train/val/test action 数保持 327,738 / 40,594 / 36,773；
- selection、split、global/frame identity 和 action-start SHA 与 V4 逐项一致；
- `no_grasp=0`、`small_grasp=0`；
- attempt1 只有 execution，attempt2 边界前只有 adjustment，边界起只有 execution；
- 408 个边界预计对 11,832 个 chunk 应用 terminal hold，共覆盖 177,480 个边界后 action step；
- execution H30 逐值等于 V4；adjustment H30 不得读取 `frame >= rexecution_frame` 的真实 action；
- override、raw stream、URDF、manifest、effective H30 和 index 的 SHA 完整且可复验。

构建时先在 `/tmp` 执行端到端 dry-run，再原子写入新 `/data1` 目录，最后重新加载全部产物并复验哈希。


## 6. Stage A 训练 CLI

V5.2 必须从原始 `pi05_base` 初始化，只训练 LoRA，不得从旧 V5 `phase_v1`
checkpoint 续训。正式训练命令为：

```bash
cd /home/test/qxh/workspace/myvla2

USE_TF=0 openpi/.venv/bin/python \
  scripts/train_vla_stage_a_openpi.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_rotation_v4 \
  --index-file /data1/outputs/vla/rotation_v5_adjustment_v2/v5_prompt_training_index.json \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --output-dir /data1/outputs/vla/stage_a_action \
  --run-name pi05_delta_tac_rotation_phase_v5_3 \
  --data-profile rotation_phase_v5_adjustment_v2 \
  --prompt-profile phase_v2 \
  --experiment-kind phase_prompt_h30_terminal_hold \
  --checkpoint /home/test/.cache/modelscope/hub/models/hairuoliu/pi05_base/params \
  --batch-size 8 \
  --num-steps 15000 \
  --action-horizon 30 \
  --action-dim 32 \
  --state-history-len 60 \
  --state-history-dim 7 \
  --paligemma-variant gemma_2b_lora \
  --action-expert-variant gemma_300m_lora \
  --train-lora-only \
  --seed 42
```

输出目录为：

```text
/data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_phase_v5_2/
```

正常完成后的最终 checkpoint 为子目录 `15000`。如果同一 V5.2 run 因正常中断需要
继续训练，在上述命令末尾增加 `--resume`；resume 会严格校验 data profile、prompt、index、
effective H30 identity、norm SHA 和训练协议。不要用旧 V5 checkpoint 或旧 V5 run 目录执行
resume，也不要在已有目录上随意使用 `--overwrite`。

Stage A 为避免 host RAM 在 checkpoint 时达到峰值，固定使用以下内存策略：

- `--num-workers 0`，避免 `spawn` worker 复制整个逐帧 phase lookup；
- Orbax checkpoint 同步保存，保存完成前不读取下一个 batch；
- `config.json` 不持久化 `_v5_action_phase_lookup` 等仅运行时缓存。旧 run 的大配置仍可
  resume，校验完后会立即释放额外的配置副本。

本次 OOM 中 `1000` 是最后一个完整 checkpoint，`2000.orbax-checkpoint-tmp-3`
未提交。恢复时使用上述同一命令并在末尾添加 `--resume`，Orbax 会从 `1000`
恢复，不会把未提交的临时目录当作可恢复 checkpoint。

## 7. Stage A 推理

V5.2 仍使用 action-only server，所有 websocket 请求的 `mode` 固定为
`execution`。阶段不是独立请求字段，只由 prompt 表达。训练时使用的 H30 terminal
hold 是 target 隔离规则，推理时不得再次截断或改写模型输出。

Server 必须严格接受以下 checkpoint 配对，并继续加载 V4 norm stats：

```text
data_profile=rotation_phase_v5_adjustment_v2
prompt_profile=phase_v2
experiment_kind=phase_prompt_h30_terminal_hold
checkpoint_kind=stage-a
```

先执行 dry-run：

```bash
cd /home/test/qxh/workspace/myvla2

USE_TF=0 openpi/.venv/bin/python \
  scripts/serve_tactile_vla_action_ablation.py \
  --checkpoint-kind stage-a \
  --checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_phase_v5_2/8000 \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --num-inference-steps 10 \
  --dry-run
```

形状检查通过后去掉 `--dry-run`，增加 `--host 0.0.0.0 --port 8000` 启动
长驻 server。

USE_TF=0 openpi/.venv/bin/python \
  scripts/serve_tactile_vla_action_ablation.py \
  --checkpoint-kind stage-a \
  --checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_phase_v5_3/15000 \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --num-inference-steps 10 \
  --host 0.0.0.0 \
  --port 8000

V5.2 客户端使用两阶段按键：

```text
1 = execution
2 = adjustment
s = 使用已选择阶段和实时图像/qpos执行下一 chunk
SPACE = 停止 execution，建立 attempt2，重置 history，直接进入 adjustment
q = 停止
```

每个完整 chunk 结束后继续记录 1 秒反馈再锁定 history。暂停期间不刷新 history；按
`s` 后重新采集实时图像和 qpos，并携带锁定的 history 发起请求。adjustment 水平移动完成后
按 `1`、再按 `s` 进入 execution，不重置 history。V5.2 禁止选择 reposition；旧 V4/V5
仍保留 `1/2/3 = execution/reposition/adjustment` 的原有操作。

客户端示例：

```bash
source /opt/ros/noetic/setup.bash
source /home/test/qxh/workspace/tac_ws/devel/setup.bash
cd /home/test/qxh/workspace/myvla2/openpi

USE_TF=0 \
LD_PRELOAD=/lib/x86_64-linux-gnu/libffi.so.7 \
python inference/agilex/inference/agilex_inference_forced_phase_anlation.py \
  --host localhost \
  --port 8000 \
  --expected-data-profile rotation_phase_v5_adjustment_v2 \
  --instruction "Pick up and transfer the object stably." \
  --rotation-direction right \
  --rotation-magnitude moderately \
  --noise-seed 42 \
  --no-captioner \
  --chunk_size 30 \
  --publish_rate 30 \
  --observation-poll-rate 200 \
  --history-freeze-delay-seconds 1.0 \
  --state-history-len 60 \
  --state-history-fps 30 \
  --state-history-max-gap-seconds 0.02 \
  --gripper-min 0.024 \
  --max_publish_step 10000
```

`--gripper-min` 仍在减去 `--gripper_offset` 后作用于最终发布的第 7 维命令。
同一 phase、phase chunk index 和 noise seed 必须产生相同 noise，以支持 V4/V5/V5.2
配对比较。
