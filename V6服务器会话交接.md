# V6 服务器会话交接

更新时间：2026-09-02
Git 分支：`v6`

## 1. 服务器接手方式

```bash
git switch v6
git pull
sudo scripts/setup_server_data_links.sh
scripts/setup_server_data_links.sh --check
```

本地数据根目录为 `/data1`，服务器数据根目录为 `/data1/qxh/tac_vla`。
仓库中的历史 CLI 和 JSON manifest 大量保存了 `/data1/tac_data`、
`/data1/outputs` 绝对路径，因此采用兼容软链接，不逐个改配置：

```text
/data1/tac_data -> /data1/qxh/tac_vla/tac_data
/data1/outputs  -> /data1/qxh/tac_vla/outputs
```

映射脚本只创建缺失链接；遇到已有目录或指向错误的链接会拒绝覆盖。

## 2. 已完成版本

### V5.2 / V5.2.1

- 将 attempt2 划分为 `adjustment` 和 `execution`。
- `rexecution_frame` 表示平移结束、准备重新执行抓取的位置。
- Stage A 使用阶段 prompt 训练动作模型。
- 当前 Stage A 产物：

```text
/data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_phase_v5_2_1/15000
```

### V5.3

- 新增只在 adjustment 阶段有效的 `adjustment_end` 二分类头。
- 分类输入包括当前图像、prompt、当前 qpos 和历史 H30 qpos 的文本离散表示。
- 当前正标签窗口为闭区间 `R-10 <= frame <= R+5`。
- 已实现共享 PaliGemma LoRA 与分类头的联合训练。
- 已实现 adjustment 阶段约 7 Hz 异步检测：

```text
openpi/inference/agilex/inference/
agilex_inference_forced_phase_anlation_5_3_asyn.py
```

详细数据、训练和部署记录见 `方案V5_2.md`、`方案v5_3.md`。

V5.3 解决了此前 adjustment 阶段上升/下降动作歧义，但没有解决 execution
内部的夹爪开合时机问题。

## 3. 真机暴露的主要问题

1. attempt1 接近物体时，本应保持张开的夹爪有时提前闭合，或者一开始就没有
   充分打开。
2. adjustment 结束切换到 execution 后，第一个 chunk 下降，第二个 chunk
   尚未到达物体两侧就开始闭合，导致抓空。该问题从 V5.2 起一直存在，调整
   `adjustment_end` 阈值不能解释或根治它。

两个问题都指向 execution 内部的夹爪状态/目标表示和开合时机，而不仅是阶段
切换错误。

## 4. 当前发现与证据边界

### 4.1 训练数据尺度

现用 V4 norm stats 的夹爪状态统计为：

```text
state[6].q01 = 0
state[6].q99 = 0.0994979969
```

文件为：

```text
/data1/outputs/vla/assets/tactile_vla_rotation_v4/norm_stats.json
```

这说明训练数据约用 `[0, 0.0995]` 表达夹爪从闭合到张开。

### 4.2 真机接口上限

当前 Piper ROS 驱动将夹爪命令乘以 `1e6` 后硬裁剪到 `80000`，所以通过该
接口能够发出的最大命令是 `0.08`。代码位于：

```text
openpi/inference/agilex/Piper_ros_private-ros-noetic/
src/piper/scripts/piper_start_ms_node.py
```

仓库 Piper README 写明夹爪行程为 `0.08 m`。但是，日志中反馈最大约为
`0.08` 本身不能证明这台真机已经机械完全张开；也可能是模型输出或驱动上限
造成的。V6 必须先标定，不能直接把 `0.08` 当成未经验证的物理全开点。

### 4.3 当前动作表示

自定义训练和服务链路当前均将 `delta_action_dims` 设为 7：

```text
scripts/compute_vla_norm_stats.py
src/tactile_vla/vla/openpi_bridge.py
scripts/serve_tactile_vla_v5_3.py
```

因此前六个关节和第七维夹爪都被转换为相对当前 state 的 delta。OpenPI 自带
Aloha 配置的设计则是前六维关节使用 delta、夹爪保持 absolute。V6 需要验证
并优先改成“arm delta + gripper absolute”。

### 4.4 当前真机安全处理

现有 forced-phase 推理在发布前执行：

```text
gripper_after_offset = raw_gripper - gripper_offset
published_gripper = max(gripper_min, gripper_after_offset)
```

这只是安全下限，不会使训练输入、训练目标、推理反馈和模型输出的夹爪语义
自动一致。日志已经记录 `raw_action`、`published_action` 和 `feedback_qpos`，可用
于区分模型输出不足、推理侧变换和驱动饱和。

## 5. V6 目标

解决夹爪在 execution 阶段“未充分打开、提前闭合、未下降到抓取高度就闭合”
的问题，同时不破坏 V5.2 的动作能力和 V5.3 的 adjustment 阶段切换能力。

V6 是新的数据/归一化/训练版本。不得覆盖 V4、V5.2 或 V5.3 的 index、norm
stats、checkpoint 和部署脚本。

## 6. V6 实施顺序

### 第一步：真机夹爪标定和日志判因

在低速、安全环境中标定并记录：

- `g_closed_mechanical`：无物体时机械闭合反馈；
- `g_closed_safe`：夹物时允许的最小安全命令；
- `g_open_effective`：当前驱动下开度不再增加时的命令和反馈；
- 命令、反馈和实际指尖距离之间的对应关系；
- 是否存在死区、迟滞、左右不对称或反馈偏差。

检查若干旧真机 trial 每帧的以下字段：

```text
raw_action[6]
published_action[6]
feedback_qpos[6]
phase / chunk / action_index
```

判别原则：

- `raw_action < 0.08` 且反馈跟随：模型侧没有要求更大开度；
- `raw_action > 0.08` 但反馈停在约 `0.08`：驱动裁剪；
- command 与 feedback 长期不一致：机械、控制或反馈问题。

标定完成前，不固定训练与真机的统一端点。

### 第二步：确定统一夹爪坐标

根据标定结果确定版本化配置：

```text
source_demo_closed
source_demo_open
robot_closed_safe
robot_open_effective
```

推荐模型外部统一使用真机有效绝对位置：

```text
g_common = robot_closed_safe
         + clip((g_demo - source_demo_closed)
                / (source_demo_open - source_demo_closed), 0, 1)
         * (robot_open_effective - robot_closed_safe)
```

同一个映射必须用于：

- 训练 `observation.state[6]`；
- 训练 `action[..., 6]`；
- 分类使用的当前/历史 qpos 第七维；
- 推理输入的真机反馈；
- 模型反归一化后的夹爪命令和发布安全裁剪。

映射参数及源文件 SHA 必须写入 V6 manifest/summary，避免静默漂移。

### 第三步：改为夹爪 absolute action

- 前六维机械臂保持 delta；
- 第七维夹爪保持 absolute；
- 训练、norm stats、server 输出逆变换必须使用相同 mask；
- 禁止只修改 server 而继续复用旧 V4 norm stats/checkpoint。

实现时不要简单假设 `delta_action_dims=6` 在任意动作布局下都正确；需要为
7 维有效动作加显式 mask，并检查 padding 到 32 维后的行为。

### 第四步：构建版本化 V6 数据与 norm stats

- 保留现有 episode、attempt 和 split，除非后续实验明确要求改变；
- action H30 的机械臂目标不变，只转换夹爪维；
- 生成新的训练 index、映射 manifest、norm stats 和 SHA 校验；
- 检查 H30 不跨 attempt；
- 输出夹爪开/闭/中间状态分布以及每个 chunk 内的开合时刻。

至少验证：

- 训练全开映射值与标定的真机有效全开反馈一致；
- 训练安全闭合映射值与真机安全闭合一致；
- state 与 action 第七维使用相同坐标；
- 反归一化和 absolute 输出可精确恢复 V6 训练目标；
- V4/V5 文件未被修改。

### 第五步：训练与离线评估

先训练新的 V6 Stage A 动作模型。不要直接在 V5.3 联合 checkpoint 上继续
训练，因为它继承旧夹爪尺度和 delta 表示。

离线评估除总体 action loss 外，必须单独报告：

- gripper 第七维 loss；
- attempt1 接近阶段保持张开的正确率；
- adjustment 后 execution 下降阶段保持张开的正确率；
- 正确闭合起始帧误差；
- 闭合时末端高度相对示范的误差；
- H30 内提前闭合帧数。

若第七维仍被 32 维平均 loss 淹没，再增加显式 gripper loss 权重；不要在
没有指标的情况下先猜权重。

### 第六步：真机安全验证

先使用单 chunk、低速和可随时停止的测试：

1. execution 开始时是否主动达到有效张开位置；
2. 接近和下降过程中是否保持张开；
3. 到达示范抓取高度附近后才闭合；
4. adjustment 切回 execution 后是否仍满足以上条件；
5. 安全闭合下限是否防止夹坏物体。

在 V6 action 模型通过上述测试后，再决定是否迁移或重训 V5.3
`adjustment_end` 联合 LoRA。不要同时改变夹爪表示、分类阈值和阶段逻辑后只做
一次混合归因实验。

## 7. 暂不作为首要修复的方法

- 只调低或调高 `adjustment_end` threshold；
- 只在模型输出末端增加 `[min, max]` clip；
- 仅扩大 `gripper_min`；
- 直接增加新的 execution 子阶段；
- 直接添加 `grasp_ready` 规则。

这些方法可以作为后续安全层或消融实验，但不能修复训练/推理坐标和动作表示
不一致。如果统一尺度与 absolute gripper 后仍提前闭合，再评估 execution
子阶段、视觉/高度抓取门控或 gripper 专项 loss。

## 8. 服务器新会话的首个任务

先不要修改训练代码。完成以下只读检查并输出报告：

1. 从现有真机 trial 日志统计 `raw_action[6]`、`published_action[6]`、
   `feedback_qpos[6]` 的范围、分位数和饱和比例；
2. 分 attempt1、adjustment 后 execution 两种场景定位提前闭合的 chunk；
3. 判断每个失败样本属于模型输出、推理 offset/floor、驱动 `0.08` 裁剪，还是
   command/feedback 不跟随；
4. 给出需要人工真机标定的最小命令序列，但在获得确认前不要发布机械臂命令；
5. 基于报告确定 V6 统一端点后，再设计数据转换和训练配置。

## 9. Git 基线

V6 分支创建时基于：

```text
babb07d 完善 V5.3 调整结束训练与异步推理
```

服务器路径映射提交：

```text
0e5cfda 添加服务器数据路径兼容映射
```

开始工作前执行 `git status --short --branch`，不要覆盖服务器本地未提交修改。
