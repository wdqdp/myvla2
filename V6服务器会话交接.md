# myvla2 项目与 V6 服务器会话交接

更新时间：2026-09-02

## 一、myvla2 整体任务目标

本项目基于 π0.5/OpenPI，为 AgileX Piper 单臂机器人训练一个带视觉、机器人
本体状态和触觉信息的 VLA 系统。机器人要完成的基础任务是：

```text
接近物体 → 张开夹爪 → 下降 → 夹取 → 抬升 → 搬运
```

项目不只要求机器人复现一次成功的抓取动作，还要让它在第一次夹取失败时自主
恢复。当前重点研究的失败是：物体被夹起后发生旋转，说明抓取点不合适。

完整目标闭环是：

```text
正常执行抓取
    ↓
根据触觉判断是否需要恢复
    ↓
判断物体旋转方向并生成恢复计划
    ↓
把物体放回并松开
    ↓
按恢复计划水平调整抓取位置
    ↓
判断调整是否结束
    ↓
重新下降、夹取、抬升并完成搬运
```

最终系统应当自动完成以上闭环，同时满足两个要求：

1. 提高旋转失败后的恢复成功率；
2. 不能明显破坏原本一次抓取成功的能力。

当前实验范围只包含：

- 一次成功的正常抓取搬运；
- `right / left / front / back` 四个旋转方向；
- `slightly / moderately` 两档水平恢复距离。

当前版本明确不处理 `no_grasp`、`small_grasp`、垂直恢复、滑移、碰撞和其他
扰动。这些能力要等旋转恢复稳定后再逐类加入。

## 二、系统的训练与推理结构

动作模型以 π0.5 为基础，通过 LoRA 微调。动作输出为 H30，即一次生成未来
30 帧动作；每帧实际控制量为 7 维（六个机械臂关节和一维夹爪），模型侧
padding 到 32 维。系统还使用相机图像、当前 qpos 和历史 qpos；触觉网络先把
触觉信号转成文本 caption，供失败判断和恢复推理使用。

项目按功能分为：

- Stage A：根据当前阶段 prompt 生成机器人动作；
- 恢复判断/推理：判断 `need_recovery`、失败方向和 recovery plan；
- `adjustment_end`：在 adjustment 阶段判断水平调整是否完成，完成后切回
  execution。

阶段 prompt 的作用是消除相同观测下不同动作目的造成的歧义。当前主要阶段为：

```text
execution：正常抓取、重新抓取、抬升和搬运
adjustment：放回物体、松开、抬起并水平移动到修正后的抓取点
```

## 三、目前已经做到哪里

### V4

固定了旋转恢复专项数据：一次成功样本加四个旋转方向、两档恢复距离。数据共
448 个 episode、776 个 attempt，并固定 train/val/test 划分。Stage A 学动作，
Stage B 原设计负责 `need → failure → plan`。

### V5.2 / V5.2.1

发现恢复过程中的动作存在阶段歧义后，将 attempt2 分成：

```text
[attempt2 开始, rexecution_frame) = adjustment
[rexecution_frame, attempt2 结束] = execution
```

`rexecution_frame` 位于水平调整完成、准备重新下降的位置。跨边界的 adjustment
H30 使用 terminal hold，防止一个 adjustment target 混入后续 execution 的下降
与闭合动作。

当前 V5.2.1 Stage A checkpoint：

```text
/data1/outputs/vla/stage_a_action/
pi05_delta_tac_rotation_phase_v5_2_1/15000
```

### V5.3

增加了 `adjustment_end` 二分类头。它只在 adjustment 阶段有效，根据当前图像、
prompt、当前 qpos 和历史 H30 qpos 判断是否已经完成调整。当前正标签窗口为：

```text
R-10 <= frame <= R+5
```

已经实现共享 PaliGemma LoRA 与分类头的联合训练，也实现了 adjustment 阶段约
7 Hz 的异步判断。真机测试表明，V5.2 的阶段 prompt 已基本解决 adjustment
阶段在松开物体后对“继续抬起还是立即下降/闭合”的歧义。

详细版本记录见：

```text
方案v4.md
方案v5.md
方案V5_2.md
方案v5_3.md
```

## 四、目前最需要解决的问题

当前最急迫的问题不再是 adjustment 阶段，而是 execution 阶段的夹爪开合：

1. 第一次尝试抓取时，夹爪在接近物体的过程中本应保持张开，但有时提前闭合，
   或者没有充分打开。
2. adjustment 结束并切回 execution 后，机械臂开始下降，但还没有下降到物体
   两侧，夹爪就提前闭合，导致抓空。

第二个问题从 V5.2 起一直存在。它在不同 `adjustment_end` 阈值下仍会出现，
因此不能简单归因于阶段结束判断过早。

目前有以下待验证原因，尚不能把其中任何一项当成最终结论：

- 训练数据中的夹爪值约覆盖 `[0, 0.0995]`，当前 Piper ROS 驱动却把命令硬
  裁剪到最大 `0.08`，训练与真机可能使用了不同的夹爪尺度；
- 当前自定义链路把七维动作全部做 delta，夹爪也变成相对动作，而 OpenPI
  Aloha 的常规设计是六维手臂 delta、夹爪 absolute；
- 夹爪只有 1 维，而 loss 在 32 维动作上平均，开合时机错误可能没有得到足够
  惩罚；
- 一个宽泛的 execution prompt 同时包含接近、下降、闭合、抬升和搬运，模型
  可能仍存在 execution 内部的动作歧义。

关于 `0.08` 必须特别注意：

- 已确认它是当前 Piper ROS 驱动允许发布的命令上限；
- 仓库 Piper README 将夹爪行程描述为 `0.08 m`；
- 但仅凭真机日志最大反馈约为 `0.08`，不能证明这台真机已经机械完全张开，
  也可能是模型输出或驱动裁剪造成的。

所以在修改训练尺度前，必须比较现有 trial 中的 `raw_action[6]`、
`published_action[6]` 和 `feedback_qpos[6]`，并完成真机开度标定。

## 五、V6 的目标

V6 只集中解决夹爪开合问题：

```text
让夹爪在接近和下降阶段可靠张开并保持张开，
到达正确抓取高度后再闭合，
同时保证闭合宽度不会损坏物体。
```

这个目标同时覆盖：

- attempt1 的第一次正常抓取；
- adjustment 结束后重新进入 execution 的第二次抓取。

V6 暂时不扩展新的扰动类型，也不优先修改 `adjustment_end` 结构或阈值。只有
夹爪动作模型稳定后，再把新的 V6 action backbone 与 V5.3 自动阶段切换能力
结合。



Git 基线：

```text
babb07d 完善 V5.3 调整结束训练与异步推理
0e5cfda 添加服务器数据路径兼容映射
```

## 八、服务器路径

本地数据根目录是 `/data1`，服务器数据根目录是
`/data1/qxh/tac_vla`。服务器拉取代码后执行：

```bash
git switch v6
git pull
sudo scripts/setup_server_data_links.sh
scripts/setup_server_data_links.sh --check
```

得到：

```text
/data1/tac_data -> /data1/qxh/tac_vla/tac_data
/data1/outputs  -> /data1/qxh/tac_vla/outputs
```

之后现有 `/data1/...` CLI 和 manifest 无需逐个修改。

## 九、给服务器新会话的第一条任务

先完整阅读本文件及 `方案V5_2.md`、`方案v5_3.md`。
