# 方案 v3：结构化触觉推理与逐步恢复判断

## 1. 目标

v3 在 v2 的基础上实现：

> 基于触觉推理的偏心物体抓取多步自适应恢复。

核心变化是将最近一段触觉序列转换为结构化 tactile caption，供 VLM 判断失败、生成恢复方案并调整后续动作。v3 第一版不向 action expert 额外输入触觉记忆或本体状态记忆。

系统仍采用长度为 30 的 action chunk。发生失败时立即停止当前 attempt、丢弃剩余 action，根据失败时的同一组观测生成失败原因和下一次恢复方案。下一次 attempt 的开头由模型输出归位动作，不额外设计安全回退控制器。

## 2. 模型结构

### 2.1 输入

每次观测包含：

- 当前正面图像；
- 当前左侧图像；
- 当前本体状态 \(q_t\)；
- 最近 30 帧左右触觉力场和形变场；
- 任务指令；
- 当前 attempt 的 `recovery_plan`；
- 仅在规划下一次恢复时使用的 attempt 级 long memory。

v2 中的 60 帧本体状态历史不再输入 action expert。模型只保留 π0.5 原有的当前时刻 \(q_t\)，并继续采用离散状态编码方式。

### 2.2 Tactile Captioner

Captioner 保持 PyTorch 实现，使用同一个空间编码器和 TCN：

```text
最近30帧力场和形变场
        ↓
逐帧空间编码器
        ↓
TCN temporal encoder
        ↓
多个分类头
        ↓
结构化 tactile caption → VLM
```

Captioner 输出字段固定为：

| 字段 | 取值 |
|---|---|
| `area` 当前状态 | `none / small / medium / full` |
| `Fx/Fy` 当前状态 | `negative / near_zero / positive` |
| `Fz` 当前状态 | `negative / near_zero` |
| `rotation` | `none / clockwise / counterclockwise` |

输入 VLM 的固定格式为：

```text
Touch[
area=small;
Fx=strong negative;
Fy=near zero;
Fz=weak positive;
rotation=clockwise
]
```

左右传感器只生成一套融合描述。输入 Captioner 的左右传感器原始数据均保持不变，不进行轴交换、符号翻转或坐标变换；仅在制作监督标签时，统一按照左手传感器坐标系定义方向和旋转语义。


### 2.3 Action expert 使用本体状态 memory

V3 沿用 V2 的本体状态 memory：将最近 60 帧、7 维 puppet qpos 按 30 Hz 构造成约 2 秒历史，经 masked GRU（7→256）、LayerNorm 和 Linear（256→1024）编码为 `history_cond`，再与 flow timestep 条件相加后注入 π0.5 action expert 的 AdaRMSNorm。历史只影响 action 生成，不进入 VLM prefix，因此 `need_recovery`、`failure_reason` 和 `recovery_plan` 仍只使用图像、当前 state、触觉文本及 reasoning memory。

LeRobot 仍只保存逐帧 `observation.state=[7]`，训练 Dataset 使用 negative `delta_timestamps` 构造 `[60,7]` 和 mask；历史复用当前 state 的 norm stats。真机端缓存约 200 Hz joint topic，按时间戳最近邻重采样到 30 Hz，每个 attempt 开始时清空，避免跨 attempt 混入历史。

缓存 chunk 执行期间，最新 tactile caption 只用于异步 `need_recovery` 判断，不修改已经生成的剩余 action。生成下一条 action chunk 时，再把最新 caption 放入 prompt。

### 2.4 Prompt 去标识化

Stage A、Stage B 和真机推理的 prompt 均不输入 `case_id`、`attempt_id` 或 `failed_attempt_id`。模型只接收任务、触觉描述、当前 `recovery_plan` 和历史 `recovery_plan–failure_reason` 等语义信息，避免利用数据编号判断当前阶段。上述编号仍保留在数据和运行日志中。

Execution：

Mode: execution. Task: <instruction> <tactile caption> Recovery plan: <plan>. Output the next robot action chunk and monitor whether recovery is needed.

Assessment（`need_recovery` 与 `failure_reason` 共用）：

Mode: tactile assessment. Task: <instruction> <tactile caption> Recovery plan: <plan>. Determine whether recovery is needed and, if needed, output only the structured failure_reason.

Reasoning：

Mode: reasoning. Task: <instruction> <failed tactile caption> Failure-recovery memory: <memory>. Choose the next recovery plan.

## 3. 在线异步执行流程

真机同步测试得到：

- VLM 服务端单次 monitor 约 136 ms，理论上限约 7.3 Hz；
- action 发布到判断完成约 517 ms，同步动作频率约 1.94 Hz；
- 无法满足 action 30 Hz，因此改用异步恢复判断。

异步执行采用一个动作主线程和一个后台 assessment 任务：

1. 根据当前图像、当前本体状态和包含 tactile caption 的 prompt 生成 30-step action chunk。
2. 动作主线程按 30 Hz 连续发布缓存 action。
3. 后台始终最多保留一个 assessment 请求，读取当前最新的图像、本体状态和触觉窗口。
4. Captioner 更新 tactile caption，VLM 用共享 assessment prefix 执行 `need_recovery` 判断。
5. 动作主线程在每个 raw action 发布前后检查后台结果，但不等待未完成的请求。
6. 处理完一个 `need_recovery=false` 结果后，以当时最新观测启动下一次 assessment。

assessment 与缓存 action 发布并行，但不与 action chunk 生成或 reasoning 共用 WebSocket 并发请求。到达 chunk 边界时，先收取尚未完成的 assessment 结果，再使用同一连接生成下一条 chunk。

`execution` 请求只生成 action chunk，不再额外运行 need head；恢复判断以后台 assessment 结果为唯一来源，避免在 action 请求中重复编码同一前缀。

attempt 2 及之后的归位窗口由 `--recovery-need-ignore-seconds` 控制。窗口内 action 推理仍输入 Captioner 的真实 tactile caption，只暂停发送后台 assessment 请求，因此既不响应 `need_recovery`，也不会继续生成 `failure_reason`。该窗口按已发布的 raw action 步数换算；旧参数名 `--recovery-tactile-ignore-seconds` 作为兼容别名保留。

### 3.1 异步真机验证结论

真机实验确认异步方案可行。一次代表性结果为：

```text
captured_step=369
handled_step=372
lag_steps=3
client_monitor_ms=117.001
server_monitor_ms=115.908
caption_ms=2.210
total_monitor_ms=126.980
monitor_rate_hz=7.683
```

在缓存 action 保持 30 Hz 发布的同时，后台 VLM 可以约 7 Hz 持续判断 `need_recovery`，典型判断结果相对采集时刻滞后约 3 个 action。该延迟在当前真机任务中可以接受，因此 v3 确定采用“30 Hz action 发布 + 约 7 Hz 异步 VLM monitor”，不再采用同步逐 action VLM。

当 `need_recovery=false` 时：

- 不输出 `failure_reason`；
- 不执行文本解码；
- 不重新生成 action chunk；
- 继续发布缓存 chunk 的下一个 action。

当 `need_recovery=true` 时：

1. 立即停止并丢弃 chunk 中尚未执行的 action。
2. 固定触发时的图像、\(q_t\)、触觉 caption、任务指令和当前 `recovery_plan`。
3. 不再发起第二次 VLM 前缀计算；直接复用 assessment 已生成的 PaliGemma KV cache，受约束自回归生成 `failure_reason`。
4. 更新 attempt 级 long memory。
5. 使用更新后的 memory 和失败触发时的观测，进行第二次独立自回归，生成新的 `recovery_plan`。
6. 开始下一次 attempt，由新的 action chunk 先完成归位，再执行恢复方案。

异步测试记录以下指标：

- `observation_wait_ms`：取得一组可用最新观测的等待时间；
- `caption_ms`：触觉 Captioner 耗时；
- `client_monitor_ms`：客户端一次 assessment 请求的总耗时；
- `server_monitor_ms`：服务端 VLM 编码和判断耗时；
- `total_monitor_ms`：最新观测获取、Captioner、输入构造和 assessment 请求的总耗时；
- `monitor_rate_hz`：连续两次 monitor 完成结果的实际频率；
- `lag_steps`：结果被动作线程处理时，相对采集时刻额外发布的 raw action 数量。

每次判断以 `async_step_monitor` 事件写入 runtime JSONL 日志。

## 4. 推理输出协议

### 4.1 need_recovery

`need_recovery` 保持非自回归二分类头，由后台任务连续调用；动作线程每个 action 都检查是否已有新结果：

```text
need_recovery=false
```

或：

```text
need_recovery=true
```

### 4.2 failure_reason

仅在 `need_recovery=true` 时生成：

```text
failure_reason=rotate {rotation_direction},grasp {grasp_position}.
```

python scripts/inspect_tactile_captioner_attempt.py \
    --episode-index 75 \
    --attempt-index 1 \
    --checkpoint /data1/outputs/tactile_captioner/tcn_v3_w15_multifield/best.pt \
    --device cuda \
    --overwrite

取值范围：

```text
rotation_direction:
- left
- right
- front
- back
- none

grasp_position:
- too_high
- too_low
- appropriate
- missing
```

Captioner 输出的 `rotation=clockwise/counterclockwise` 是触觉表面的旋转观测；`failure_reason` 中的
`rotation_direction=left/right/front/back` 是 VLM 根据旋转观测、当前图像和物体姿态推理出的物体偏转方向，
不能直接复制 Captioner 的顺/逆时针标签。`none` 仅表示当前失败没有旋转问题，例如纯抓空或接触面积不足。
同理，`grasp_position` 是 VLM 根据 Captioner 输出的接触面积及视觉信息推理得到的抓取状态。

### 4.3 recovery_plan

在 long memory 更新后单独生成：

```text
recovery_plan=move horizontally {horizontal_direction} {horizontal_magnitude}, move vertically {vertical_direction} {vertical_magnitude}.
```

取值范围：

```text
horizontal_direction:
- left
- right
- front
- back
- none

horizontal_magnitude:
- slightly
- moderately
- significantly

vertical_direction:
- up
- down
- none

vertical_magnitude:
- slightly
- moderately
- significantly
```

两个 magnitude 均只从 `slightly / moderately / significantly` 中选择。

`failure_reason` 和 `recovery_plan` 均采用真正的自回归生成：

- 训练时使用 teacher forcing；
- 推理时使用 greedy decoding；
- 解码过程限制在合法模板和固定词表内；
- `failure_reason` 与 `need_recovery` 使用完全相同的 assessment prompt；服务端只编码一次图像、状态、触觉文本和任务文本；
- `need_recovery=false` 时立即结束，不做文本解码；`need_recovery=true` 时从同一 prefix 的首 token logits 和 KV cache 继续生成 `failure_reason`；
- `recovery_plan` 因输入包含新增的 failure-memory，仍使用独立 reasoning prompt 和独立前缀计算；
- 不生成 `failure_type` 或开放式长推理文本。

## 5. 触觉窗口与恢复记忆

### 5.1 触觉时间窗口

Captioner 使用最近 30 帧触觉序列生成当前 tactile caption。该时间窗口不作为 latent 输入 action expert，也不保存跨 attempt 的隐藏状态。

### 5.2 长记忆

long memory 只保存已经得到结果的恢复经验：

```text
recovery_plan=initial plan
failure_reason=<initial failure>

recovery_plan=<executed recovery plan>
failure_reason=<failure after executing the plan>
```

各条记录按时间顺序排列为完整的 `recovery_plan–failure_reason` 对，不附带 attempt 编号。第一对的 `recovery_plan` 固定为 `initial plan`。当前正在执行但结果未知的 `recovery_plan` 不提前写入 memory；只有该方案执行后仍失败，才与新的 `failure_reason` 组成完整 pair。

系统最多执行 5 次 attempt：第 1～4 次失败后，分别使用包含 1～4 个 pair 的 memory 生成下一条 recovery plan；第 5 次仍失败则直接终止，不生成第 6 条 plan。因此训练和推理输入的 memory 最多包含 4 个 pair。离线构造时，长度 1～4 的 reasoning 样本均需存在并保持近似均衡；数据先划分 train/val/test，memory donor 只能来自同一 split。

long memory 不保存逐帧图像、触觉 caption 或 action 轨迹，只在生成下一条 `recovery_plan` 时输入 VLM。

## 6. 训练任务

v3 需要训练四类能力：

1. Captioner 预测结构化 tactile caption 的多个字段；
2. π0.5 根据图像、当前本体状态和包含 tactile caption 的 prompt 预测 action chunk；
3. VLM 二分类头预测 `need_recovery`；
4. PaliGemma 使用 teacher forcing 学习两个固定模板的自回归生成。

Stage B 从 Stage A checkpoint 初始化，按 `action → need_recovery → failure_reason → recovery_plan` 的固定顺序循环更新，默认比例为 1:1:1:1。只更新 PaliGemma LoRA 和 `need_recovery` 头；视觉编码器、action expert、PaliGemma 基础权重和 token embedding 冻结。action replay 用于限制语言微调造成的动作退化。

`need_recovery` 与 `failure_reason` 的原始文本 prompt 完全相同，并统一使用结构化 `Answer:` 前缀进行 tokenization。训练时仍使用各自的数据流和 loss；共享发生在在线推理的一次 assessment 前向中，不改变现有 Stage B 四任务循环比例。

`failure_reason` 和 `recovery_plan` 的 train split 都使用失败起点及后续共 15 帧，即 `i..i+14`；val/test 每个 attempt 只使用 `i+14`，避免连续相似帧虚高指标。失败后不足 15 个连续同步帧时直接报错，不补帧。`need_recovery` 是持续监控任务，仍使用全部有效状态帧。

V3 Stage B 保存一套统一 checkpoint，包含更新后的 π0.5 参数、need head、optimizer、grammar 和标签 schema。服务端只加载该 checkpoint；assessment 阶段以一次共享前缀前向同时得到 need logits、failure 首 token logits 和 KV cache。未触发时立即返回；触发后用缓存完成 `failure_reason` 解码，再使用更新后的 memory 独立生成 `recovery_plan`。

本次统一 assessment prompt 改变了 Stage B 的训练输入，已有旧 V3 Stage B checkpoint 不能直接用于该协议，需要重新训练 Stage B。Execution prompt 和 Stage A 结构未变，因此已有 V3 Stage A checkpoint 可以继续作为初始化权重。

Captioner 和 π0.5/JAX policy 不合并为同一框架，二者只通过结构化 tactile caption 文本连接。

## 7. 当前未确定项

- v3 数据采集范式；

除上述项目外，v3 第一版的主要功能和接口已经确定。



在同一个任务 memory 内累计所有旋转类 `failure_reason`，中间出现抓空或接触面积问题时不重置。当前是第 1 次旋转时，按当前旋转方向调整 `moderately`；当前是第 2 次及以后旋转时，按当前旋转方向调整 `slightly`，不要求与上一次旋转方向相反。每个历史 plan 和最终目标 plan 都必须根据各自之前的 memory 前缀逐步校验。
垂直调整都是moderately。因为垂直能动的范围比较小。

# 训练

以下命令均基于 H30、30 Hz、60 帧本体状态历史的配置。VLA 相关命令从
`/home/test/qxh/workspace/myvla2/openpi` 执行。

## 1. 触觉 Captioner

V3 正式推理使用最近 30 帧的流动触觉窗口，因此 W30 是默认模型；W15 只作为消融实验。

```bash
cd /home/test/qxh/workspace/myvla2

python scripts/train_tactile_captioner.py \
  --dataset-dir /data1/tac_data/tac_cap_data/captioner_dataset_30 \
  --output-dir /data1/outputs/tactile_captioner \
  --run-name tcn_v3_w30_multifield \
  --batch-size 128 \
  --epochs 50 \
  --device cuda
```

W15 消融版本：

```bash
python scripts/train_tactile_captioner.py \
  --dataset-dir /data1/tac_data/tac_cap_data/captioner_dataset_15 \
  --output-dir /data1/outputs/tactile_captioner \
  --run-name tcn_v3_w15_multifield \
  --batch-size 128 \
  --epochs 50 \
  --device cuda
```

纯触觉数据训练完成后，使用演示数据单独微调旋转头。W30：

```bash
python scripts/finetune_tactile_rotation_head.py \
  --base-checkpoint /data1/outputs/tactile_captioner/tcn_v3_w30_multifield/best.pt \
  --demo-data-dir /data1/tac_data/raw_data \
  --pure-dataset-dir /data1/tac_data/tac_cap_data/captioner_dataset_30 \
  --output-dir /data1/outputs/tactile_captioner \
  --run-name tcn_v3_w30_rotation_head \
  --batch-size 128 \
  --epochs 50 \
  --num-workers 4 \
  --device cuda
```

W15：

```bash
python scripts/finetune_tactile_rotation_head.py \
  --base-checkpoint /data1/outputs/tactile_captioner/tcn_v3_w15_multifield/best.pt \
  --demo-data-dir /data1/tac_data/raw_data \
  --pure-dataset-dir /data1/tac_data/tac_cap_data/captioner_dataset_15 \
  --output-dir /data1/outputs/tactile_captioner \
  --run-name tcn_v3_w15_rotation_head \
  --batch-size 128 \
  --epochs 50 \
  --num-workers 4 \
  --device cuda
```

训练结束会在对应 run 目录生成 `best.pt`、`last.pt`、`metrics.jsonl`、
`split_manifest.json` 和 `summary.json`。部署默认使用：

```text
/data1/outputs/tactile_captioner/tcn_v3_w30_rotation_head/best.pt
```

已有 W15 基础模型测试记录：

```text
best_val_mean_macro_f1=0.979098
test_mean_macro_f1=0.954094
test_area_macro_f1=0.931985
test_fx_state_macro_f1=0.929855
test_fy_state_macro_f1=0.973902
test_fz_state_macro_f1=0.936075
test_rotation_macro_f1=0.998654
```

## 2. VLA 训练前置产物

开始 VLA 训练前应存在：

```text
/data1/tac_data/lerobot_data/tactile_vla_v3
/data1/outputs/vla/indices/splits_v3.json
/data1/outputs/vla/reasoning_manifests/v3/train.jsonl
/data1/outputs/vla/reasoning_manifests/v3/val.jsonl
/data1/outputs/vla/reasoning_manifests/v3/test.jsonl
```

split 和 reasoning manifest 的生成方法见
`/home/test/qxh/workspace/tac_ws/README.MD` 的“第三版演示数据处理”。必须先划分 split，
再在各 split 内构造 memory，禁止跨 split 组合 donor。

新增本体状态 memory 不改变落盘数据格式：已有逐帧 qpos 的 V3 LeRobot 数据可以直接使用，
不需要重新同步 raw/HDF5、重新转换 LeRobot、重新划分 split 或重新构造 reasoning manifest。
历史与当前 state 复用同一套 norm stats，因此已有匹配当前数据集的 norm stats 也可以继续使用。

### 2.1 计算 norm stats

norm stats 只使用 train split 中 `execution_eligible=true` 且具有完整 H30 action chunk 的帧：

```bash
cd /home/test/qxh/workspace/myvla2/openpi

UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
uv run python ../scripts/compute_vla_norm_stats.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_v3 \
  --split-file /data1/outputs/vla/indices/splits_v3.json \
  --output-dir /data1/outputs/vla/assets/tactile_vla_v3 \
  --action-horizon 30
```

输出：

```text
/data1/outputs/vla/assets/tactile_vla_v3/norm_stats.json
/data1/outputs/vla/assets/tactile_vla_v3/summary.json
```

### 2.2 生成 V3 index 并检查四类数据流

这一步不加载 Stage A checkpoint，可以在 Stage A 训练前执行：

```bash
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
uv run python ../scripts/train_vla_stage_b_v3.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_v3 \
  --split-file /data1/outputs/vla/indices/splits_v3.json \
  --index-file /data1/outputs/vla/indices/vla_indices_v3.json \
  --reasoning-manifest-dir /data1/outputs/vla/reasoning_manifests/v3 \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_v3 \
  --reasoning-window-frames 15 \
  --use-state-history \
  --state-history-len 60 \
  --state-history-dim 7 \
  --state-history-fps 30 \
  --history-hidden-dim 256 \
  --batch-size 2 \
  --num-workers 0 \
  --overwrite-index \
  --dry-run
```

正常情况下会打印 `action/need/failure/plan` 四个 batch。`need` 和 `failure` 的
`tokenized_prompt` 长度为 200，`plan` 的长度为 320；train 中每个有效失败应贡献 15 帧，val/test 每个失败只贡献
第 15 帧。重复检查且数据没有变化时，可去掉 `--overwrite-index`。

## 3. Stage A：H30 动作训练

V3 Stage A 使用当前 `observation.state` 和最近 60 帧本体状态历史。历史编码器只通过
flow-matching action loss 学习，不增加辅助监督。模型从本地 ModelScope 的 π0.5 base 参数初始化：

```bash
CUDA_VISIBLE_DEVICES=0 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
uv run python ../scripts/train_vla_stage_a_openpi.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_v3 \
  --split-file /data1/outputs/vla/indices/splits_v3.json \
  --index-file /data1/outputs/vla/indices/vla_indices_v3.json \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_v3 \
  --output-dir /data1/outputs/vla/stage_a_action \
  --run-name pi05_delta_tac_v3_m60 \
  --checkpoint /home/test/.cache/modelscope/hub/models/hairuoliu/pi05_base/params \
  --action-horizon 30 \
  --use-state-history \
  --state-history-len 60 \
  --state-history-dim 7 \
  --history-hidden-dim 256 \
  --batch-size 8 \
  --num-steps 15000 \
  --overwrite
```

正式训练前可以在相同命令末尾增加 `--dry-run` 检查 batch；dry-run 不应保留
`--overwrite`。正常输出 checkpoint 为：

```text
/data1/outputs/vla/stage_a_action/pi05_delta_tac_v3_state_memory/10000
```

Stage A 断点续训时去掉 `--overwrite`，添加 `--resume`，其他数据、index、norm 和模型配置不得改变。

## 4. Stage B：统一动作、监控和自回归推理训练

Stage B 从 Stage A step 10000 初始化，按照
`action → need_recovery → failure_reason → recovery_plan` 的 1:1:1:1 顺序循环训练：

- `action`：带 60 帧本体状态历史的 H30 action replay；
- `need_recovery`：二分类 head；
- `failure_reason`：失败起点 `i..i+14` 的 15 帧 teacher forcing；
- `recovery_plan`：相同 15 帧规则和对应 memory 的 teacher forcing。

只更新 PaliGemma LoRA 和 need head。Stage A 已训练好的 action expert、
`history_gru/history_norm/history_proj`、视觉编码器、PaliGemma 基础参数和 token embedding 均保持冻结。
本体历史只出现在 action 数据流；need/failure/plan 数据流不把历史输入 VLM。

```bash
CUDA_VISIBLE_DEVICES=0 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
uv run python ../scripts/train_vla_stage_b_v3.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_v3 \
  --split-file /data1/outputs/vla/indices/splits_v3.json \
  --index-file /data1/outputs/vla/indices/vla_indices_v3.json \
  --reasoning-manifest-dir /data1/outputs/vla/reasoning_manifests/v3 \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_v3 \
  --stage-a-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_v3_m60/15000 \
  --output-dir /data1/outputs/vla/stage_b_v3 \
  --run-name pi05_stage_b_v3_m60 \
  --action-horizon 30 \
  --use-state-history \
  --state-history-len 60 \
  --state-history-dim 7 \
  --state-history-fps 30 \
  --history-hidden-dim 256 \
  --reasoning-window-frames 15 \
  --batch-size 8 \
  --num-steps 10000 \
  --eval-max-text-samples 4 \
  --eval-max-need-samples 256 \
  --resume
```

带历史与不带历史的模型参数树不同，因此不能从旧 V3 Stage A/Stage B run 直接 `--resume`；必须使用上面的新 run name 从头训练。首次训练用 `--overwrite`，新 run 中断后再去掉 `--overwrite` 并添加
`--resume`，程序会先加载同一个 Stage A checkpoint，再恢复 Stage B 增量、优化器、step 和 RNG。
输出目录：

```text
/data1/outputs/vla/stage_b_v3/pi05_stage_b_v3_state_memory/
├── config.json
├── metrics.jsonl
├── <step>/params              # PaliGemma LoRA + need head + 少量模型 RNG 状态
├── <step>/train_state         # optimizer、step、RNG，不含冻结参数
└── best/
    ├── delta_params           # 最佳 Stage B 增量
    └── metrics.json
```

`best` 使用 need macro-F1、failure exact match 和 plan exact match 的平均值选模。若当前模型的
action validation loss 相对 Stage A 初始化基线恶化超过 10%，该次评估不能更新 `best`。训练阶段
不再把 6.4 GiB 冻结参数传回主机，因此不会在每 1000 step 保存时产生原来的内存峰值。

训练结束后合并用于部署的完整 checkpoint：

```bash
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
uv run python ../scripts/merge_v3_stage_b_delta.py \
  --stage-a-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_v3_m60/15000 \
  --stage-b-delta /data1/outputs/vla/stage_b_v3/pi05_stage_b_v3_m60/10000 \
  --output /data1/outputs/vla/stage_b_v3/pi05_stage_b_v3_m60/merged_10000 \
  --overwrite
```

V3 服务端：

```bash
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
uv run python ../scripts/serve_tactile_vla_policy_v3.py \
  --checkpoint /data1/outputs/vla/stage_b_v3/pi05_stage_b_v3_m60/merged_best \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_v3 \
  --port 8000
```

客户端仍使用 `agilex_inference_tactile_vla_sync_single.py`。它会根据 server metadata 自动切换到 V3 协议：action execution 使用客户端附带的 state-history；异步 assessment 不把该历史输入 VLM。触发后冻结同一观测，从共享 KV cache 生成 `failure_reason`，写入 memory 后再生成 `recovery_plan`。

## 5. 部署前检查

训练退出后单独合并 Stage A 与最佳 Stage B 增量。合并过程仍需读取并写出一次完整参数，但此时没有
训练进程占用内存：

```bash
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
uv run python ../scripts/merge_v3_stage_b_delta.py \
  --stage-a-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_v3_state_memory/10000 \
  --stage-b-delta /data1/outputs/vla/stage_b_v3/pi05_stage_b_v3_state_memory/best \
  --output /data1/outputs/vla/stage_b_v3/pi05_stage_b_v3_state_memory/merged_best \
  --overwrite
```

合并输出包含 `merged_best/params`、`config.json` 和 `merge_manifest.json`。启动统一 V3 server：

```bash
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
uv run python ../scripts/serve_tactile_vla_policy_v3.py \
  --checkpoint /data1/outputs/vla/stage_b_v3/pi05_stage_b_v3_state_memory/merged_best \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_v3 \
  --port 8000
```

真机客户端：

```bash
cd /home/test/qxh/workspace/myvla2/openpi

LD_PRELOAD=/lib/x86_64-linux-gnu/libffi.so.7 \
python inference/agilex/inference/agilex_inference_tactile_vla_sync_single.py \
  --host localhost \
  --port 8000 \
  --instruction "Pick up and transfer the object stably." \
  --captioner_checkpoint /data1/outputs/tactile_captioner/tcn_v3_w30_rotation_head/best.pt \
  --tactile_window_size 30 \
  --recovery-need-ignore-seconds 5.0 \
  --state-history-len 60 \
  --state-history-fps 30 \
  --max_publish_step 10000 \
  --chunk_size 30 \
  --max_attempts 5
```

客户端会根据 server metadata 自动启用 V3 shared-assessment 协议：30 Hz 发布 action，后台持续判断
`need_recovery`；未触发时保持约 7 Hz 的目标监控频率，触发后从同一 KV cache 生成
`failure_reason`，更新 memory 后再独立生成 `recovery_plan`。修改后的实际监控频率和触发延迟仍需在真机上重新测量。
