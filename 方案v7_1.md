# V7.1：排除阶段边界附近的静止 Action Index

## 1. 问题与目标

V7 将 attempt2 划分为：

```text
[0, rexecution_frame)   -> adjustment
[rexecution_frame, end) -> execution
```

真机测试发现，夹爪松开后模型经常连续输出多个近似静止的 adjustment chunk。
数据检查表明，这不是推理代码或 H60 history 误输入导致的：V7 no-history server
使用了正确的 `phase_v2` prompt，并且不会把 history 送入模型。

问题来自边界附近的静止训练样本：

- `move_start_timestamp` 前存在夹爪已经松开、机械臂仍在等待的 action-start；
- `rexecution_timestamp` 前存在平移已经结束、但仍标为 adjustment 的静止尾部；
- `rexecution_timestamp` 后还可能存在尚未开始下降的静止 execution 样本。

这些样本使同一个阶段 prompt 和相近观测同时对应“继续移动”和“保持不动”。V7 又不使用
历史状态，无法根据运动轨迹判断阶段内进度，因此容易反复生成静止 chunk。

V7.1 的目标是在不修改 HDF5、LeRobot 和原始动作轨迹的前提下，只从训练 index 中排除
边界附近的纯静止 action-start，减少这种阶段内动作歧义。

## 2. 静止判定

每个候选 action-start 使用模型实际训练的 H30 target 判断，同时使用 puppet qpos 反馈
交叉校验。初始机械臂阈值为：

```text
H30 内前 6 个关节相对起始帧的最大变化 <= 0.02 rad
```

还必须满足：

- 第 7 维夹爪在 H30 内变化很小；夹爪阈值根据数据中的稳定噪声分布确定并写入配置；
- 条件至少连续满足 10 帧，避免将瞬时低速误判成等待；
- 仅检查 `move_start` 和 `rexecution` 附近的连续静止段，不删除其他任务阶段的正常停顿。

夹爪仍在打开、机械臂正在抬升或 H30 已包含平移/下降动作时，样本必须保留。

## 3. Index 排除规则

### 3.1 move_start 前

定位 `move_start_frame` 左侧最后一个连续静止区间，只排除其中同时满足以下条件的
action-start：

```text
frame < move_start_frame
机械臂 H30 基本不动
夹爪已经稳定打开
H30 不包含真实平移动作
```

保留夹爪松开、抬升，以及起点虽早于 `move_start_frame`、但未来 H30 已经包含平移的样本。

### 3.2 rexecution 附近

定位包含 `rexecution_frame` 的连续静止区间：

- 排除 rexecution 前平移已经结束的静止 adjustment 尾部；
- 排除 rexecution 后尚未开始下降的静止 execution 开头；
- 从第一个具有持续下降动作的 action-start 开始重新保留 execution 样本。

被排除的静止帧不改标成另一阶段，也不参加 action loss。

## 4. H30 与数据身份

V7.1 只过滤 action-start，不从时间轴删除帧，也不把静止区间前后的动作拼接起来：

- 保留样本继续使用原始 H30 target；
- H30 已包含真实平移或下降时保留该样本；
- H30 完全静止且命中上述边界静止段时删除；
- adjustment 到 execution 的 terminal-hold 规则继续沿用 V7。

manifest 为每个候选 index 增加：

```json
{
  "trainable": false,
  "exclusion_reason": "pre_move_static_wait",
  "arm_h30_motion": 0.0081,
  "gripper_h30_motion": 0.0002
}
```

`exclusion_reason` 至少区分：

```text
pre_move_static_wait
pre_rexecution_static_wait
post_reexecution_static_wait
```

V7 原产物保持不变，V7.1 输出到独立版本目录。由于有效 action indices 已改变，必须重新
生成 index、manifest、summary 和全部哈希。为保持单变量对比，继续复用 V4 norm stats，
但在 V7.1 identity 中明确记录为 inherited norm，不能再声明 action indices 与 V7 完全一致。

## 5. 代码修改范围

1. 在 V7 数据构建逻辑上增加静止区间检测及 action-start 过滤，生成独立 V7.1 profile。
2. 构建器输出每个 episode 的排除区间、原因、阈值和过滤前后样本数。
3. Stage A loader 只采样 `trainable=true` 的 V7.1 action index，不进行静止区间拼接。
4. 训练配置、server 和推理客户端增加 V7.1 profile/experiment identity 校验。
5. 模型结构、`phase_v2` prompt、H30、LoRA 配置、训练步数和 no-history 设置保持 V7 不变。

构建完成后必须人工抽查边界图像，并验证：夹爪打开和抬升样本没有被删除，保留下来的
move-start/下降起点 H30 确实包含有效运动，且 train/val/test 不发生跨 split 变化。
