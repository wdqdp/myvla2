# V7.5

## 目标

V7.5 在 V7.4 Stage-A 动作模型基础上，用 multitask 方式训练 `adjustment_end` 分类头。Action Expert 仍无连续 state history；H100 只离散化后写入分类 prompt。

## 数据规划

设：

- `G = gripper_motion_stop`
- `A = arm_adjustment_start`
- `S = arm_adjustment_stop`
- `C = gripper_close_start`
- `p` 为当前样本帧

样本范围仅取 `p ∈ [A,S+10]`，并强制检查 `S+10<C`。标签为：

- `p ∈ [A,S-11]`：`adjustment_end=false`
- `p ∈ [S-10,S+10]`：`adjustment_end=true`

历史窗口是包含当前帧的 `H(p)=[p-99,p]`，长度 100，最终向 prompt 写入 11 个 qpos 离散点。静止内部区间为 `I=[G+1,A-1]`，令 `l=|H(p)∩I|`：

- `l=0`：正常均匀采样 11 点，不计入 8:1:1 比例。
- `l>0`：从静止内部随机保留 `k∈{0,1,2}` 帧，删掉其余静止帧，在非静止时间线上均匀采样 `11-k` 点。
- 必须满足 `l>k×10`；因此 `k=1` 要求 `l>10`，`k=2` 要求 `l>20`。
- 每个 split 的可扰动样本池中，`k=0:1:2` 按 8:1:1 做确定性最大余数分配；随机种子为 42。
- 保留帧、采样帧、`l` 和 `k` 全部写入 manifest，便于复查和复现。

预期样本数：

| split | false | true | total |
|---|---:|---:|---:|
| train | 7386 | 3024 | 10410 |
| val | 1017 | 378 | 1395 |
| test | 809 | 378 | 1187 |

## 实现文件

- `src/tactile_vla/vla/v7_5_phase_change.py`：H100/11 点 prompt 协议。
- `src/tactile_vla/vla/v7_5_adjustment_end_data.py`：标注、静止帧扰动、manifest/index 构建与验证。
- `scripts/build_v7_5_adjustment_end_data.py`：数据构建入口。
- `scripts/train_vla_adjustment_end_multitask_v7_5.py`：V7.4 action replay + V7.5 分类交替训练。
- `src/tactile_vla/vla/v7_5_adjustment_end_evaluation.py`：相对 `S` 的概率评估。
- `tests/test_v7_5_adjustment_end.py`：边界、比例和历史采样单测。

V7.4 action replay 保留非连续 `action_target_offsets`，共享 LeRobot dataset 会按实际最大 offset 加载，避免将压缩 H30 误截断为前 30 帧。

## 构建数据

```bash
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/build_v7_5_adjustment_end_data.py
```

默认输出：

```text
/data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_5_adjustment_end_h100/
├── adjustment_end_manifest.jsonl
├── adjustment_end_training_index.json
├── adjustment_end_summary.json
└── artifact_hashes.json
```

## 训练

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 \
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/train_vla_adjustment_end_multitask_v7_5.py \
  --dataset-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/lerobot_data/tactile_vla_rotation_v4 \
  --action-index-file /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_4_adjustment/v7_4_prompt_training_index.json \
  --index-file /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_5_adjustment_end_h100/adjustment_end_training_index.json \
  --norm-stats-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats \
  --stage-a-checkpoint /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action/pi05_delta_tac_rotation_phase_v7_4_no_history/15000 \
  --output-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/adjustment_end_v7_5 \
  --run-name pi05_adjustment_end_rotation_v7_5_multitask_h100_no_history \
  --batch-size 8 \
  --num-steps 8000 \
  --no-use-state-history \
  --state-history-len 0 \
  --state-history-dim 7 \
  --history-hidden-dim 0 \
  --paligemma-variant gemma_2b_lora \
  --action-expert-variant gemma_300m_lora \
  --fsdp-devices 2 \
  --seed 42
```

每两个 update 为一个完整周期：1 个 V7.4 action replay update + 1 个 adjustment-end update。动作专家冻结，训练 PaliGemma LoRA 和分类头；最终 checkpoint 同时导出 `delta_params` 和便于部署的 `full_params`。

## 异步运行

异步分类使用原始 H100：缓存 99 个已执行 action 后的 qpos，再抓取与分类图像同步的当前 qpos `p`，组成 `[p-99,p]`。对该 H100 等间隔采样 11 帧：`[0,10,20,30,40,50,59,69,79,89,99]`。
运行时按机器人实时约束，不识别或压缩 `G=gripper_motion_stop` 与 `A=arm_adjustment_start` 之间的静止段；这与离线训练时的 8:1:1 扰动策略有意不同。
启动 server 时，用部署中的 captioner 计算 SHA：

```bash
CAPTIONER=/path/to/deployed_captioner.pt
CAPTIONER_SHA=$(sha256sum "$CAPTIONER" | awk '{print $1}')

CUDA_VISIBLE_DEVICES=0 \
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/serve_tactile_vla_v7_5.py \
  --adjustment-end-checkpoint /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/adjustment_end_v7_5/pi05_adjustment_end_rotation_v7_5_multitask_h100_no_history/8000 \
  --norm-stats-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats \
  --captioner-checkpoint-sha256 "$CAPTIONER_SHA" \
  --port 8000
```
异步客户端为 `openpi/inference/agilex/inference/agilex_inference_forced_phase_anlation_v7_5_asyn.py`；保留 V5.3 的双 WebSocket、单个 in-flight 请求、过期结果丢弃与 fail-closed 机制，最多等待 99 个反馈帧后再提交第一次分类。

USE_TF=0 \
LD_PRELOAD=/lib/x86_64-linux-gnu/libffi.so.7 \
python inference/agilex/inference/agilex_inference_forced_phase_anlation_v7_5_asyn.py \
  --host localhost \
  --port 8000 \
  --rotation-direction right \
  --rotation-magnitude moderately \
  --noise-seed 42 \
  --gripper-min 0.024 \
  --adjustment-end-rate-hz 7 \
  --publish_rate 25 \
  --captioner-checkpoint /path/to/deployed_captioner.pt \
  --norm-stats-file /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats/norm_stats.json \
  --allow-experimental-adjustment-end