# 待修改

## Prompt 去标识化

- Stage A 和 Stage B status 的 Execution prompt 删除 `Case` 和 `Attempt`。
- Stage B reasoning 的 Reasoning prompt 删除 `Case` 和 `Failed attempt`。
- failure-recovery memory 删除每条记录的 attempt 编号，仅按时间顺序保留 `recovery_plan–failure_reason`。
- 数据中的 `case_id`、`attempt_id` 和 `reasoning_failed_attempt_id` 继续保留，仅用于数据组织、样本配对、运行控制和日志，不再输入模型。
- 训练和真机推理必须使用相同的新 prompt 模板。
- 修改完成后需要重新训练 Stage A 和 Stage B；无需重新生成 indices、splits 或 norm stats。

## 数据采集

### 数据类型

采集数据分为两类：

1. 一次成功数据：保持 v2 的完整演示，一个 episode 只有一个成功 attempt。
2. 恢复数据：一个 episode 固定包含两个 attempt。
   - attempt1：记录旋转、抓空或接触面积过小等失败。
   - attempt2：从 attempt1 的失败姿态开始，完整记录归位、执行 `recovery_plan`、重新抓取以及调整后的结果。

attempt2 结束前至少保留约 1 秒稳定观测，保证存在完整触觉窗口用于 Captioner 和 `need_recovery` 标注。归位动作必须包含在 attempt2 中，不能在开始录制前由人工完成。
attempt2 中需要包括成功恢复的案例和恢复失败的案例。

### 调整方向和程度

`recovery_plan` 表示相对当前抓取点的增量移动。各程度必须对应真实采集的运动轨迹，不能在离线阶段把 `moderately` 轨迹重新标成 `slightly`。具体位移需要在正式采集前通过真机标定，并在同一数据版本中保持一致。

- 当前是 memory 中第 1 次旋转：按照当前旋转方向调整 `moderately`。
- 当前是 memory 中第 2 次及以后旋转：按照当前旋转方向调整 `slightly`，不要求与上一次旋转方向相同或相反。
- 两次旋转之间出现抓空或接触面积问题时不重置旋转累计次数。
- 垂直调整统一使用 `moderately`，不根据 memory 改变程度。
- `significantly` 只用于大幅抓空，不用于旋转和垂直调整；采集 attempt2 时直接通过 `horizontal_magnitude=significantly` 标记。
- 接触面积过小时必须区分 `grasp_position=too_high/too_low`，不能只根据 `area=small` 推断垂直方向。

### 离线构造 memory

离线阶段只组合 Stage B reasoning 使用的语义 memory，不拼接或伪造原始图像、触觉序列和动作轨迹。当前观测、当前失败标签和动作监督必须来自真实采集片段。

memory 沿用 v2 的 JSON 列表形式，每一项都是一个完整的 `recovery_plan–failure_reason` 对，但不包含 attempt 编号。第一项固定为：

```text
recovery_plan=initial plan
failure_reason=<第一次抓取后的失败>
```

第 1～4 次失败后分别构造包含 1～4 个 pair 的 memory，用于生成第 2～5 次 attempt 的 recovery plan。第 5 次 attempt 仍失败则终止，不生成第 6 条 plan，所以模型输入最多包含 4 个 pair。各 split 内都要存在长度 1～4 的样本并尽量保持近似均衡，donor 不得跨 split。

`slightly` reasoning 样本的当前失败必须是同一个 memory 中第 2 次或之后的旋转，并包含以下因果结构：

```text
failure_reason=rotate A
recovery_plan=move horizontally A moderately
failure_reason=rotate B
```

其目标计划为：

```text
move horizontally B slightly
```

其中 B 可以与 A 相同，也可以与 A 相反。若两次旋转之间出现非旋转抓取失败，
旋转计数仍然保留。

`moderately` 和 `significantly` 样本也要构造包含非空 memory 的 hard negative，避免模型仅根据“是否存在 memory”判断是否输出 `slightly`。hard negative 包括：

- 有历史记录，但当前旋转是 memory 中第 1 次旋转；
- memory 很长，但之前只有抓空或接触面积问题；
- memory 中只有垂直失败；
- 当前没有旋转问题，需要执行抓空或接触面积恢复。

必须先按物体、episode 和采集批次划分 train/val/test，再分别在各 split 内构造 memory，禁止同一真实片段跨 split 使用。

### Stage A / Stage B 数据过滤

- 一次成功 attempt：参与 Stage A 动作训练和 Stage B 状态训练。
- 纯旋转失败 attempt：整个 attempt 参与 Stage A 和 Stage B。纯旋转要求 `rotation_direction != none`、`grasp_position=appropriate` 且接触面积为 `medium/full`。
- 抓空、接触面积过小或同时包含 grasp 问题的旋转 attempt：不参与 Stage A，只参与 Stage B 的状态判断和 reasoning 训练。
- recovery attempt 按调整后的最终结果使用相同规则。
- 纯旋转 attempt 在确认旋转后必须立即结束，不能继续记录归位、松开或重新抓取动作。

新增独立的 `stage_a_eligible=true/false` 字段表示该 attempt 是否适合作为动作监督，不复用表示数据本身是否有效的 `valid`。生成 `execution_indices` 时只选择 `stage_a_eligible=true` 的样本，norm stats 也必须使用完全相同的 Stage A indices。

### 需要新增的采集参数和元数据

通用结果字段：

```text
result                 # success / failure
rotation_direction     # none / left / right / front / back
grasp_position         # appropriate / missing / too_high / too_low
stage_a_eligible       # true / false
```

attempt2 的 recovery plan 字段：

```text
horizontal_direction   # left / right / front / back / none
horizontal_magnitude   # slightly / moderately / significantly
vertical_direction     # up / down / none
vertical_magnitude     # moderately
```

抓空 attempt1 只记录 `grasp_position=missing`。其恢复方向和程度不再通过额外字段推导，而是直接使用 attempt2 的 `horizontal_direction` 和 `horizontal_magnitude` 构造 Stage B reasoning 目标及 Execution prompt。`case_id`、`attempt_id` 等编号继续保留用于数据组织和日志，但不进入模型 prompt。
