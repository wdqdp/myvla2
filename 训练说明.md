# 训练说明

本文档说明本项目从数据产物到模型训练、评估和最终推理集成的训练流程。当前默认版本为 H30 state-memory V2：action chunk 长度 30、发布频率 30 Hz，并使用最近 60 帧（约 2 秒）puppet qpos 历史作为 pi05 action expert 的额外条件。旧 H30/H50 实验产物保留用于对比。

## 1. 数据准备阶段

原始数据先通过 `data_tools` 转换成训练可读的数据产物。`data_tools` 的职责只到离线数据处理，不放模型代码、训练脚本或在线推理逻辑。

### Tactile Captioner 数据

当前触觉分类数据目录：

```bash
data/tactile_captioner_data
```

关键文件：

- `meta.json`：记录数据格式、窗口长度、label map、attempt 元信息、shard 信息。
- `stats.json`：记录 `mesh_motion` 和 `force` 的归一化均值、标准差。
- `shards/*.hdf5`：保存每个 attempt 的触觉序列。
- `indices/train_balanced.npz`：Tactile Captioner 训练使用的平衡采样索引。
- `indices/val.npz`、`indices/test.npz`：验证集和测试集索引，保留原始分布。

Tactile Captioner 的训练输入只包含：

- `mesh_motion`: `[T, 12, 35, 20]`
- `force`: `[T, 6, 35, 20]`
- `label`: `none / clockwise / counterclockwise`

即使数据产物中存在 `force_resultant`，当前模型也不使用。

### VLA 数据

当前 H30 state-memory V2 的 VLA 数据目录：

```bash
/data1/tac_data/lerobot_data/tactile_vla
```

VLA 使用 LeRobot 格式。关键字段语义：

- `observation.state`：puppet jointState，作为 VLA 输入机器人状态。
- `action`：master jointState，原始数据中保存为绝对关节位置；Stage A 训练时动态转换为 delta action 监督。
- `tactile_caption`：由 `rotation_state` 模板映射得到的触觉文本输入。
- `need_recovery`：Execution Mode 的恢复判断监督。
- `failure_reason`：Execution Mode 的失败诊断监督。
- `recovery_plan`：Reasoning Mode 的恢复计划生成目标。
- `input_recovery_plan`：当前 recovery attempt 执行时持续输入给 Execution Mode 的计划。
- `failure_recovery_memory`：Reasoning Mode 输入的历史失败和恢复记录。

## 2. Tactile Captioner 训练

### 训练目标

Tactile Captioner 是触觉分类模型，不是语言模型。它读取过去一个固定窗口的触觉序列，预测窗口级旋转状态：

```text
mesh_motion + force -> none / clockwise / counterclockwise
```

预测结果再通过固定模板转成 VLA 可读的 tactile caption：

- `none` -> `Tactile: no rotation.`
- `clockwise` -> `Tactile: clockwise rotation.`
- `counterclockwise` -> `Tactile: counterclockwise rotation.`

### 训练入口

推荐使用已有的 `tactile` conda 环境：

```bash
/home/qxh/.conda/envs/tactile/bin/python scripts/train_tactile_captioner.py \
  --dataset-dir data/tactile_captioner_data \
  --run-name tcn_v1_balanced \
  --batch-size 128 \
  --epochs 50 \
  --device cuda
```

CPU smoke test：

```bash
/home/qxh/.conda/envs/tactile/bin/python scripts/train_tactile_captioner.py \
  --dataset-dir data/tactile_captioner_data \
  --run-name smoke_cpu \
  --batch-size 2 \
  --epochs 1 \
  --num-workers 0 \
  --device cpu \
  --max-train-batches 1 \
  --max-val-batches 1 \
  --max-test-batches 1
```

### 训练输出

默认输出目录：

```bash
outputs/tactile_captioner/<run_name>/
```

文件含义：

- `config.json`：本次训练使用的数据路径、batch size、学习率、epoch、模型维度等配置。
- `metrics.jsonl`：逐 epoch 训练和验证指标，每行一个 JSON。
- `best.pt`：验证集 `macro_f1` 最好的 checkpoint，后续推理优先使用这个文件。
- `last.pt`：最后一个 epoch 的 checkpoint，用于恢复或排查训练过程。
- `test_metrics.json`：使用 `best.pt` 在测试集上评估得到的最终指标。

主要指标：

- `accuracy`：整体准确率。
- `macro_f1`：三类平均 F1，作为第一版最重要的模型选择指标。
- `per_class`：每一类的 precision、recall、F1 和 support。
- `confusion_matrix`：三分类混淆矩阵，检查 `clockwise` 和 `counterclockwise` 是否混淆。

## 3. Tactile Captioner 评估

评估已训练 checkpoint：

```bash
/home/qxh/.conda/envs/tactile/bin/python scripts/eval_tactile_captioner.py \
  --checkpoint outputs/tactile_captioner/tcn_v1_balanced/best.pt \
  --dataset-dir data/tactile_captioner_data \
  --split test \
  --batch-size 128 \
  --device cuda
```

评估输出会直接打印 JSON，字段和 `test_metrics.json` 中的指标一致。若只想快速确认 checkpoint 能加载，可以加：

```bash
--device cpu --batch-size 2 --num-workers 0 --max-batches 1
```

## 4. VLA 训练阶段

当前扩展数据版 VLA 训练基于 `/data1/tac_data/lerobot_data/tactile_vla_expanded` 和 OpenPI/pi05。它沿用 V2 的 H30 state-memory 架构，只增加训练数据；旧 V2 数据、split、norm 和 checkpoint 均保留不覆盖。项目侧代码放在：

```bash
src/tactile_vla/vla
```

当前新增入口：

```bash
scripts/prepare_vla_data.py
scripts/compute_vla_norm_stats.py
scripts/train_vla_stage_a_openpi.py
scripts/train_vla_stage_b_heads.py
```

VLA 脚本建议使用 OpenPI 的 uv 环境运行：

```bash
cd /home/test/qxh/workspace/myvla2/openpi
UV_CACHE_DIR=/home/qxh/workspace/myvla2/.uv-cache uv run python ../scripts/<script>.py ...
```

脚本默认把 HuggingFace/torch cache 指到项目内 `.cache/`，避免写用户目录下的全局 cache。

### 4.1 VLA index 和 split

先按原始 `episode_id` 切分数据，保证同一个任务 episode 下的多个 attempt 不会分到不同集合。扩展数据共有 320 个原始 episode、680 个 LeRobot episode、395614 帧。

扩展 split 保留旧 V2 的 96/12/12 个 train/val/test episode，再将新增的 200 个原始 episode 按 seed=42 分配为 160/20/20。最终是 256/32/32，仍为 80%/10%/10%，同时旧 V2 的评估 episode 不换组。`prepare_vla_data.py` 会校验 split 必须完整覆盖数据集，避免复用旧 split 时静默漏掉新增 episode。

```bash
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/prepare_vla_data.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_expanded \
  --base-split-file /data1/outputs/vla/indices/splits_h30_state_memory.json \
  --split-file /data1/outputs/vla/indices/splits_h30_state_memory_expanded.json \
  --index-file /data1/outputs/vla/indices/vla_indices_h30_state_memory_expanded.json \
  --output-dir /data1/outputs/vla/indices \
  --reasoning-augment-after-frames 10 \
  --action-horizon 30
```

扩展 split 已生成。后续重建 index 时不要传 `--overwrite`，脚本会原样读取已有 expanded split；只有明确需要从旧 V2 split 重新确定性扩展时才使用 `--overwrite`。

输出：

- `/data1/outputs/vla/indices/splits_h30_state_memory_expanded.json`：按原始 `episode_id` 保存 expanded train/val/test，并记录旧 split 路径及新增 ID。
- `/data1/outputs/vla/indices/vla_indices_h30_state_memory_expanded.json`：保存 expanded H30 state-memory 三个训练分支使用的 frame index。

`vla_indices.json` 中的关键字段：

- `execution_indices`：Stage A action 训练样本，按本次 `action_horizon` 和 attempt 边界动态筛选。
- `status_indices`：Stage B 状态/诊断样本；train split 保留所有 `need_recovery=true`，并按 3:1 采样 `need_recovery=false`，优先选 failure 前约 2 秒内的 hard negative。
- `reasoning_indices`：Stage B recovery plan 样本。train/val/test 都对每个 `reasoning_has_sample=true` 的触发帧取同一 attempt 内 `t..t+10` 的窗口，模拟实际推理中 `need_recovery` 检测可能落在附近帧的情况。
- `reasoning_source_indices`：与 `reasoning_indices` 一一对应，记录每个 reasoning 样本应使用哪一帧的 `reasoning_*` 标注。窗口内后续帧的图像/状态来自自身，prompt 和 `recovery_plan` label 来自触发帧。

`reasoning_augment_after_frames=10` 表示除了触发帧 `t`，还取后续 10 帧；因此每个失败 attempt 最多贡献 11 个 recovery plan 样本。该窗口不是单纯训练增强，而是对应推理时 `need_recovery` 检测时刻可能在失败发生附近抖动的真实输入分布。

expanded 数据实测 split 概况：

- train：256 个原始 episode，303282 个有效 H30 action chunk，86156 个 status 样本，3223 个增强后 reasoning 样本。
- val：32 个原始 episode，38916 个有效 H30 action chunk，40975 个 status 样本，429 个增强后 reasoning 样本。
- test：32 个原始 episode，33696 个有效 H30 action chunk，35436 个 status 样本，308 个增强后 reasoning 样本。
- reasoning 触发帧的 back/front/left/right 分布：train=67/74/76/76，val=14/12/8/5，test=9/4/6/9；val/test 均覆盖四类，旧 val 缺少右转样本的问题不再存在。

### 4.2 VLA norm stats

Stage A/Stage B 都使用 OpenPI 的 state/action 归一化统计。统计只从 train split 的 Execution Mode 有效 action chunk 计算；其中 action 统计使用 delta action，而不是原始绝对 master jointState：

```text
delta_action[t+k] = master_jointState[t+k] - puppet_jointState[t]
```

```bash
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/compute_vla_norm_stats.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_expanded \
  --split-file /data1/outputs/vla/indices/splits_h30_state_memory_expanded.json \
  --output-dir /data1/outputs/vla/assets/tactile_vla_h30_state_memory_expanded \
  --action-horizon 30
```

输出：

- `/data1/outputs/vla/assets/tactile_vla_h30_state_memory_expanded/norm_stats.json`：OpenPI `Normalize` 使用的 `state` 和 `actions` 统计。
- `/data1/outputs/vla/assets/tactile_vla_h30_state_memory_expanded/summary.json`：记录数据路径、frame 数、action horizon 和 action space。

历史状态与当前状态同为 puppet qpos，因此 `state_history` 复用 `state` 的 quantile 统计，不额外向 `norm_stats.json` 添加新 key。expanded 正式统计已完成，使用 `action_horizon=30`，处理 train split 的 303282 个 action chunk。

### 4.3 Stage A：Execution Action

Stage A 使用 OpenPI 原生 JAX/NNX pi05 训练动作生成能力：

```text
front image + left wrist image + puppet state + 60-frame state history
  + instruction + tactile_caption + input_recovery_plan
  -> next H frames delta action chunk
```

当前脚本默认 `H=30`，使用 expanded 数据及其独立的 state-memory index/norm，checkpoint 输出到 `/data1/outputs/vla/stage_a_action/pi05_delta_tac_h30_state_memory_expanded/`。

当前机器已有完整的 pi05 base，Stage A 默认直接从下列本地路径加载：

```text
/home/test/.cache/modelscope/hub/models/hairuoliu/pi05_base/params
```

`/data1/outputs/openpi_cache/openpi-assets/checkpoints/pi05_base/params` 中的旧副本只有约 111 MB，OCDBT 数据文件被截断，会报 `OUT_OF_RANGE`，不要用于训练。只有需要重新从 Google Cloud 下载时才使用项目下载脚本：

```bash
cd /home/test/qxh/workspace/myvla2/openpi
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/download_pi05_base.py
```

下载脚本默认写入 `/data1/outputs/openpi_cache/openpi-assets/checkpoints/pi05_base/params`。只有需要强制重新下载完整 checkpoint 时才追加 `--force`。

数据映射：

- `observation.images.front` -> OpenPI `base_0_rgb`
- `observation.images.left` -> OpenPI `left_wrist_0_rgb`
- 缺失的 `right_wrist_0_rgb` 用黑图补齐，并设置 mask=false。
- `observation.state` 是 puppet jointState，输入维度 7，pad 到 pi05 的 `action_dim=32`。
- Dataset 通过 negative `delta_timestamps=-59/30..0` 查询最近 60 帧 `observation.state`；episode 开头由 LeRobot 重复首帧并返回 padding 标记。
- `state_history=[60,7]` 使用与当前 state 相同的归一化统计；`state_history_mask=[60]` 保证 masked GRU 不更新 padding 帧。
- masked GRU 为 `7 -> 256`，再经 LayerNorm 和 `256 -> 1024` 小初始化投影，最终使用 `adarms_cond=time_cond+history_cond` 注入 pi05 action expert。
- 当前 state 仍按 pi05 原有逻辑离散化到 prompt；历史模块只影响 action expert，不增加辅助监督，Stage A 仍只训练 flow-matching action loss。
- `action` 是原始 master jointState，训练 transform 在 Normalize 前将前 7 维转换为 delta action，之后 pad 到 `action_dim=32`。
- Stage A 的监督目标是 `master_jointState[t:t+H] - puppet_jointState[t]`，不是直接监督绝对 master jointState。
- 推理阶段若低级控制器需要绝对关节目标，需要把模型输出的 delta action 加回当前 puppet state。

   split    总 Stage A 样本           有 plan          无 plan
  ━━━━━━━  ━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━
   train             303282    175683，57.93%   127599，42.07%
  ───────  ─────────────────  ────────────────  ───────────────
   val                38916     23184，59.57%    15732，40.43%
  ───────  ─────────────────  ────────────────  ───────────────
   test               33696     16075，47.71%    17621，52.29%
  ───────  ─────────────────  ────────────────  ───────────────
   all               375894    214942，57.18%   160952，42.82%

dry-run 检查数据：

```bash
CUDA_VISIBLE_DEVICES=0 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python ../scripts/train_vla_stage_a_openpi.py \
  --dry-run \
  --max-frames 4 \
  --batch-size 2 \
  --num-workers 0
```

正式训练示例：

```bash
CUDA_VISIBLE_DEVICES=0 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/train_vla_stage_a_openpi.py --overwrite
```

无参数正式训练默认使用：expanded 数据、H30、60 帧历史、batch 8、10000 steps、run name `pi05_delta_tac_h30_state_memory_expanded`。`--train-lora-only` 默认开启：原有 backbone 按 LoRA 规则冻结，新增加的 GRU/LayerNorm/history projection 保持可训练。需要覆盖已有 expanded run 时显式传 `--overwrite`；继续同一 run 使用 `--resume`。

默认从 `/home/test/.cache/modelscope/hub/models/hairuoliu/pi05_base/params` 加载 JAX 版 pi05 base 参数，也可以用 `--checkpoint /path/to/pi05_base/params` 显式覆盖。base checkpoint 没有历史模块参数，loader 只允许 `history_*` 和 LoRA 参数缺失，并用当前初始化补齐；其他参数仍严格校验。学习率前 7000 step 为 `5e-5`，之后为 `5e-7`。

输出目录：

```bash
/data1/outputs/vla/stage_a_action/<run_name>/
```

输出含义：

- `config.json`：训练配置。
- `metrics.jsonl`：action flow-matching loss 日志。
- `<step>/params`：OpenPI JAX/Orbax inference 参数，可被 `openpi.models.model.restore_params()` 读取。
- `<step>/train_state`：JAX 训练状态，包含 optimizer state 等恢复训练需要的信息。

### 4.4 Stage B：Status / Diagnosis / Plan Heads

Stage B 使用 JAX/NNX 版 pi05 prefix encoder 训练三个辅助分类头。默认只训练辅助头，pi05 backbone 冻结；backbone 参数从 Stage A 的 JAX/Orbax checkpoint 读取。

- `need_recovery`：二分类，判断当前帧是否需要恢复。
- `failure_reason`：四分类，`tilted left/right/front/back`。
- `recovery_plan`：四分类，`Regrasp left/right/front/back.`。

Status 样本使用 Execution prompt，只训练 `need_recovery` 和 `failure_reason`。

- train split 中 `need_recovery=false : need_recovery=true = 3 : 1`。
- expanded train status 数据：`need_recovery=true` 为 21539，采样的 `need_recovery=false` 为 64617，合计 86156。
- `need_recovery_head` 使用全部 86156 个 status 样本。
- `failure_reason_head` 只使用其中 `need_recovery=true` 的 21539 个样本。

Reasoning 样本使用 Reasoning prompt，只训练 `recovery_plan`。每个触发帧扩展为触发帧加后续 10 帧；窗口内后续帧复用触发帧的 `reasoning_failure_recovery_memory` 和 `reasoning_recovery_plan`。Stage B 的 status/reasoning 分支只读当前帧，不读取 action chunk；action chunk 只用于 Stage A 的动作训练。第一版不做自由文本生成，分类结果再映射成固定字符串。

当前 Stage B 默认读取 `/data1/outputs/vla/stage_a_action/pi05_delta_tac_h30_state_memory_expanded/10000`，使用 expanded H30 state-memory index/norm，输出到 `/data1/outputs/vla/stage_b_heads/pi05_heads_jax_h30_state_memory_expanded/`。第一版历史只注入 action expert，Stage B prefix heads 不直接使用 history；Stage B 仍需用与 Stage A 一致的模型配置加载 backbone。

dry-run 检查数据：

```bash
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python ../scripts/train_vla_stage_b_heads.py \
  --dry-run \
  --max-status-frames 4 \
  --max-reasoning-frames 4 \
  --batch-size 2 \
  --num-workers 0
```

正式训练示例：

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/train_vla_stage_b_heads.py
```

无参数正式训练默认使用 expanded H30 state-memory Stage A step 10000、batch 8、5 epochs 和 run name `pi05_heads_jax_h30_state_memory_expanded`。Stage A step 10000 不存在时不要启动正式 Stage B；正式实验可按验证集表现显式增加 `--epochs 20`。

`--backbone-checkpoint` 可以传 Stage A 的 step 目录，也可以直接传其中的 `params` 目录；脚本会自动识别 `<step>/params`。JAX 版 Stage B 当前不支持 `--train-backbone`，该阶段只训练辅助头。

输出目录：

```bash
/data1/outputs/vla/stage_b_heads/<run_name>/
```

输出含义：

- `config.json`：训练配置。
- `metrics.jsonl`：逐 epoch loss 和 val 指标。
- `best/head_params.npz`：按 `need_recovery/failure_reason/recovery_plan` 三个 macro_f1 的平均值选择的最好辅助头参数。
- `best/metrics.json`：best checkpoint 对应指标。
- `last/head_params.npz`：最后一个 epoch 的辅助头参数。
- `last/metrics.json`：最后一个 epoch 对应指标。

Stage B checkpoint 只保存 head 参数，不保存 pi05 backbone。推理时需要同时加载 Stage A 的 JAX pi05 checkpoint 和 Stage B 的 `head_params.npz`。

### 4.5 H50 独立训练版本（action horizon = 50）

H50 用于与现有 H30 做独立对比，约定如下：

- 使用同一份 LeRobot 数据、同一份原始 episode train/val/test split、同一 pi05 base、seed=42 和 10000 个 Stage A optimizer step。
- 不覆盖 H30 的 index、norm stats 或 checkpoint。
- Stage A 从 pi05 base 独立训练，不从 H30 checkpoint 接着训练。
- H50 Stage A 完成后重新训练 Stage B heads，不能把 H30 heads 直接作为正式 H50 部署产物。
- 模型输出 50 步，真机使用 `--chunk_size 50` 完整执行 50 步；30 Hz 下每个开环 chunk 约 1.67 秒。

原始 LeRobot 数据中的 `action_chunk_valid` 是转换时按 H30 写入的。当前 index 代码已经改为按每个 attempt 的 `frame_index + action_horizon <= frame_count` 动态筛选，并在 index JSON 中记录 `action_horizon`。训练脚本会检查 index 和本次训练 horizon 是否一致；旧 index 没有该字段时只按 H30 兼容。

不需要重新编码 6.2 GB 的 LeRobot 视频。H50 已生成产物：

```text
dataset: /home/test/qxh/workspace/tac_ws/lerobot_data/tactile_vla
split:   /data1/outputs/vla/indices/splits.json
index:   /data1/outputs/vla/indices/vla_indices_h50.json
norm:    /data1/outputs/vla/assets/tactile_vla_h50
Stage A output: /data1/outputs/vla/stage_a_action/pi05_delta_tac_h50
Stage B input:  /data1/outputs/vla/stage_a_action/pi05_delta_tac_h50/10000
Stage B output: /data1/outputs/vla/stage_b_heads/pi05_heads_jax_h50
```

H50 现在是历史对比版本，下面所有 H50 命令必须显式传路径和 `--action-horizon 50`。四个脚本的无参数默认值已经切换为 H30 state-memory V2，不能再用无参数命令生成 H50 产物。

```bash
cd /home/test/qxh/workspace/myvla2/openpi

UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/prepare_vla_data.py

UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/compute_vla_norm_stats.py
```

命令行参数仍可覆盖默认路径。上面的两条无参数命令现在生成 H30 state-memory V2 的 split/index 和 norm stats。

H50 有效 Execution 样本数：

```text
train: 177103
val:    23734
test:   19048
```

生成或重建 H50 index：

```bash
cd /home/test/qxh/workspace/myvla2/openpi

UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/prepare_vla_data.py \
  --dataset-dir /home/test/qxh/workspace/tac_ws/lerobot_data/tactile_vla \
  --split-file /data1/outputs/vla/indices/splits.json \
  --index-file /data1/outputs/vla/indices/vla_indices_h50.json \
  --output-dir /data1/outputs/vla/indices \
  --action-horizon 50 \
  --reasoning-augment-after-frames 10
```

这里不要传 `--overwrite`，否则会按 seed 重新写 split；H50 必须复用 H30 的 split 文件。

计算 H50 norm stats：

```bash
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/compute_vla_norm_stats.py \
  --dataset-dir /home/test/qxh/workspace/tac_ws/lerobot_data/tactile_vla \
  --split-file /data1/outputs/vla/indices/splits.json \
  --output-dir /data1/outputs/vla/assets/tactile_vla_h50 \
  --action-horizon 50
```

Stage A H50 dry-run：

```bash
CUDA_VISIBLE_DEVICES=0 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/train_vla_stage_a_openpi.py \
  --dataset-dir /home/test/qxh/workspace/tac_ws/lerobot_data/tactile_vla \
  --index-file /data1/outputs/vla/indices/vla_indices_h50.json \
  --split-file /data1/outputs/vla/indices/splits.json \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_h50 \
  --action-horizon 50 \
  --dry-run \
  --max-frames 4 \
  --batch-size 2 \
  --num-workers 0
```

已验证输出 `actions: shape=(2, 50, 32)`。正式训练：

V1 H50 实验从 `/home/test/.cache/modelscope/hub/models/hairuoliu/pi05_base/params` 独立初始化，下面显式保留该参数路径，不依赖当前 V2 脚本的默认值。

```bash
CUDA_VISIBLE_DEVICES=0 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/train_vla_stage_a_openpi.py \
  --dataset-dir /home/test/qxh/workspace/tac_ws/lerobot_data/tactile_vla \
  --index-file /data1/outputs/vla/indices/vla_indices_h50.json \
  --split-file /data1/outputs/vla/indices/splits.json \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_h50 \
  --output-dir /data1/outputs/vla/stage_a_action \
  --run-name pi05_delta_tac_h50 \
  --action-horizon 50 \
  --checkpoint /home/test/.cache/modelscope/hub/models/hairuoliu/pi05_base/params \
  --batch-size 8 \
  --num-steps 10000 \
  --seed 42 \
  --train-lora-only
```

先使用 batch 8；如果 GPU dry-run/首步 OOM，则固定降为 batch 4，并在实验记录中注明该版本不再是严格 batch-matched 消融。

Stage A 到达 step 10000 后，训练 H50 Stage B：

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/train_vla_stage_b_heads.py \
  --dataset-dir /home/test/qxh/workspace/tac_ws/lerobot_data/tactile_vla \
  --index-file /data1/outputs/vla/indices/vla_indices_h50.json \
  --split-file /data1/outputs/vla/indices/splits.json \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_h50 \
  --output-dir /data1/outputs/vla/stage_b_heads \
  --run-name pi05_heads_jax_h50 \
  --backbone-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_h50/10000 \
  --action-horizon 50 \
  --reasoning-augment-after-frames 10 \
  --batch-size 8 \
  --epochs 20 \
  --seed 42
```

H50 Stage B 的 status/reasoning 数据 dry-run 已通过。当前执行环境无法连接 NVIDIA driver，因此本轮没有启动正式 Stage A/Stage B GPU 训练。

H50 server dry-run：

```bash
CUDA_VISIBLE_DEVICES=4 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python ../scripts/serve_tactile_vla_policy.py \
  --stage-a-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_h50/10000 \
  --stage-b-heads /data1/outputs/vla/stage_b_heads/pi05_heads_jax_h50/best/head_params.npz \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_h50 \
  --no-use-state-history \
  --dry-run
```

必须看到 `actions_shape=[50, 7]`，之后去掉 `--dry-run` 启动正式 server。客户端先执行一个 chunk 的 no-publish 检查：

```bash
python inference/agilex/inference/agilex_inference_tactile_vla_sync_single.py \
  --host localhost \
  --port 8000 \
  --captioner_checkpoint /data1/outputs/tactile_captioner/tcn_v1_balanced/best.pt \
  --chunk_size 50 \
  --max_publish_step 50 \
  --recovery-tactile-ignore-seconds 5.0 \
  --no-publish
```

当前 V2 client 强制要求 state-history server；需要复现旧 H50 真机 client 时应切换到 V1/H50 对应分支。旧 client 会校验 `chunk_size <= server action_horizon`，避免误连 H30 server。

## 5. 推理集成阶段

推理阶段已经拆成两个进程：

1. policy server：加载 Stage A action backbone 和 Stage B 辅助头，通过 websocket 提供 `execution/reasoning` 两种模式。
2. 真机同步推理脚本：订阅 ROS 图像、关节状态和原始触觉 topic，在线构造 tactile caption，维护 attempt/recovery 状态机，并发布单臂关节目标。

### 5.1 Policy server

server 入口：

```bash
scripts/serve_tactile_vla_policy.py
```

推荐启动命令：

```bash
cd /home/test/qxh/workspace/myvla2/openpi

CUDA_VISIBLE_DEVICES=4 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python ../scripts/serve_tactile_vla_policy.py \
  --stage-a-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_h30_state_memory/10000 \
  --stage-b-heads /data1/outputs/vla/stage_b_heads/pi05_heads_jax_h30_state_memory/best/head_params.npz \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_h30_state_memory \
  --state-history-fps 30 \
  --port 8000
```

server 内部做的事情：

- 构造和训练时一致的 JAX `Pi0Config`：`pi05=True`、`action_horizon=30`、`action_dim=32`、`use_state_history=True`、历史形状 `[60,7]`。
- 从 Stage A step 目录自动识别 `<step>/params`，加载 OpenPI JAX/Orbax 参数。
- 加载 Stage B 的 `head_params.npz`，输出 `need_recovery`、`failure_reason`、`recovery_plan`。
- 对 Stage A action 输出执行 `Unnormalize` 和 `AbsoluteActions`，把 delta action 加回当前 puppet state。
- 最终只返回前 7 维，作为单臂 absolute joint target。
- metadata 显式记录 `state_history_fps=30`；该频率表示训练/推理历史状态的时间网格，不是 ROS 本体 topic 频率，也不是 action 发布频率。
- websocket 开始监听前，自动各跑一次 `execution` 和 `reasoning`，完成 JAX 编译预热并检查 action shape 和 NaN/Inf，避免第一次真机请求承担编译延迟。

Execution 请求字段：

```python
{
    "mode": "execution",
    "observation/image": front_rgb,
    "observation/wrist_image": left_wrist_rgb,
    "observation/state": puppet_qpos_7,
    "observation/state_history": puppet_qpos_history_60x7,
    "observation/state_history_mask": history_valid_mask_60,
    "prompt": execution_prompt,
}
```

Execution 响应字段：

```python
{
    "actions": actions,                  # [30, 7], absolute joint target
    "need_recovery": need_recovery,      # bool
    "need_recovery_probs": probs,        # [p_false, p_true]
    "failure_reason": failure_reason,    # tilted left/right/front/back
    "failure_reason_probs": probs,
}
```

Reasoning 请求使用同样图像和状态，但 `mode="reasoning"`，`prompt` 使用 `build_reasoning_prompt(...)`。响应字段：

```python
{
    "recovery_plan": recovery_plan,      # Regrasp left/right/front/back.
    "recovery_plan_probs": probs,
}
```

完整模型级 dry-run 可以在 server 命令后追加：

```bash
--dry-run
```

普通 server 启动和 `--dry-run` 都会使用全零图像/状态分别预热一次 `execution` 和 `reasoning`。`--dry-run` 会打印 action shape 和辅助头输出后退出；普通启动则在预热成功后才监听 websocket。

### 5.2 真机同步推理脚本

真机脚本入口：

```bash
openpi/inference/agilex/inference/agilex_inference_tactile_vla_sync_single.py
```

默认 topic 直接写在脚本参数默认值里，实际测试不依赖 `data/aloha_data_params.yaml`。`--config_path` 只是可选覆盖项，不传就完全不读 YAML。

默认输入 topic：

- front image：`/camera_f/color/image_raw`
- left wrist image：`/camera_l/color/image_raw`
- puppetRight state：`/puppet/joint_right`
- left tactile force：`/xense/OG001251/force`
- right tactile force：`/xense/OG000991/force`
- left tactile mesh：`/xense/OG001251/mesh_3d`
- right tactile mesh：`/xense/OG000991/mesh_3d`
- left tactile flow：`/xense/OG001251/mesh_3d_flow`
- right tactile flow：`/xense/OG000991/mesh_3d_flow`

已用 `rostopic hz /puppet/joint_right` 确认真机本体状态约为 200 Hz。V2 在该 callback 中独立缓存带 ROS 时间戳的 qpos；即使 action chunk 正在以 30 Hz 发布，历史仍持续采集。缓存按时间保留约 2.47 秒数据，不再按“最近 120 条消息”截断。

每次真机推理以和图像同步的当前 puppet state 时间 `t` 为终点，构造 60 个目标时间：

```text
t-59/30, t-58/30, ..., t-1/30, t
```

然后为每个目标时间选择 200 Hz 缓冲中时间戳最近的 qpos，得到严格的 30 Hz、`[60,7]` 历史。这和离线 `data_sync_attempts.py` 为同步时间选择最近传感器样本的方式一致；不能简单每隔 6 条 ROS 消息取一条，因为 `200/30` 不是整数且 topic 存在时间抖动。

- `--state-history-fps 30`：定义模型历史的目标采样频率，60 帧覆盖 `(60-1)/30=1.967` 秒；它不修改 ROS 订阅频率。
- `--publish_rate 30`：只控制 action target 发布频率，与历史采样频率相互独立。
- `--state-history-max-gap-seconds 0.02`：最近 qpos 距目标时间超过 20 ms 时将该历史点置 `mask=false`，避免在 topic 丢帧/中断时使用时间错误的状态。
- attempt 开头不足约 2 秒的部分重复最早状态并置 `mask=false`；最后一个历史点强制使用当前同步 state 并置 `mask=true`。
- 每个新 attempt 开始时清空真机历史，防止把上一次失败轨迹带入新 attempt。

client 会分别校验 server metadata 中的 history 长度、维度和 `state_history_fps`，不再错误地用 action `publish_rate` 代替历史频率进行校验。

默认输出 topic：

- masterRight command：`/master/joint_right`

真机启动示例：

```bash
cd /home/test/qxh/workspace/myvla2/openpi

UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  inference/agilex/inference/agilex_inference_tactile_vla_sync_single.py \
  --host localhost \
  --port 8000 \
  --instruction "Pick up and transfer the object stably." \
  --captioner_checkpoint /data1/outputs/tactile_captioner/tcn_v1_balanced/best.pt \
  --state-history-fps 30 \
  --recovery-tactile-ignore-seconds 3.0 \
  --max_publish_step 10000

python inference/agilex/inference/agilex_inference_tactile_vla_sync_single.py \
  --host localhost \
  --port 8000 \
  --instruction "Pick up and transfer the object stably." \
  --captioner_checkpoint /data1/outputs/tactile_captioner/tcn_v1_balanced/best.pt \
  --state-history-fps 30 \
  --recovery-tactile-ignore-seconds 3.0 \
  --max_publish_step 10000 \
  --chunk_size 30
```

captioner checkpoint 不存在时脚本会直接报错，不再静默退化为永久的 `no rotation`。只有确实希望完全禁用触觉时才显式传 `--no-captioner`。

低速/小步真机检查建议先限制发布步数，并可先不发布到机械臂：

```bash
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  inference/agilex/inference/agilex_inference_tactile_vla_sync_single.py \
  --host localhost \
  --port 8000 \
  --captioner_checkpoint /data1/outputs/tactile_captioner/tcn_v1_balanced/best.pt \
  --recovery-tactile-ignore-seconds 5.0 \
  --max_publish_step 5 \
  --no-publish

python inference/agilex/inference/agilex_inference_tactile_vla_sync_single.py \
  --host localhost \
  --port 8000 \
  --captioner_checkpoint /data1/outputs/tactile_captioner/tcn_v1_balanced/best.pt \
  --recovery-tactile-ignore-seconds 5.0 \
  --max_publish_step 5 \
  --no-publish
```

确认 server 响应、action shape、captioner 和状态机日志正常后，再去掉 `--no-publish`。

### 5.3 在线触觉 caption

触觉运行时代码：

```bash
src/tactile_vla/runtime/tactile_buffer.py
```

触觉端直接订阅原始 `Float32MultiArray` topic，不要求预先发布 `mesh_motion`。每个原始 topic 按 `layout.dim=[35,20,3]` reshape。

拼接规则固定为：

```python
force = concat(left_force, right_force)  # [35, 20, 6]
mesh_motion = concat(
    left_mesh_3d_flow,
    left_mesh_3d,
    right_mesh_3d_flow,
    right_mesh_3d,
)  # [35, 20, 12]
```

### 5.4 第一版部署行为（V1 历史记录）

第一版以跑通最小功能为目标，当前约定如下：

- 保留 Stage A 默认的 30 步 action horizon，并默认一次开环发布完整 30 步；`--chunk_size` 默认值仍为 `30`。
- 第一次 attempt 正常使用在线 tactile caption。
- 每个非第一次 attempt 的开头是归位阶段。默认 `--recovery-tactile-ignore-seconds 5.0`，在这段时间内不调用 captioner，并强制给 VLA 输入 `Tactile: no rotation.`。
- 5 秒按“已发布的基础动作步数”计算：默认 `publish_rate=30` 时共屏蔽 150 步，也就是 5 个 30 步 chunk。模型/网络推理耗时不会消耗屏蔽窗口。若启用 action interpolation，插值出的额外发布点不计入这 150 步。
- 屏蔽期间原始触觉 ROS callback 和 tactile buffer 仍继续收集数据，因此屏蔽结束后直接使用最近窗口，不清空 buffer。
- “不看触觉”只表示替换 tactile caption；`need_recovery` 辅助头仍可根据图像、关节状态和 prompt 触发。当前没有在归位期间强制设置 `need_recovery=False`。
- runtime JSONL 日志会记录 `tactile_ignored` 和 `tactile_ignore_remaining_steps`，用于确认归位屏蔽是否生效。
- infer 在发布前检查 action 必须为 `[T, 7]` 且全部为有限值，遇到 NaN/Inf 直接停止并报错；这只是最小检查，不等同于完整的机械臂安全控制。

如果实际归位时间不是 5 秒，按实测修改参数。例如归位 3 秒可传：

```bash
--recovery-tactile-ignore-seconds 3.0
```

### 5.5 V1 已验证项和待修改项（历史记录）

本轮已验证：

- 原始 topic 名称与 `/home/test/qxh/workspace/tac_ws/src/data_tools/config/aloha_data_params.yaml` 一致。
- 原始 tactile 到训练 HDF5 的拼接顺序与在线 buffer 一致：force 为 left/right，mesh motion 为 left flow/mesh、right flow/mesh。
- Stage A 的 delta action 归一化、反归一化和加回当前 state 的方向一致，server 最终输出 `[30, 7]` absolute joint target。
- server 和 infer 语法检查通过；使用 `episode1/attempt1` 做 mock/no-publish 回放时完整发布 30 步通过。
- 触觉屏蔽 0/149/150 步边界、captioner 缺失报错和 NaN/Inf action 拒绝发布通过定向检查。

第一版暂不修改、后续需要处理的事项：

1. 在线六路触觉目前按“六个 topic 都至少更新一次”组帧，没有基于消息时间戳做严格同步或最大 skew 检查；输入约 60 Hz 时，30 帧窗口约为 0.5 秒，而训练同步数据约 30 Hz、30 帧约为 1 秒。后续应按 30 Hz 时间轴重采样并加入时间戳/陈旧数据检查。
2. 图像和关节状态当前只做队列取帧，没有与 tactile 窗口做同一时间戳的严格多模态同步。后续应明确允许的时间偏差并丢弃超时 observation。
3. 目前保留 30 步全开环。若真机出现偏差累积，再改成短 chunk 重规划、action overlap 或 temporal ensemble；第一版不做。
4. `need_recovery` 目前单次超过阈值就切换 attempt，没有连续多帧确认、迟滞或冷却时间。若归位期间视觉也造成误触发，可再增加“归位窗口内强制 `need_recovery=False`”或独立 homing 状态；当前只屏蔽触觉。
5. action 安全目前只有 shape 和 NaN/Inf 检查，尚缺关节上下限、相邻目标最大变化、速度/加速度限制、通信 watchdog 和硬件急停联动。这些必须在更正式的真机部署前补齐。
6. websocket client 尚无显式请求超时、断线重连和失败后的安全停机状态；server 也缺健康检查接口。
7. Stage A、Stage B、norm stats 和 captioner 依赖人工保证来自同一训练版本，尚无统一 manifest/hash 和启动时一致性校验。
8. runtime 默认 `case_id="runtime"` 不在训练的 `case_001/case_002/case_003` 分布中；需要结合线上任务语义决定映射或去掉该文本特征。
9. HDF5 replay 目前用于数据通路检查，不按真实 wall-clock 和跨 attempt 状态完整复现，不能替代真机状态机测试。
10. captioner 在当前 CPU 检查中单次约 0.5 秒，正式部署应确认实际设备上的 GPU/CPU 延迟和显存占用。
11. 当前环境 NVIDIA driver 不可用，因此本轮没有执行 Stage A + Stage B 的真实 GPU server 预热；部署机器上应先执行 server `--dry-run`，成功后再连接真机 infer。

### 5.6 H30 state-memory V2 实现记录

当前 V2 state-memory 架构约定（扩展数据版保持不变）：

- action horizon 和 client `chunk_size` 均为 30，发布频率为 30 Hz。
- LeRobot 仍只保存单帧 `observation.state=[7]`，历史窗口在 Dataset 加载阶段动态构造。
- 训练样本使用 `delta_timestamps=-59/30..0`，得到 `current_state=[7]`、`state_history=[60,7]`、`state_history_mask=[60]`。
- episode 开头左侧 padding 重复首帧，但 masked GRU 对 `mask=false` 的位置不更新 hidden state。
- 历史编码结果只加到 action expert 的 AdaRMS condition；Stage A 不增加进度标签或辅助 loss。
- state-history encoder 在一次 action chunk 推理中只计算一次，10 次 flow-matching 迭代复用同一个 `history_cond`。
- pi05 base checkpoint 缺少历史模块时，只允许 `history_*` 和 LoRA 参数使用当前初始化补齐，其他缺失参数仍报错。
- `history_proj` 的 normal initializer 使用模块级单例，避免两次构建模型时闭包函数地址不同，导致 JAX 报 `pjit out_shardings.model_def` GraphDef metadata 不一致。
- server 从 Stage A `config.json` 恢复历史配置，并检查 checkpoint 是否实际包含 `history_*` 参数，防止静默丢弃。
- 真机端在 ROS joint callback 中持续缓存 qpos，而不是每个 chunk 只记录一帧；每个新 attempt 清空历史。
- 真机 `/puppet/joint_right` 为约 200 Hz，在线历史按 ROS 时间戳最近邻重采样为 30 Hz；60 帧覆盖 1.967 秒，而不是直接取最近 60 条回调所对应的约 0.3 秒。
- 在线缓冲按时间保留约 2.47 秒；最近样本距目标时间超过 20 ms 时对应 mask 为 false。`state_history_fps` 和 action `publish_rate` 是两个独立参数。
- replay 端直接按 HDF5 frame index 读取最近 60 帧，和训练窗口保持一致。

当前 expanded 训练默认产物路径：

```text
dataset: /data1/tac_data/lerobot_data/tactile_vla_expanded
split:   /data1/outputs/vla/indices/splits_h30_state_memory_expanded.json
index:   /data1/outputs/vla/indices/vla_indices_h30_state_memory_expanded.json
norm:    /data1/outputs/vla/assets/tactile_vla_h30_state_memory_expanded
Stage A: /data1/outputs/vla/stage_a_action/pi05_delta_tac_h30_state_memory_expanded
Stage B: /data1/outputs/vla/stage_b_heads/pi05_heads_jax_h30_state_memory_expanded
```

在 `openpi` 目录下依次运行：

```bash
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python ../scripts/prepare_vla_data.py

UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python ../scripts/compute_vla_norm_stats.py

CUDA_VISIBLE_DEVICES=0 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/train_vla_stage_a_openpi.py --overwrite

CUDA_VISIBLE_DEVICES=0 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/train_vla_stage_b_heads.py --epochs 5
```

已完成的 expanded 数据准备与 CPU/数据通路验证：

- expanded LeRobot 共 320 个原始 episode、680 个 attempt、395614 帧；split 为 256/32/32。
- H30 execution 样本为 303282/38916/33696，Stage B reasoning 样本为 3223/429/308。
- expanded norm stats 已用 train split 的全部 303282 个有效 action chunk 计算完成。
- Stage A dry-run：`state=(2,32)`、`state_history=(2,60,7)`、`state_history_mask=(2,60)`、`actions=(2,30,32)`。
- Stage B status/reasoning dry-run 均得到相同 history 形状。
- state-history padding/mask、200 Hz→30 Hz 最近邻重采样、约 2 秒时间跨度、异常时间间隔 mask 和历史归一化单元测试通过。
- OpenPI 原有 pi0 freeze-filter、“history 参数保持可训练”和“state-history GraphDef 稳定”测试通过。
- `episode1/attempt1` mock replay/no-publish 已通过 V2 payload 和 metadata 校验。

正式训练前后仍需完成：

1. 启动 expanded Stage A GPU 训练，确认第一步能够从 pi05 base 加载并初始化新 `history_*` 参数。
2. Stage A 到达 step 10000 后重新训练 expanded Stage B heads，正式部署不要混用旧数据版 heads。
3. 使用 expanded checkpoint 执行 server `--dry-run`，确认 `actions_shape=[30,7]` 和 metadata 中 `use_state_history=true`。
4. 真机先用 `--max_publish_step 5 --no-publish` 检查 history 有效帧数、推理延迟和 action，再恢复完整 30 步发布。

UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
  uv run python ../scripts/train_vla_stage_a_openpi.py \
    --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_expanded \
    --split-file /data1/outputs/vla/indices/splits_h30_state_memory_expanded.json \
    --index-file /data1/outputs/vla/indices/vla_indices_h30_state_memory_expanded.json \
    --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_h30_state_memory_expanded \
    --output-dir /data1/outputs/vla/stage_a_action \
    --run-name pi05_delta_tac_h30_state_memory_expanded \
    --checkpoint /home/test/.cache/modelscope/hub/models/hairuoliu/pi05_base/params \
    --action-horizon 30 \
    --num-steps 15000 \
    --resume

stageb
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
  uv run python ../scripts/train_vla_stage_b_heads.py \
    --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_expanded \
    --split-file /data1/outputs/vla/indices/splits_h30_state_memory_expanded.json \
    --index-file /data1/outputs/vla/indices/vla_indices_h30_state_memory_expanded.json \
    --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_h30_state_memory_expanded \
    --backbone-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_h30_state_memory_expanded/15000 \
    --output-dir /data1/outputs/vla/stage_b_heads \
    --run-name pi05_heads_jax_h30_state_memory_expanded \
    --action-horizon 30 \
    --epochs 2

server
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
  uv run python ../scripts/serve_tactile_vla_policy.py \
    --stage-a-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_h30_state_memory/10000 \
    --stage-b-heads /data1/outputs/vla/stage_b_heads/pi05_heads_jax_h30_state_memory/best/head_params.npz\
    --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_h30_state_memory \
    --use-state-history \
    --state-history-len 60 \
    --state-history-dim 7 \
    --state-history-fps 30 \
    --history-hidden-dim 256 \
    --action-horizon 30 \
    --port 8000

python inference/agilex/inference/agilex_inference_tactile_vla_sync_single.py \
    --host localhost \
    --port 8000 \
    --captioner_checkpoint /data1/outputs/tactile_captioner/tcn_v1_balanced/best.pt \
    --publish_rate 30 \
    --chunk_size 30 \
    --state-history-len 60 \
    --state-history-fps 30 \
    --state-history-max-gap-seconds 0.02 \
    --recovery-tactile-ignore-seconds 3.0 \
    --max_attempts 3 \
    --max_publish_step 10000