# 触觉 VLA V3 会话交接（2026-08-11）

## 1. 给下一个会话的说明

用户正在开发一套基于 π0.5 的触觉 VLA，目标是让机器人根据连续触觉反馈识别旋转、抓空和接触面积不足，再通过多次 attempt 调整抓取位置。当前已经进入 V3 训练和真机验证阶段。

新会话应先阅读本文，再按任务需要查看：

- `/home/test/qxh/workspace/myvla2/方案v3.md`
- `/home/test/qxh/workspace/myvla2/待修改.md`
- `/home/test/qxh/workspace/tac_ws/README.MD` 的“第三版纯触觉数据采集”、“第三版演示数据采集”和“第三版演示数据处理”。

注意：`方案v3.md` 是逐步更新的设计文档，少量早期说法已过时。若文档内部冲突，以本交接文件、实际 checkpoint `config.json` 和当前代码为准。

## 2. 仓库与 Git 状态

### myvla2

- 路径：`/home/test/qxh/workspace/myvla2`
- 当前分支：`feature/v3-bi-tac-memory`
- HEAD：`494ccc9`
- 远端：`git@github.com:wdqdp/myvla2.git`
- 当前存在大量未提交的 V3 修改和未跟踪新文件。这些都是有效工作，不要 `git reset --hard`、不要切换分支覆盖，也不要对整个工作区做机械回退。

### tac_ws

- 路径：`/home/test/qxh/workspace/tac_ws`
- 当前分支：`v2`
- HEAD：`8831b60`
- 数据采集和 V3 离线处理代码同样尚未提交，必须保留。

新会话开始时建议先分别执行：

```bash
cd /home/test/qxh/workspace/myvla2 && git status --short --branch
cd /home/test/qxh/workspace/tac_ws && git status --short --branch
```

## 3. 已确认的 V3 方案

### 3.1 输入和动作

- action chunk 长度固定为 30，真机按 30 Hz 发布。
- action 输入包含当前两幅图像、当前 7 维 qpos、结构化 tactile caption、任务和当前 `recovery_plan`。
- V3 现已沿用 V2 的本体状态 memory：最近 60 帧、7 维 qpos，按 30 Hz 构成约 2 s 历史，通过 masked GRU 编码后注入 action expert。
- 真机 `/puppet/joint_right` 约 200 Hz；客户端保留时间戳缓存，再重采样为 30 Hz 的 60 点窗口。当前实现使用时间网格的最近邻采样，并用 `--state-history-max-gap-seconds` 生成 mask，不是直接取 200 Hz 的最近 60 条。
- 每次 attempt 开始时清空历史，避免跨 attempt 混入。

### 3.2 Tactile Captioner

- 正式版使用 30 帧滑动窗口；真机 Captioner 可约 7 Hz 跟随后台 assessment。
- 输出字段：
  - `area = none / small / medium / full`
  - `Fx = negative / near_zero / positive`
  - `Fy = negative / near_zero / positive`
  - `Fz = negative / near_zero`
  - `rotation = none / clockwise / counterclockwise`
- 不使用任何“趋势”字段。
- 左右传感器原始数据都不做轴交换或符号翻转；只在标注语义上统一按左手传感器坐标系定义。
- 纯触觉数据训练多个头，再用演示数据 episode 40–120 的 attempt1 单独微调 rotation head。
- 当前 W30 微调模型：`/data1/outputs/tactile_captioner/tcn_v3_w30_rotation_head/best.pt`。

### 3.3 结构化推理

- Execution prompt 只向 action 模型提供当前 `recovery_plan`；`failure_reason` 不直接放进 action prompt。
- prompt 已去掉 `case_id`、`attempt_id`和 `failed_attempt_id`。这些 ID 仍可保留在数据组织和日志中，但不得输入模型。
- `need_recovery` 是非自回归二分类头。
- `failure_reason` 与 `need_recovery` 共用同一个 assessment prompt 和 VLM prefix；只在 need=true 时从同一 KV cache 继续受约束自回归生成失败文本。
- `recovery_plan` 使用更新后的 failure-recovery memory 单独做一次 reasoning prefix 和受约束自回归生成。

`failure_reason` 固定模板：

```text
failure_reason=rotate {left|right|front|back|none},grasp {appropriate|missing|too_high|too_low}.
```

Captioner 的顺/逆时针是触觉表面观测；`failure_reason` 的 left/right/front/back 需要 VLM 结合图像和物体姿态推理，两者不是同一个标签空间。

`recovery_plan` 固定模板：

```text
recovery_plan=move horizontally {left|right|front|back|none} {slightly|moderately|significantly}, move vertically {up|down|none} {slightly|moderately|significantly}.
```

当前数据规则是：任务 memory 内第 1 次旋转用 `moderately`；第 2 次及以后的旋转都用 `slightly`，不再要求前后两次旋转方向相反。中间出现抓空或面积问题不重置旋转计数。垂直调整用 `moderately`。

### 3.4 Failure-recovery memory

- memory 中每一项必须是完整的 `recovery_plan–failure_reason` 对。
- 第一对的 plan 固定为 `initial plan`。
- 当前 plan 执行完且失败后，才将它和新 failure 写入 memory。
- 最多执行 5 次 attempt；第 1–4 次失败后可生成下一条 plan，因此模型输入最多包含 4 对 memory。第 5 次仍失败则终止。
- 数据集需要覆盖 1–4 对 memory，并在每个 split 中尽量均衡。donor 不得跨 split。

### 3.5 真机并发逻辑

- action 线程按 30 Hz 发布已缓存 chunk。
- assessment 在后台异步运行，真机已测得约 7.7 Hz，典型滞后约 3 个 action step，用户认为可行。
- need=false 时继续发布缓存 action，不重新生成 chunk。
- need=true 时立即停止并丢弃剩余 chunk，生成 failure、更新 memory、生成新 plan，然后开始新 attempt。
- attempt2 及之后的归位期间不改写 tactile caption；`--recovery-need-ignore-seconds` 只是暂停 assessment，因此不响应 need=true，也不生成 failure。设为 `0` 即不 ignore。

## 4. 数据采集和离线处理要点

- 原始演示数据：`/data1/tac_data/raw_data`
- V3 LeRobot：`/data1/tac_data/lerobot_data/tactile_vla_v3`
- 纯触觉数据：`/data1/tac_data/tac_cap_data`
- V3 split：`/data1/outputs/vla/indices/splits_v3.json`
- V3 index：`/data1/outputs/vla/indices/vla_indices_v3.json`
- reasoning manifest：`/data1/outputs/vla/reasoning_manifests/v3`
- norm stats：`/data1/outputs/vla/assets/tactile_vla_v3`

采集模式有两类：一次成功 episode，以及含“失败 attempt + 恢复 attempt”的 episode。不使用“attempt2 只保存 meta”的方案；attempt2 仍完整采集图像、触觉、qpos 和动作。

Stage A 过滤规则：

- 一次成功 attempt 参与 Stage A。
- 纯旋转失败（`rotation != none`、`grasp=appropriate`、面积 medium/full）可参与 Stage A。
- 抓空、抓得太少或同时有 grasp 问题的旋转不参与 Stage A，但参与 Stage B 状态判断和 reasoning。
- 使用独立字段 `stage_a_eligible`，不复用 `valid`。
- norm stats 必须与 Stage A `execution_indices` 使用完全相同的过滤结果。

离线顺序：raw → 同步/HDF5 → LeRobot → 按 episode 分 train/val/test → 在各 split 内构造 reasoning manifest → index/norm stats。`hdf5_to_lerobot_attempts.py` 已支持 `--resume`，会校验已有 episode 后跳过。

V3 离线处理主要代码在：

```text
/home/test/qxh/workspace/tac_ws/src/data_tools/scripts_new/
  data_to_hdf5_attempts.py
  hdf5_to_lerobot_attempts.py
  prepare_v3_splits.py
  build_v3_reasoning_manifest.py
  validate_v3_attempts.py
  v3_attempt_schema.py
  migrate_v3_rotation_direction.py
  build_tactile_captioner_dataset.py
  tactile_label_schema.py
```

## 5. 模型训练和 checkpoint 现状

### Stage A

- checkpoint：`/data1/outputs/vla/stage_a_action/pi05_delta_tac_v3_m60/15000`
- 实际存在，约 8.9 GiB。
- H30，启用 60x7 state history，history hidden dim 256。
- 从 `/home/test/.cache/modelscope/hub/models/hairuoliu/pi05_base/params` 初始化。

### Stage B

- run：`/data1/outputs/vla/stage_b_v3/pi05_stage_b_v3_m60`
- 已训练到 step 10000。
- best delta：`.../best/delta_params`，约 103 MiB。
- 部署用完整合并 checkpoint：`.../merged_best`，约 5.1 GiB。
- Stage B 按 `action -> need -> failure -> plan` 1:1:1:1 循环；`num_steps=10000` 是总更新次数，因此每个任务约 2500 次。
- 可训参数为 PaliGemma LLM LoRA（排除 action expert `_1`）和 need head。视觉编码器、PaliGemma base、action expert、history encoder、embedding 均冻结。
- action replay 仍会通过 flow loss 更新 PaliGemma LoRA，所以 Stage B 可能改变 action 语义。
- Stage B 现改为只保存 LoRA、need head、优化器和 RNG 增量，避免每次保存 6.4 GiB 冻结参数导致内存峰值。部署前再用 `scripts/merge_v3_stage_b_delta.py` 离线合并。

必须注意的验证问题：当前 `best/metrics.json` 来自 step 1000，`eval_max_need_samples=2048` 时抽到的 2048 个 need 样本全是 false，true support=0，因此 need `macro_f1=0.5` 不是有效的正负类验证。后续应检查 need 验证采样是否按类别分层，不要直接把当前 best 分数当成 need head 有效的证据。

## 6. 当前真机问题及已完成的原因分析

当前现象：V3 生成了“水平移动 moderately/slightly”的 `recovery_plan`，但 action 实际几乎没有水平移动。

用户的初步猜测：`moderately` 和 `slightly` 一起训练，模型无法区分。

已做的数据检查：

- Stage A train 中水平 `slightly`：44 个 attempt，30,183 个 action frame。
- Stage A train 中水平 `moderately`：98 个 attempt，64,923 个 action frame。
- 用“30-step 内 6 个手臂关节的 qpos 总位移”做粗略 proxy：
  - slightly median = 0.1372 rad，p10 = 0.0231，p90 = 0.8007
  - moderately median = 0.1290 rad，p10 = 0.0222，p90 = 0.5918
- 这不是笛卡尔水平位移，但已表明两类 action 监督高度重叠，整体不呈现 moderate > slight 的单调关系。

更可能的根因是：`recovery_plan` 被赋给整个 attempt，Stage A 又将大量 execution-eligible 帧都当作 action 样本。归位、张爪、夹紧和抬升等大量非水平位移片段，都在同一 moderate/slight prompt 下训练，真正短暂的水平移动监督被稀释。因此“两个程度词放在一起”不一定是唯一原因。

还有第二个待分离变量：Stage B 会更新 PaliGemma LoRA，有可能破坏 Stage A 已学会的 recovery action。

## 7. 下一会话的首要任务（尚未实现）

用户最新确认的思路是编写一组“手动强制 recovery prompt 的 action-only 真机对照脚本”，目的是不运行 need/failure/reasoning，直接比较原始 Stage A 和 Stage B merged 的动作能力。

请注意：这个脚本目前只完成了方案讨论，代码还没有创建。不要误以为已实现。

### 7.1 客户端交互

启动时由命令行提前给定：

- 用于记录的预设失败类型，例如 `failure_reason=rotate right,grasp appropriate.`
- 强制 recovery plan，例如 `recovery_plan=move horizontally right moderately, move vertically none moderately.`

状态机：

```text
NORMAL --按下空格键--> RECOVERY
```

- NORMAL 阶段 action prompt 使用 `Recovery plan: none`。
- 真机发生旋转时，操作者按一次空格键。
- 按键后必须立即停止并丢弃当前 chunk 剩余 action，不能等 chunk 执行完。
- 获取最新图像、qpos、tactile caption 和时间戳，然后切换为命令行预设的 recovery plan。
- 参照真实新 attempt 逻辑：清空/重置 state-history，将 `pre_action` 设为当前 qpos，重新推理一个 30-step chunk。
- 不调用 `need_recovery`、`failure_reason` 生成或 `recovery_plan` 生成。预设 failure reason 只写日志，不放进 action prompt。
- tactile caption 仍然使用真实 Captioner 输出，不要改写或强制为 none。

### 7.2 服务端

建议新增，而不是将实验特例硬塞入正式 V3 服务：

```text
/home/test/qxh/workspace/myvla2/scripts/serve_tactile_vla_action_ablation.py
/home/test/qxh/workspace/myvla2/openpi/inference/agilex/inference/agilex_inference_forced_recovery_ablation.py
```

服务端要支持两种 checkpoint：

1. 原始 Stage A Pi0 checkpoint；
2. Stage B `merged_best` 的 `StageBV3Model`，但只调用 `model.backbone.sample_actions`。

两者必须使用同一个 model config、norm stats、action output transform、history 设置和 inference steps。

### 7.3 固定 flow noise 的对照

普通服务每次 action 调用都分裂新 JAX RNG，因此直接重复请求不是严格对照。实验服务应支持客户端传入固定 `action_noise`，形状为 `[30, 32]`，并校验 shape 和 finite。`Pi0.sample_actions` 本身已支持可选 `noise`参数。

理想的按键瞬间对照：

1. 冻结同一组最新 observation、state history 和 tactile caption。
2. 使用完全相同的 `[30,32]` flow noise，分别请求 `plan=none` 和强制 recovery plan。
3. 只发布 recovery plan 对应的 action，none 结果只用于日志和对比。
4. Stage A 和 Stage B 测试使用相同 noise seed/序列，并交替测试顺序。

日志至少记录：checkpoint 类型与路径、phase、完整 prompt、预设 failure/plan、tactile caption、切换前 qpos、raw model actions、实际发布 actions、实际 qpos feedback、history/mask 和 noise seed。

### 7.4 对照结果如何解释

- Stage A 就无法对 recovery plan 产生正确水平动作：主要是 Stage A 数据/动作监督问题。
- Stage A 可以，Stage B merged 不可以：Stage B 更新 PaliGemma LoRA 破坏了 action 能力。
- Stage A 和 Stage B 离线输出都有明显差异，但真机不移动：继续检查 output transform、发布限幅、插值和机器人反馈。

对照成立的关键不是只看动了没有，而是对同一 observation 计算 none/slight/moderate 在预期水平方向上的笛卡尔位移差异。最好将 qpos action 通过 FK 转成末端位置再统计，不要用单个关节差代替水平位移。

## 8. 如果确认是 Stage A 数据问题，后续建议

先做一个专门的水平恢复 action 微调对照，不要直接以整个 recovery attempt 作为“水平恢复”数据。

- 为每个 recovery attempt 人工或半自动标注真正水平位移的 start/end，例如写入独立 sidecar `horizontal_recovery_segments.json`。
- 保留包含位移开始的 chunk、位移进行中的 chunk 以及结束/停止 chunk，让模型同时学会启动、移动和停止。
- 第一个诊断实验可只训 moderate，四个方向均衡，从现有 Stage A 开始，learning rate 约 `5e-6–1e-5`，500–1500 step。
- 完整模型建议用 60–70% 水平恢复片段 + 30–40% 普通 action replay，避免破坏原有抓取能力。
- 复用当前整体数据的 norm stats，不要只用水平子集重新计算 norm。
- 生成新 Stage A 后需要重训 Stage B。旧 Stage B delta 绑定原 Stage A，不应强行合并到新 Stage A。

## 9. 关键代码地图

```text
/home/test/qxh/workspace/myvla2/
  方案v3.md
  待修改.md
  训练说明.md
  scripts/train_tactile_captioner.py
  scripts/finetune_tactile_rotation_head.py
  scripts/annotate_demo_tactile_captions.py
  scripts/inspect_tactile_captioner_attempt.py
  scripts/train_vla_stage_a_openpi.py
  scripts/train_vla_stage_b_v3.py
  scripts/merge_v3_stage_b_delta.py
  scripts/serve_tactile_vla_policy_v3.py
  src/tactile_vla/vla/prompts.py
  src/tactile_vla/vla/openpi_bridge.py
  src/tactile_vla/vla/stage_b_v3_model.py
  src/tactile_vla/vla/stage_b_v3_jax.py
  src/tactile_vla/vla/stage_b_v3_checkpoint.py
  src/tactile_vla/vla/structured_generation.py
  src/tactile_vla/vla/structured_text.py
  src/tactile_vla/runtime/state_history.py
  openpi/inference/agilex/inference/agilex_inference_tactile_vla_sync_single.py
```

## 10. 已知风险和代码细节

- `scripts/serve_tactile_vla_policy_v3.py::_prepare` 当前有两次连续的 `transformed = transform(inputs)`，结果通常相同但是重复工作，后续可单独清理，不要在无关实验中顺手大改。
- `方案v3.md` 开头仍有“V3 第一版不加本体 memory”的早期说法，但后续用户已明确要求参考 V2 加回 state memory，当前代码和 checkpoint 都已启用 60x7 memory。
- `方案v3.md` 的部分训练路径仍并存旧 run name，实际模型路径以本文第 5 节和 checkpoint `config.json` 为准。
- ROS/Python 环境曾遇到 `libp11-kit.so.0: undefined symbol ffi_type_pointer, version LIBFFI_BASE_7.0`；真机 Python 客户端通常需在系统 ROS 环境下运行，必要时使用 `LD_PRELOAD=/lib/x86_64-linux-gnu/libffi.so.7`。
- 训练机内存曾被 VS Code `cpptools`、Pylance、WebKit 大量占用；Stage B 验证/保存前应用 `free -h` 和 `ps -eo pid,%mem,rss,cmd --sort=-rss | head -20` 检查 RAM/swap。
- 不要删除或覆盖 `/data1` 中已有训练产物，除非用户明确要求。

## 11. 常用部署命令

V3 server：

```bash
cd /home/test/qxh/workspace/myvla2/openpi

UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
uv run python ../scripts/serve_tactile_vla_policy_v3.py \
  --checkpoint /data1/outputs/vla/stage_b_v3/pi05_stage_b_v3_m60/merged_best \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_v3 \
  --port 8000
```

V3 客户端：

```bash
cd /home/test/qxh/workspace/myvla2/openpi

LD_PRELOAD=/lib/x86_64-linux-gnu/libffi.so.7 \
python inference/agilex/inference/agilex_inference_tactile_vla_sync_single.py \
  --host localhost \
  --port 8000 \
  --instruction "Pick up and transfer the object stably." \
  --captioner_checkpoint /data1/outputs/tactile_captioner/tcn_v3_w30_rotation_head/best.pt \
  --tactile_window_size 30 \
  --chunk_size 30 \
  --publish_rate 30 \
  --observation-poll-rate 200 \
  --state-history-len 60 \
  --state-history-fps 30 \
  --recovery-need-ignore-seconds 0 \
  --max_publish_step 10000
```

## 12. 建议新会话的第一句请求

```text
请先完整阅读 /home/test/qxh/workspace/myvla2/会话交接_2026-08-11.md。
继续实现第 7 节的 Stage A/Stage B 手动强制 recovery prompt 对照工具。
开始前先检查现有工作区，不要覆盖未提交修改。
```
