# V7.3

## 目标

V7.3 在 V7.2 的四事件边界过滤上，去掉调整停止附近的 H30 起点，并完全禁用 terminal hold。execution 样本不受 adjustment-stop 规则影响。

## 过滤规则

设 `A=arm_adjustment_start`、`S=arm_adjustment_stop`，H30 起点为 `s`。

- 正常调整段 `A < S-29`：过滤 `[S-29,S]`。
- 短调整段 `A >= S-29`：保留 `[A,S-19]`，过滤 `[S-18,S]`；保留 H30 在 stop 后最多包含 10 帧。
- 如果 `A > S-19`，无法同时满足保留移动起点和最多跨 stop 10 帧，数据构建直接失败。
- 排除仍然跨越 `rexecution_frame` 的 adjustment H30，不再改写 action。

V7.3 复用经人工审核的 `v7_2_boundary_filter.json`和 V4 norm stats。

## 生成 training index

```bash
openpi/.venv/bin/python scripts/prepare_v7_3_adjustment_index.py \
  --verify-h30-targets
```

默认输出：

```text
/data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_3_adjustment/
├── action_phase_manifest.jsonl
├── filter_summary.json
├── v7_3_prompt_training_index.json
└── artifact_hashes.json
```

## 训练

```bash
CUDA_VISIBLE_DEVICES=4,5,6,7 \
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/train_vla_stage_a_openpi.py \
  --dataset-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/lerobot_data/tactile_vla_rotation_v4 \
  --index-file /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_3_adjustment/v7_3_prompt_training_index.json \
  --norm-stats-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats \
  --output-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action \
  --run-name pi05_delta_tac_rotation_phase_v7_3_no_history \
  --data-profile rotation_phase_v7_3_adjustment \
  --prompt-profile phase_v2 \
  --experiment-kind phase_prompt_h30_native_reexecution_event_gap_adaptive_stop_filtered \
  --checkpoint /home/qxh/.cache/openpi/openpi-assets/checkpoints/pi05_base/params \
  --batch-size 8 \
  --num-steps 10000 \
  --action-horizon 30 \
  --action-dim 32 \
  --no-use-state-history \
  --state-history-len 0 \
  --state-history-dim 7 \
  --history-hidden-dim 0 \
  --paligemma-variant gemma_2b_lora \
  --action-expert-variant gemma_300m_lora \
  --train-lora-only \
  --fsdp-devices 2 \
  --seed 42
```

已新增离线测试脚本：scripts/infer_vla_action_from_dataset.py

  调整模式示例：

  CUDA_VISIBLE_DEVICES=0 \
  OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
  USE_TF=0 \
  openpi/.venv/bin/python scripts/infer_vla_action_from_dataset.py \
    --checkpoint /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action/pi05_delta_tac_rotation_phase_v7_3_no_history\
    --episode 37 \
    --attempt 2 \
    --timestamp 1788587501.190162 \
    --mode adjustment \
    --direction right \
    --degree slightly \
    --num-inference-steps 10 \
    --noise-seed 0 \
    --output-json /home/qxh/workspace/myvla2/tmp/vla_h30_action_11.json

  执行模式：

  CUDA_VISIBLE_DEVICES=0 \
  OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
  USE_TF=0 \
  openpi/.venv/bin/python scripts/infer_vla_action_from_dataset.py \
    --checkpoint /path/to/checkpoint_or_run \
    --episode 1 \
    --attempt 1 \
    --timestamp 1785650125.246478 \
    --mode execution

  主要行为：

  - run 目录会自动选择最大的数字 checkpoint，例如 10000。
  - 从 checkpoint 的 config.json 自动读取数据集和 norm stats 路径，也可用参数覆盖。
  - 在指定 episode/attempt 内匹配最接近的 float64 ROS 时间戳，默认最大误差 0.02s。
  - 输入训练时的 front 和 left-wrist 图片。
  - 历史模型自动从 observation.state（puppetRight/从臂）按 30 Hz 构造历史及 padding mask。
  - V7.3 是无历史模型，因此不会向模型输入历史。
  - 输出为反归一化、delta 解码后的绝对关节目标，形状 [30,7]。
  - --magnitude 可作为 --degree 的别名。

  测试文件：tests/test_infer_vla_action_from_dataset.py，共 8 项测试通过，ruff 和 git diff --check 也已通过。尚未提交 Git。

- episode:59 1788593615.443176
  right, moderately : "y": -38.815,
  right, slightly : "y": -34.464,
  left, moderately: "y": 26.042,
  left, slightly: "y": 19.898,

- episode:37 1788587502.190162
  right, moderately : "y": -42.84,
  right, slightly : "y": -38.658,
  left, moderately: "y": 31.654,
  left, slightly: "y": 25.838,

 OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
  USE_TF=0 \
  openpi/.venv/bin/python scripts/infer_vla_four_adjustments.py \
    --episode 194 \
    --attempt 2 \
    --timestamp 1788625225.757669 \
    --model pi05_delta_tac_rotation_phase_v7_2_no_history \
    --output /home/qxh/workspace/myvla2/tmp/mod72_ep194_at2_t1788625225.757669.json
