# IPC (industrial PC) inference environment — use with Python 3.10, after installing PyTorch (see README).
# Install: pip install -r train_deploy_alignment/inference/agilex/requirements_inference_ipc.txt
# Note: "detr" is installed separately via: cd <path_to_detr> && pip install -e . (see README step 5).
pyquaternion==0.9.9
pyyaml==6.0
pexpect==4.8.0
mujoco==2.3.7
dm_control==1.0.14
matplotlib==3.7.5
einops==0.7.0
packaging==23.1
h5py==3.8.0
ipython==8.12.3
opencv-python==4.9.0.80
rospkg==1.5.0
empy==3.3.4
catkin-pkg
diffusers==0.26.3
termcolor==2.4.0
imageio==2.34.0
