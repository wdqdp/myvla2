# 第一版真机实验问题与原因分析

> 实验版本：第一版推理部署实验  
> 记录日期：2026-07-11  
> 本文只记录第一版真机现象、原因判断和后续验证方向。文中的“已确认”表示能够由当前代码、标签生成逻辑或训练数据直接证明；“高概率”与“待验证”表示仍需通过日志或对照实验确认。

## 1. 总体结论

这批现象不是一个单独 bug 导致的，主要来自以下四类问题：

1. **失败标签覆盖范围太窄。** 第一版把 `need_recovery` 直接绑定到人工标注的旋转区间，只覆盖抬升阶段出现旋转的失败，没有移动阶段失败、接触面积不足、碰撞、滑移或抓空等监督。
2. **`recovery_plan` 是非常强的动作捷径。** 数据中 plan 与调整方向是确定对应关系，同一视觉状态下更换 plan 的反事实样本基本不存在。消融结果证明模型确实能按 plan 调整，但也说明 plan 对动作的支配强于视觉定位。

## 2. 已确认的训练和运行时事实

### 2.1 `need_recovery` 与旋转标签完全绑定

第一版标签生成逻辑为：

```python
need_recovery = rotation_state_id != 0 and attempt_is_failed
```

`rotation_state_id` 又只由 `rotation_direction` 和人工 `shift_timestamp` 生成。shift 之前全部为 `none/false`，shift 之后到失败 attempt 结束才为旋转/true。

全量 440 个 attempt 中：

- `need_recovery=true` 共 15857 帧。
- `clockwise` 共 8767 帧，`counterclockwise` 共 7090 帧，两者相加正好也是 15857。
- `case_001/attempt1`、`case_002/attempt2`、`case_003/attempt3` 的成功轨迹没有任何 `need_recovery=true`。
- 失败 attempt 通常在观察到抬升旋转后不久结束，没有继续采集带失败状态的完整移动阶段。

因此第一版的 `need_recovery` 实际学到的是“当前是否处于已标注的抬升旋转区间”，还不是一般意义上的“当前执行是否需要恢复”。

### 2.2 tactile caption 只表达旋转，不表达接触质量

第一版 caption 只有三类：

```text
Tactile: no rotation.
Tactile: clockwise rotation.
Tactile: counterclockwise rotation.
```

它没有表达：

- 是否已经接触；
- 左右接触面积是否足够；
- 是否只夹到边缘；
- 是否正在滑移；
- 是否碰撞或顶住物体；
- 夹持力是否足够。

因此“小接触面但暂时不旋转”在 VLA prompt 中与“稳定夹持”都是 `no rotation`。

### 2.3 case3 的采集设计本身就是连续调整两次

`case_003` 共 80 个 episode，每个都包含：

```text
attempt1 失败 -> attempt2 同方向再次失败 -> attempt3 成功
```

attempt1 和 attempt2 的 `rotation_direction/tilt_direction` 相同，第三次才标记成功。这给模型和辅助头提供了很强的“两次同方向恢复”先验。

### 2.5 当前动作执行没有阶段锁存或时序融合

运行时当前行为为：

- 每次重新请求都会使用新的随机采样 key 生成 action chunk。
- 默认完整开环执行 30 步，然后再根据新 observation 重新采样。
- 不使用前一个 action chunk 的 overlap、temporal ensemble 或动作历史。
- `need_recovery` 单次概率超过 `0.5` 就立即切换 attempt，没有连续确认或迟滞。
- 一个 recovery plan 会持续输入到整个 attempt 结束，没有“该 plan 已执行一次”的标志。

相近 observation 在相邻请求中可能采样到不同动作模式，且模型无法明确知道“我刚刚已经夹紧/抬升/调整过一次”。

## 3. 各真机现象分析

原始记录中编号 4 重复，本文按 1～8 重新编号。

### 3.1 只有抬起过程中的 tactile 会进入归位，移动过程中不会（0）

**原因等级：已确认。**

主要原因不是 server 或状态机漏读 tactile，而是训练标签定义和采集范围：

1. `need_recovery=true` 只存在于失败 attempt 的旋转标注区间。
2. 这些旋转/shift 样本主要来自抓取后抬升阶段。
3. 失败 attempt 在旋转后很快停止，训练数据没有“移动过程中发生同类失败”的正样本。

即使移动阶段出现异常，系统也可能得到 `no rotation`；即使 captioner 输出 rotation，Stage B 也没有见过足够的“移动阶段 + rotation -> recovery”上下文。

**结论：** 仅调低 `need_recovery_threshold` 不能从根本上解决，反而可能增加抬升阶段误触发。需要补充移动阶段失败数据和更广义的失败标签。这个可以暂时不管

### 3.2 抓住后不动，或过较久才动；小接触面时更明显（1）

**原因等级：高概率，且“action 仍向下”已由真机观察确认。**

这并不是模型单纯输出了静止动作，而是模型仍处在示教中的“下降/闭合”阶段：

1. 真实夹爪因位置偏差或接触面较小而提前接触物体。
2. action chunk 仍命令机械臂继续下降，同时夹爪继续闭合。
3. 机械结构已经被物体约束，命令无法完成，表现为“抓着不动”。

调整：数据采集每次夹取落到底部再夹，不要一边动一边夹

### 3.3 夹住后反复抬起、落下（1）

**原因等级：高概率。**

主要是阶段歧义和 chunk 间不连续共同造成：

- 模型没有 `phase=lifted` 或“已经开始抬升”的锁存状态。
- 单帧图像和关节状态相似时，训练数据里可能同时存在下降、抬升、放回和再次抬升动作。
- 每次请求重新随机采样，可能在相邻 chunk 选择不同动作模式。
- 30 步 chunk 之间没有 overlap/ensemble，前后 chunk 不保证速度方向连续。
- 没有“夹紧后禁止再次下降，除非进入明确 recovery/place phase”的运动约束。

这类振荡更像行为克隆与运行时控制结构的问题，不像 delta/absolute action 转换错误。此前 delta -> absolute 链路已经做过数值回环验证。
调整：试试把action_chunk改为50

### 3.4 调整夹取点时反复执行夹住、抬起、落下、松开、再抓取（1）

**原因等级：已确认的数据结构问题 + 高概率的运行时放大。**

训练中的 recovery attempt 本身就是一条包含“放回、松开、归位、换点、再夹、再抬”的完整长轨迹，而且同一个 recovery plan 在全程保持不变。

模型每 30 步重新观察时，没有一个显式进度变量告诉它：

- 放回是否已经完成；
- plan 的横向调整是否已经执行；
- 是否已经重新夹紧；
- 当前应该继续抬升还是重新开始 recovery 前缀。

因此在相似状态下，它可能重新进入 recovery 轨迹的前面阶段，形成重复循环。plan 一直存在又进一步强化“继续执行重抓动作”的倾向。
调整：如果去掉归位呢？直接移动到对应的地方。

### 3.5 夹之前张开不够，或位置偏导致夹爪顶到盒子（1）

**原因等级：高概率。**

调整：每次夹爪张开就开到最大

### 3.6 第一次抓取经常抓不到中间（0）

**原因等级：高概率，但需要真机轨迹日志进一步确认。**

采集方案虽然规定第一次 attempt 固定抓中间，但训练目标只是模仿 master joint action，没有“盒子中心坐标”或“夹取点误差”的显式监督。因此模型更可能学到平均关节轨迹，而不是稳健的图像相对定位。

具体风险包括：

- 初始机械臂位置、相机外参、盒子位置与训练分布稍有偏差，平均轨迹就会偏离中心。
- 消融已经表明模型对文本 plan 的依赖强、视觉定位能力弱；第一次 attempt 又没有方向 plan 可作为纠偏信号。
- no-plan action chunk 只占全部 Stage A 样本的 41.51%，其中真正属于“第一次接近并对准中心”的片段占比更小。
- prompt 中的 `Case: runtime.` 从未在训练出现，会引入额外分布偏移。
- 没有显式的 pre-grasp 视觉闭环，默认 30 步开环可能跨过适合重新定位的时刻。


## 4. 第一版实验阶段结论

第一版已经证明了以下功能链路可以工作：

- 在线触觉能够在训练覆盖的抬升旋转阶段触发恢复；
- Stage B 能输出 failure reason 和 recovery plan；
- Stage A 能按 recovery plan 改变重新夹取方向；
- 多 attempt 推理和 30 步 action 发布链路能够在真机运行。

第一版同时暴露出：当前系统更接近“旋转检测驱动的 plan 条件行为克隆”，还不是具备接触理解、阶段记忆和稳定闭环控制的通用触觉 VLA。下一版应优先解决抓取前端的视觉定位与夹爪安全、`need_recovery` 标签解耦、恢复阶段状态表示，再考虑扩大模型规模或继续单纯训练当前结构。

- 测试了action_chunk=30/50，并不能改善进度歧义。
- 推理时触觉传感器的频率设置为30好一点。因为tactile captioner输入构造是最近30帧

## 5. 第二版数据采集要点

1.数据采集每次夹取落到底部再夹，不要一边动一边夹
2.用触觉的历史信息来解决进度歧义/用历史机械臂姿势来作为记忆
3.每次夹爪张开就开到最大



# 第二版真机实验问题与原因分析

> 实验版本：第二版推理部署实验  
> 记录日期：2026-07-22

第二版本添加了过去2s左右的历史qpos作为记忆，希望减轻进度歧义。
重新采集了120个episode
1.仅仅用120个episode训练，使用历史qpos。结果效果不好
2.把新旧一共320个episode添加在一起训练v1和v2,v2的效果优于v1。

1.当重心在右2，抓右3的时候，tactile——caption输出逆时针，但是旋转类型和恢复策略输出错误。输出都是右转，向右
2.当重心在左2，抓左3的时候，recoveryplan正确，但是没有执行恢复动作


# 第三版真机实验问题与原因分析

## 测试need_recovery判断和action发布同步
每个action发布后都更新最新观测然后输入vlm输出need_recovery

结果：
Published Step 1
sync fail when get_ros_observation
Step monitor: step=1 need_recovery=false client=137.712 ms server=136.443 ms action_to_decision=516.873 ms effective_max_rate=1.935 Hz 30Hz_deadline_met=false
Published Step 2
sync fail when get_ros_observation
Step monitor: step=2 need_recovery=false client=138.499 ms server=136.863 ms action_to_decision=228.902 ms effective_max_rate=4.369 Hz 30Hz_deadline_met=false
Published Step 3
sync fail when get_ros_observation
Step monitor: step=3 need_recovery=false client=137.950 ms server=136.289 ms action_to_decision=240.510 ms effective_max_rate=4.158 Hz 30Hz_deadline_met=false

### 解释
client=137.712 ms
客户端调用 policy.infer() 的总耗时
server=136.443 ms
server内部处理这次 need_recovery 判断的耗时
action_to_decision=516.873 ms
从开始发布当前 action，到最终得到 need_recovery 结果的完整耗时，包括：
发布action
  + 等待动作后的新同步观测
  + 生成tactile caption
  + 图像预处理和请求构造
  + WebSocket传输
  + server推理
effective_max_rate=1.935 Hz
根据完整周期计算：
  [
  1000/516.873=1.935\text{ Hz}
  ]
  即采用当前同步流程，理论上每秒最多发布约1.94个 action。

## 测试need_recovery判断和action发布异步
结果：
Async monitor: captured_step=369 handled_step=372 lag_steps=3 need_recovery=false client=117.001 ms server=115.908 ms caption=2.210 ms total=126.980 ms monitor_rate=7.683 Hz

### 解释
captured_step=369
后台任务在机械臂执行到第369个 action 时，获取了当时最新的图像、qpos和触觉输入。

handled_step=372
VLM返回判断结果时，动作线程已经执行到了第372步。

lag_steps=3
判断期间额外发布了：
372-369=3
个action。这是异步监控的动作延迟。

monitor_rate=7.683 Hz
相邻两次异步判断完成结果的实际频率约为7.68 Hz。


## 恢复要求水平移动时恢复动作几乎没有移动

猜测
### 1 训练数据中恢复的部分占比太少
从现有数据中专门挑旋转的attempt2数据中恢复抓取片段的数据训练微调一次

数据范围：
1.只用moderately的数据
2.用attempt2的数据
3.截取前k秒

UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
uv run python ../scripts/serve_tactile_vla_action_ablation.py \
  --checkpoint-kind stage-a \
  --checkpoint /data1/outputs/vla/stage_a_recovery_action_finetune/pi05_delta_tac_v3_m60_recovery_moderately_k15/4000 \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_v3 \
  --port 8000

测试了250和1000步，用人工输入prompt的方法，能够抬起来但是还是不会对应移动。和stagea15000一样。
训练到4000步模型学会了移动，但是还是存在抓取点位太高的问题。
重新看抓取点位太高的问题：
正常用stageb_best：第一次抓点位较高，但是可以通过面积太小修正
消融实验用stageb_best：第一次点位较高
消融实验用stagea_15000：第一次点位较高

### 2 stageb训练破坏stagea的知识
  启动 Stage A：

  cd /home/test/qxh/workspace/myvla2/openpi

  UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
  uv run python ../scripts/serve_tactile_vla_action_ablation.py \
    --checkpoint-kind stage-a \
    --checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_v3_m60/15000 \
    --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_v3 \
    --port 8000

  测试 Stage B 时改为：

  --checkpoint-kind stage-b \
  --checkpoint /data1/outputs/vla/stage_b_v3/pi05_stage_b_v3_m60/merged_best

  客户端：

  LD_PRELOAD=/lib/x86_64-linux-gnu/libffi.so.7 \
  python inference/agilex/inference/agilex_inference_forced_recovery_ablation.py \
    --host localhost \
    --port 8000 \
    --forced-failure-reason "failure_reason=rotate back,grasp appropriate." \
    --forced-recovery-plan "recovery_plan=move horizontally back moderately, move vertically none moderately." \
    --noise-seed 42

  操作键：空格进入 RECOVERY，s 确认成功，q 安全停止。A/B 两次试验保持相同 --noise-seed。

  - 结果证明，不是stageb的问题。stagea训练的模型也分不清moderately和slightly。动作生成的差。并且10000step的比15000的差

### 触觉太复杂导致动作差

只用旋转数据训练stagea和stageb，并且取消执行模式输入prompt中的触觉描述和最后一句话。

  cd /home/test/qxh/workspace/myvla2

  #### 检查数据、哈希、norm stats 和 GPU
  scripts/rotation_moderately_v1.sh preflight

  #### Stage A：10,000 steps
  scripts/rotation_moderately_v1.sh stage-a

  #### Stage B：4,000 steps
  scripts/rotation_moderately_v1.sh stage-b

  #### 合并 best delta
  scripts/rotation_moderately_v1.sh merge

  #### 合并模型 warm-up
  scripts/rotation_moderately_v1.sh warmup

  #### 启动推理服务
  scripts/rotation_moderately_v1.sh serve --host 0.0.0.0 --port 8000

  #### 真机客户端
  scripts/rotation_moderately_v1.sh client --host localhost --port 8000

  断点续训：

  scripts/rotation_moderately_v1.sh stage-a --resume
  scripts/rotation_moderately_v1.sh stage-b --resume

  数据和 norm stats 已准备好，不需要重新生成。如需重建：

  scripts/rotation_moderately_v1.sh prepare --overwrite
  scripts/rotation_moderately_v1.sh norm

  脚本还提供：

  scripts/rotation_moderately_v1.sh stage-a-dry
  scripts/rotation_moderately_v1.sh stage-b-dry
  scripts/rotation_moderately_v1.sh serve-stage-a
  scripts/rotation_moderately_v1.sh serve-stage-b
  scripts/rotation_moderately_v1.sh ablation-client
  scripts/rotation_moderately_v1.sh --help
  LD_PRELOAD=/lib/x86_64-linux-gnu/libffi.so.7 \
  scripts/rotation_moderately_v1.sh ablation-client \
  --host localhost \
  --port 8000 \
  --rotation-direction right \
  --noise-seed 42

  - 训练10000step stagea的效果非常差。
  数据量太少了

总结一下：数据太少，学的太杂
V4
先从旋转开始训练
扩增旋转的数据，包括轻微移动和大幅移动
把“正常“改为”大幅“
执行阶段的prompt：取消触觉描述以及最后一句话
