# V6.1：增加双夹爪法向力偏置描述

`Fx`、`Fy` 为切向力，`Fz` 为法向力。触觉元描述新增：

```text
Fz_bias=left | right | balanced
```

- `left`：物体受到朝向左夹爪的外力；
- `right`：物体受到朝向右夹爪的外力；
- `balanced`：未检测到明显朝向任一夹爪的外力。

`Fz` 继续描述整体法向力强度；`Fz_bias` 根据两只夹爪的法向力差异描述外力方向。
这里的 `left/right` 基于左右夹爪定义，与相机画面的左右方向无关。

## Captioner 输出字段

新版触觉描述的字段及取值固定为：

| 字段 | 取值 |
| --- | --- |
| `area` 当前状态 | `none / small / medium / full` |
| `Fx/Fy` 当前状态 | `negative / near_zero / positive` |
| `Fz` 当前状态 | `negative / near_zero` |
| `Fz_bias` | `left / right / balanced` |
| `rotation` | `none / clockwise / counterclockwise` |
