# V7.4.2：新环境 Stage A 微调

## 数据与基座

- 新数据：`/data1/qxh/tac_vla_new/tac_data/demon_data/black_box/lerobot_data/tactile_vla_rotation_v4_1_new_env`，原始 episode 261–350。261–270 只有 attempt1，其余 80 个 episode 具有 attempt2。
- 新 V4 索引：`/data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4_1_new_env/v4_training_index.json`。
- 归一化：同目录下的 `norm_stats/`，与新 V4 训练候选索引绑定。
- 权重基座：V7.4 Stage A `pi05_delta_tac_rotation_phase_v7_4_no_history/15000/params`。V7.4.2 从这些模型权重初始化，优化器和 step 从零开始；`--resume` 仅用于续跑同一个 V7.4.2 run。

## 阶段与 H30 规则

attempt1 全程为 `execution`。attempt2 使用离线标定的两个边界：`arm_adjustment_start` 表示夹爪第一次开到最大后开始平移，`arm_adjustment_stop` 是平移结束、夹爪开始闭合前的最后一帧。标注在 `outputs/rotation_v7_4_2_adjustment/adjustment_boundaries.json`，覆盖 80/80 个 attempt2。

边界由 LeRobot action 离线检测，采样审阅记录在同目录的 `boundary_audit_10.json`。重新生成边界使用：

```bash
openpi/.venv/bin/python scripts/prepare_v7_4_2_adjustment_boundaries.py
```

V7.2 风格的十例随机审阅表为同目录的 `boundary_audit_random.json`，只列出两个 arm 事件、帧号、全局索引、ROS 时间戳及数据划分；`manual_review_status=pending` 表示尚待人工复核。可用 `scripts/prepare_v7_4_2_boundary_audit_random.py` 按 seed 42 重建。

attempt2 从开始到 `arm_adjustment_stop`（含该帧）为 `adjustment`，下一帧起为 `execution`。使用原始连续 H30 动作监督。adjustment 起点的 H30 如果跨过 stop，则从训练样本中排除；不做静止帧压缩或 terminal hold。阶段 prompt 沿用 `phase_v2`。

新 V4 候选起点与过滤后数量：

| 划分 | 候选 | V7.4.2 可训练 | 跨阶段 H30 过滤 |
| --- | ---: | ---: | ---: |
| train | 68,253 | 66,397 | 1,856 |
| val | 8,576 | 8,344 | 232 |
| test | 8,573 | 8,341 | 232 |

## 生成并校验索引

```bash
openpi/.venv/bin/python scripts/prepare_v7_4_2_adjustment_index.py
```

输出在 `outputs/rotation_v7_4_2_adjustment/`：`action_phase_manifest.jsonl`、`filter_summary.json`、`v7_4_2_prompt_training_index.json`、`artifact_hashes.json`。构建和训练入口都会验证新 V4 索引、边界、norm stats 和 manifest 的身份。若需要检查而不写文件，加 `--dry-run`。

## 训练 10000 step

批量 8，H30、32 维动作、无状态历史，LoRA 参数继续训练。学习率采用余弦调度，从 `1e-5` 平滑降至 `1e-6`。每 1000 step 保存一次，最终模型保存于新 run 的 `10000/`。仅使用新环境的 train split。

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 \
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/train_vla_stage_a_openpi.py \
  --dataset-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/lerobot_data/tactile_vla_rotation_v4_1_new_env \
  --index-file /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_4_2_adjustment/v7_4_2_prompt_training_index.json \
  --norm-stats-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4_1_new_env/norm_stats \
  --output-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action \
  --run-name pi05_delta_tac_rotation_phase_v7_4_2_no_history \
  --data-profile rotation_phase_v7_4_2_adjustment \
  --prompt-profile phase_v2 \
  --experiment-kind phase_prompt_h30_offline_arm_adjustment_stop_raw_actions \
  --checkpoint /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action/pi05_delta_tac_rotation_phase_v7_4_no_history/15000/params \
  --batch-size 8 \
  --num-steps 10000 \
  --lr 1e-5 \
  --lr-final 1e-6 \
  --lr-transition-steps 10000 \
  --action-horizon 30 \
  --action-dim 32 \
  --no-use-state-history \
  --state-history-len 0 \
  --state-history-dim 7 \
  --history-hidden-dim 0 \
  --paligemma-variant gemma_2b_lora \
  --action-expert-variant gemma_300m_lora \
  --train-lora-only \
  --fsdp-devices 2 \
  --seed 42
```

对同一 run 续跑时使用同一命令并添加 `--resume`。训练标签可使用离线 stop；实际部署需要在线切换信号，离线事件帧不能直接用于实时推理。

当前状态：索引已生成，数据加载 dry-run 与相关测试通过。当前工作环境的 JAX 只识别 CPU，10000 step 的 GPU 训练尚未启动。
