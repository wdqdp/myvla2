# V8.2

## 数据处理

V8.2 仅重新处理 `small_grasp` 的 attempt2；`one_success`、`moderate_lift`、
`slightly_lift` 的候选帧、过滤结果和 H30 offset 与 V7.4 逐项校验一致。

`small_grasp` 的四个运动边界为：

| 边界 | 定义 |
|---|---|
| G：`gripper_motion_stop` | attempt2 开始后，夹爪 action 第一次达到最大值的帧 |
| A：`arm_adjustment_start` | G 后机械臂结束静止、末端开始持续下降的首帧 |
| S：`arm_adjustment_stop` | rexecution 附近持续下降运动的最后一帧 |
| C：`gripper_close_start` | S 后夹爪开始持续闭合的首帧 |

末端 Z 由 Piper URDF 正运动学计算。下降检测阈值为 `0.1 mm/frame`，至少
5 个活动帧、总下降至少 `5 mm`；相距不超过 5 帧的下降片段视为同一次下降。
夹爪闭合阈值为 `0.00005/frame`，至少 5 个活动帧、总闭合量至少 `0.005`。
S 在 `[R-60,R+5]` 内搜索，C 在 `[R-5,R+60]` 内搜索。
生成器强制检查 `G < A <= S < C`，并在 `boundary_filter.json` 中保存边界、
ROS 时间戳、检测信号和运动片段，便于审计。

训练起点和 H30 沿用 V7.4：

- 不训练严格位于 `(G,A)` 和 `(S,C)` 内的静止起点；
- 对 G 前 H30 覆盖范围内的起点，保留到 G 的前缀，跳过 `(G,A)`，再从 A
  连续取帧补满 H30；不使用 terminal hold；
- 过滤 adjustment 阶段跨越 S 的 H30 起点，并过滤跨越 rexecution 的起点；
- `small_grasp` 的下降可能短于 20 帧，此时不存在既保留原始下降起点、又让
  H30 在 S 后最多包含 10 帧的解。因此移除这些原始下降起点，下降监督由 G 前
  经 V7.4 拼接后的 H30 提供；若拼接后的末帧超过 `min(S+10,R-1)`，该起点也移除。

生成 training index：

```bash
openpi/.venv/bin/python scripts/prepare_v8_2_adjustment_index.py
```

默认输出目录：

```text
/data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v8_2_adjustment
```

训练索引为 `v8_2_prompt_training_index.json`。

## Stage-A 训练

V8.2 从原始 `pi05_base` 权重训练无历史 state 的 Action Expert，训练协议与
V7.4 相同。

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 \
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/train_vla_stage_a_openpi.py \
  --dataset-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/lerobot_data/tactile_vla_rotation_v4 \
  --index-file /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v8_2_adjustment/v8_2_prompt_training_index.json \
  --norm-stats-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats \
  --output-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action \
  --run-name pi05_delta_tac_rotation_phase_v8_2_no_history \
  --data-profile rotation_phase_v8_2_adjustment \
  --prompt-profile phase_v2 \
  --experiment-kind phase_prompt_h30_v7_4_policy_detected_small_grasp_boundaries \
  --checkpoint /home/qxh/.cache/openpi/openpi-assets/checkpoints/pi05_base/params \
  --batch-size 8 \
  --num-steps 15000 \
  --no-use-state-history \
  --state-history-len 0 \
  --state-history-dim 7 \
  --history-hidden-dim 0 \
  --fsdp-devices 2 \
  --seed 42
```
