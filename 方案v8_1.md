# V8.1

## 数据处理

V8.1 使用更新后的 black_box 数据集。除 `small_grasp` 外，边界、起点过滤和 H30 监督均与 V7.4 保持一致。

`small_grasp` 的边界定义为：

- `gripper_motion_stop = arm_adjustment_start = move_start_timestamp` 对应帧；
- `arm_adjustment_stop = gripper_close_start = rexecution_timestamp` 对应帧；
- 时间戳映射到首个满足 `ros_timestamp >= timestamp` 的帧；
- 因两个静止区间为空，不压缩 H30；仍过滤跨越 `arm_adjustment_stop` 的 adjustment H30 起点。

生成 training index：

```bash
openpi/.venv/bin/python scripts/prepare_v8_1_adjustment_index.py
```

默认训练索引：

```text
/data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v8_1_adjustment/v8_1_prompt_training_index.json
```

## Stage-A 训练

V8.1 从原始 `pi05_base` 权重开始训练无历史 state 的 Action Expert，训练参数与 V7.4 相同。

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 \
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/train_vla_stage_a_openpi.py \
  --dataset-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/lerobot_data/tactile_vla_rotation_v4 \
  --index-file /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v8_1_adjustment/v8_1_prompt_training_index.json \
  --norm-stats-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats \
  --output-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action \
  --run-name pi05_delta_tac_rotation_phase_v8_1_no_history \
  --data-profile rotation_phase_v8_1_adjustment \
  --prompt-profile phase_v2 \
  --experiment-kind phase_prompt_h30_v7_4_policy_refreshed_small_grasp_equal_boundaries \
  --checkpoint /home/qxh/.cache/openpi/openpi-assets/checkpoints/pi05_base/params \
  --batch-size 8 \
  --num-steps 15000 \
  --no-use-state-history \
  --state-history-len 0 \
  --state-history-dim 7 \
  --history-hidden-dim 0 \
  --paligemma-variant gemma_2b_lora \
  --action-expert-variant gemma_300m_lora \
  --fsdp-devices 2 \
  --seed 42
```

验证：

 OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
  USE_TF=0 \
  openpi/.venv/bin/python scripts/infer_vla_five_adjustments.py \
    --episode 221 \
    --attempt 2 \
    --timestamp 1789394594.674148 \
    --model /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action/pi05_delta_tac_rotation_phase_v8_3_no_history/15000 \
    --output /home/qxh/workspace/myvla2/tmp/mod83_ep221_at2_t1789394594.674148.json


  G = gripper_motion_stop
  A = arm_adjustment_start
  S = arm_adjustment_stop
  C = gripper_close_start
  R = rexecution_frame
