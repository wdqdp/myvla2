# 基于触觉反馈的多步自适应调整 Tactile-VLA 初步方案

## 1. 研究目标

本方案面向未知物理属性物体的抓取与转移任务，目标是构建一个能够根据触觉反馈进行失败原因诊断，并执行多步自适应调整的触觉 VLA 系统。

核心问题是：在物体的重量、粗糙度、易碎性、偏心程度等物理属性未知的情况下，如何使 VLA 在抓取和转移过程中利用触觉反馈判断当前执行是否稳定，诊断失败原因，并通过多轮“探测-诊断-调整-验证”的闭环过程完成任务。


## 2. 关键问题

### 2.1 未知物理属性下的触觉反馈理解

第一版先聚焦偏心物体。物体质心位置通常无法从单帧视觉中可靠判断，但会在抓取和抬升过程中表现为局部接触旋转和整体姿态倾斜。

触觉模块的作用不是判断物体最终向左、右、前、后哪个方向倾斜，而是从形变场中更早识别局部旋转状态：

- no rotation。
- clockwise rotation。
- counterclockwise rotation。

这些分类结果通过固定模板转化为 VLA 可理解的 tactile caption。

### 2.2 失败原因诊断与恢复动作生成

触觉异常并不直接等价于某一种固定恢复动作。相同的触觉模式在不同物体、不同任务目标和不同接触阶段下，可能对应不同的恢复策略。

例如，检测到局部旋转时：

- VLA 结合视觉判断物体向 `left/right/front/back` 哪个方向倾斜。
- VLA 输出对应的 `failure_reason`，例如 `tilted left`。
- Reasoning Mode 生成对应的 `recovery_plan`，例如 `Regrasp left.`

因此，恢复策略应由 VLA 结合视觉、语言任务、机器人状态、tactile caption 和历史恢复记忆共同推理得到，而不是由固定规则直接映射。

### 2.3 多步闭环调整

本方案强调恢复不是一次性动作，而是一个多步闭环过程：

1. 探测：执行当前 action chunk，并收集对应触觉形变序列。
2. 诊断：根据触觉旋转状态和视觉姿态判断是否存在失败或不稳定状态。
3. 调整：生成 recovery plan，并在后续多个 chunk 中持续作为条件输入。
4. 验证：通过新的触觉反馈判断恢复是否有效，必要时进入下一轮恢复尝试。

这种设计使模型能够处理多轮偏心恢复，例如第一次重抓后仍出现局部旋转，模型可以在下一轮诊断中进一步调整重抓方向。

## 3. 第一版范围与优先级

第一版不同时展开所有未知物理属性，而是将问题收敛到偏心物体抓取-抬升-转移任务。重量、粗糙度、易碎性等属性只作为后续扩展方向，不进入第一版标签体系和主实验结论。

第一版需要先完成一个最小可验证闭环：

1. 采集偏心物体抓取-转移轨迹，并按 action chunk 对齐图像、机器人状态、动作和触觉序列。
2. 训练 Tactile Captioner，将每个 chunk 的触觉序列分类为 `none / clockwise / counterclockwise`。
3. 将分类结果转成固定 tactile caption，作为 VLA 的文本输入。
4. 在 VLA 中判断 `need_recovery`，输出四类倾斜诊断 `tilted left/right/front/back`。
5. 在 Reasoning Mode 中生成四类模板化恢复计划 `Regrasp left/right/front/back.`。
6. 通过多轮 recovery attempt 验证触觉反馈是否能更早触发恢复，并减少重复无效恢复。

## 4. 总体系统结构

系统由两个核心模块组成：

1. Tactile Captioner：负责将一个 action chunk 内的触觉形变序列分类为局部旋转状态，并用固定模板生成 tactile caption。
2. VLA：负责根据视觉、语言、机器人状态、tactile caption 和恢复记忆进行失败判断、失败诊断、恢复计划生成和动作输出。


## 5. Tactile Captioner

### 5.1 模块职责

Tactile Captioner 是触觉感知分类模块，不是语言模型。它只负责从传感器表面的运动场和力场中识别局部旋转状态，不直接预测重量、粗糙度、易碎性、偏心程度，也不直接输出 failure reason 或 recovery plan。

输入是一个 action chunk 执行期间采集到的触觉序列，包含两类张量：

```text
mesh_motion: shape=(T, 35, 20, 12)
force: shape=(T, 35, 20, 6)
```

其中 left/right 同一时刻的数据在通道维拼接：

- `mesh_motion` 为 left `(Mesh3DFlow, Mesh3D)` 6 通道 + right `(Mesh3DFlow, Mesh3D)` 6 通道。
- `force` 为 left `Force` 3 通道 + right `Force` 3 通道。

第一版可以使用简单的 CNN+TCN 编码 `mesh_motion` 和 `force`，后接一个分类头：

```text
rotation_state: none / clockwise / counterclockwise
```

### 5.2 固定模板输出

分类结果通过固定模板转成 VLA 的文本输入：

| rotation_state | tactile caption |
|---|---|
| `none` | `Tactile: no rotation.` |
| `clockwise` | `Tactile: clockwise rotation.` |
| `counterclockwise` | `Tactile: counterclockwise rotation.` |

Tactile Captioner 只提供局部接触旋转证据。物体最终往 `left/right/front/back` 哪个方向倾斜，由 VLA 结合视觉和机器人状态判断。

### 5.3 与 VLA 的同步方式

推理时，Tactile Captioner 与 VLA 按 action chunk 同步运行：

1. VLA 根据当前图像、指令、机器人状态和当前 recovery plan 输出一个 action chunk。
2. 机器人执行该 action chunk，同时记录该 chunk 内的触觉形变序列。
3. chunk 执行结束或被安全策略中断后，将该段触觉序列重采样到固定长度 T。
4. Tactile Captioner 输出 chunk-level `rotation_state`。
5. `rotation_state` 通过固定模板转成 tactile caption，进入下一次 VLA status 判断和动作生成。

因此，tactile caption 表示最近一个已执行 chunk 的触觉反馈，而不是未来动作的先验信息。第一版先采用 chunk 后处理同步方式，不做逐帧在线中断；在线中断可作为后续扩展。

### 5.4 触觉形变序列重采样

不同 action chunk 的执行时间可能不同，因此采集到的触觉帧数也可能不同。为保证输入格式一致，推理时将每个 chunk 的触觉形变序列统一重采样到固定长度 T。

训练时不在采集阶段预先切 chunk，而是在数据读取阶段为每个可训练 frame 动态读取过去一段固定长度 T 的触觉窗口。T 需要在训练前确定，并保证训练与推理阶段的时间覆盖范围一致。

### 5.5 输出粒度

Tactile Captioner 输出的是最近触觉窗口或最近已执行 chunk 的局部旋转状态，而不是逐帧瞬时结果。第一版只判断该窗口内是否已经出现明确的 `clockwise` 或 `counterclockwise` 旋转证据；若没有明显旋转，则输出 `none`。

## 6. VLA 模块

VLA 是系统中的决策与推理主体，负责：

- 根据当前上下文判断是否需要恢复。
- 输出失败原因。
- 在 reasoning mode 中生成 recovery plan。
- 在 execution mode 中根据 recovery plan 和当前观察输出 action chunk。
- 维护 failure-recovery memory，用于避免重复无效恢复策略。

VLA 有两种主要工作模式：Execution Mode 和 Reasoning Mode。

## 7. VLA 模型架构设计

本方案中的 VLA 基于 pi05 架构扩展：保留连续 action expert 生成 action chunk，同时在共享的视觉-语言上下文上增加恢复判断和语言生成能力。

### 7.1 共享上下文编码

模型输入统一组织为 prefix tokens：

- 图像、任务指令、机器人状态。
- 当前 chunk 的 tactile caption。
- recovery plan 和 failure-recovery memory。
- mode token，用于区分 `execution` 和 `reasoning`。

这些信息共享同一个 PaliGemma 视觉-语言主干编码。动作生成仍使用 pi05 的 action suffix 和 flow matching 方式，不将连续动作强行改成离散文本 token。

### 7.2 三类输出头

VLA 共享同一个 backbone，但包含三类输出接口：

1. `need_recovery` 判别头：接在 `<STATUS_QUERY>` token 的 hidden state 上，输出是否需要恢复。
2. LM head：自回归生成失败诊断和 recovery plan。失败诊断与 recovery plan 使用同一个 LM head，通过 mode token 和 query token 区分任务。
3. action flow head：沿用 pi05 的连续 action expert，输出下一段 action chunk。

这种设计使模型既能进行实时动作控制，也能输出可解释的失败诊断和恢复计划。

### 7.3 Execution Mode 的单次前向结构

Execution Mode 中建议使用如下 token 结构：

```text
[prefix: image / tactile caption / state / instruction / recovery plan]
<EXEC>
<STATUS_QUERY>
<DIAGNOSIS_BOS>
[diagnosis tokens]
[action suffix]
```

`need_recovery` 由 `<STATUS_QUERY>` 的 hidden state 直接预测。诊断文本从 `<DIAGNOSIS_BOS>` 开始自回归生成。二者共享同一次 prefix prefill 和 KV cache，因此不需要在判断失败后重新编码图像、触觉和状态。

推理时，如果 `need_recovery = false`，系统可以停止诊断解码并只使用 action chunk；如果 `need_recovery = true`，则继续利用已有 KV cache 生成失败诊断。

### 7.4 Reasoning Mode 的自回归计划生成

Reasoning Mode 中不直接输出动作，只生成 recovery plan：

```text
[prefix: image / failed tactile caption / state / instruction / failure-recovery memory]
<REASON>
<RECOVERY_PLAN_BOS>
[recovery plan tokens]
```

生成的 recovery plan 会作为后续 recovery attempt 中每个 execution chunk 的持续条件输入，引导 action expert 完成多步恢复。

`recovery_plan` 的文本语义保持不变，只表示重抓方向，例如 `Regrasp left.`。后续 recovery attempt 的动作序列固定包含三个阶段：先放回当前抓住的物体，再回到起始位，最后根据 `recovery_plan` 重新抓取并继续抬升/转移。这三个阶段都由 Execution Mode 的 action expert 生成，不由低级线性插值执行器单独完成。

## 8. Stage 1：Execution Mode

### 8.1 输入

Execution Mode 的输入包括：

- 图像 image。
- 任务指令 instruction。
- 机器人状态 robot state。
- 当前 chunk 的 tactile caption。
- recovery plan，初始为空；若处于恢复尝试中，则为当前 attempt 的持续条件。
- mode = execution。

### 8.2 输出逻辑

Execution Mode 在同一次前向中准备三类输出：

```json
{
  "need_recovery": true
}
```

`need_recovery` 由 `<STATUS_QUERY>` 对应的分类头输出，action chunk 由 action flow head 输出，失败诊断由 LM head 从 `<DIAGNOSIS_BOS>` 开始生成。

如果 `need_recovery = false`，则停止诊断解码或忽略诊断分支，只执行 action expert 输出的下一段 action chunk。

如果 `need_recovery = true`，则停止继续执行原任务轨迹，LM head 继续利用已有 KV cache 自回归输出 `failure_reason`。

第一版不再使用低级关节插值作为任务级归位策略。触发失败时，机械臂可能仍然抓着物体，放回物体和回到起始位都交给后续 recovery attempt 的 VLA action expert 生成。

当失败诊断完成后，系统将当前失败 attempt 写入 `failure_recovery_memory`，并立即进入 Stage 2：Reasoning Mode。此时不等待单独的归位完成信号，也不新增 `return_done` 判断。

### 8.3 失败诊断输出格式

示例：

```json
{
  "need_recovery": true,
  "failure_reason": "tilted left"
}
```

第一版只保留偏心物体抓取中的四种倾斜诊断：

- `tilted left`
- `tilted right`
- `tilted front`
- `tilted back`

`failure_reason` 使用短模板表达物体倾斜方向，不再单独输出 `failure_type`。

## 9. Stage 2：Reasoning Mode

### 9.1 输入

Reasoning Mode 的输入包括：

- 图像 image。
- 任务指令 instruction。
- 机器人状态 robot state。
- failed tactile caption，也就是触发 `need_recovery = true` 的那个 chunk 的 tactile caption。
- failure-recovery memory。
- mode = reasoning。

Reasoning Mode 不再单独维护当前失败原因字段。本次刚失败的 attempt 会先写入 `failure_recovery_memory`，因此 memory 的最后一条就是当前失败上下文。

`failure_recovery_memory` 表示已经执行并失败的 attempt 历史。每个 item 中的 `recovery_plan` 表示该 attempt 实际执行时使用的高层策略；对于第一次正常抓取 attempt，固定写为 `"initial grasp"`。

示例：

```json
{
  "failure_recovery_memory": [
    {
      "attempt_id": 1,
      "recovery_plan": "initial grasp",
      "failure_reason": "tilted left"
    }
  ]
}
```

### 9.2 输出

Reasoning Mode 只输出 recovery plan，不直接输出动作。

输出形式是一句模板化的恢复计划，例如：

```json
{
  "recovery_plan": "Regrasp left."
}
```

### 9.3 Recovery Plan 的持续条件作用

一旦生成 recovery plan，该计划将在当前 recovery attempt 的每个后续 action chunk 中作为持续条件输入 VLA。

也就是说，在恢复尝试完成之前，每个 execution chunk 都能看到同一个高层恢复意图，从而保持恢复行为的一致性。

例如，当前 recovery plan 为：

```text
Regrasp left.
```

那么后续 action expert 输出动作时，应持续受该计划约束，完成“放回物体 -> 回起始位 -> 按该方向重新靠近和重新夹持 -> 抬升/转移”的连续动作序列。

## 10. Recovery Attempt 机制

一次 recovery attempt 可以包含多个 action chunk。系统需要维护以下状态：

- 当前是否处于 recovery attempt 中。
- 当前 attempt 的 recovery plan。
- 当前 attempt 的起始 chunk。
- 当前 attempt 内的 tactile caption 历史。
- 当前 attempt 是否成功。

每个 recovery attempt 的动作前缀固定为：

1. 将当前仍抓着或接触中的物体放回支撑面。
2. 机械臂回到起始位或可重新抓取的标准位姿。
3. 根据当前 `recovery_plan` 重新抓取。
4. 继续抬升、转移和放置。

这段归位动作属于 recovery attempt 的 Execution Mode 训练目标，不单独设置低级 fallback controller，也不需要额外判断“归位是否结束”。VLA action expert 通过图像、机器人状态和连续动作监督学习在多个 chunk 中完成阶段切换。

如果恢复过程中 VLA 判断 `need_recovery = false`，并且任务进展重新稳定，则继续执行当前 plan 或逐步退出恢复状态。

如果正常抓取或恢复过程中触发 `need_recovery = true`，则将当前失败 attempt 写入 failure-recovery memory，并进入下一轮 reasoning。`attempt1` 的 memory item 使用 `recovery_plan="initial grasp"`；恢复 attempt 的 memory item 使用该 attempt 的 `input_recovery_plan`。


## 11. 训练数据组织

### 11.1 原始数据目录结构

第一版采集任务固定为偏心物体的抓取-抬升-短距离转移-放置。原始数据按 episode 和 recovery attempt 两级组织：

```text
data/
  episode1/
    attempt1/
    attempt2/
    attempt3/
  episode2/
    attempt1/
```

一个 episode 表示一次完整任务，从机器人初始姿态开始，记录到任务成功、恢复失败或达到最大恢复次数为止。每个 episode 最多包含 3 个 attempt；如果 3 次 attempt 都未能稳定完成任务，则该 episode 标记为任务失败。

一个 attempt 表示一次连续执行片段：

- `attempt1` 是正常抓取和抬升尝试，输入 recovery plan 为空。
- `attempt2` 和 `attempt3` 是恢复尝试，输入 recovery plan 来自上一轮 reasoning。恢复尝试从上一轮失败结束时的物理状态开始，动作序列包含放回物体、回起始位、按 `recovery_plan` 重新抓取、抬升和转移。
- attempt 之间不允许训练样本跨界，避免把上一轮失败片段或下一轮恢复动作混入当前 attempt 的 action chunk。

每个 attempt 下保存各模态原始数据和逐帧标签：

- 多视角或单视角图像 `image`。
- 任务指令 `instruction`，第一版可以固定为 `Pick up and transfer the object stably.`。
- 机械臂关节状态 `arm/jointState`，字段命名参考 `data_tools_tac/config/aloha_data_params.yaml`：`masterRight` 来自 `/master/joint_right`，`puppetRight` 来自 `/puppet/joint_right`，按原始采集频率逐帧保存 `position`、`velocity` 和 `effort`。
- 触觉数据 `mesh_motion` 和 `force`，保留原始帧率时间戳。
- 在线采集元信息 `meta.json`，只记录 `episode_id`、`attempt_id`、`case_id`、采集前设置的 `rotation_direction`、`tilt_direction`、shift 节点时间戳、开始/结束时间戳等。
- 离线标注后的逐帧标签 `labels`。

原始数据以 frame 为基本索引单位。采集阶段不在命令行设置 `failure_reason`、`recovery_plan`、`input_recovery_plan` 或 `failure_recovery_memory`，这些字段在离线标注和数据转换阶段生成。action chunk 和 Tactile Captioner 的触觉窗口都在训练读取阶段动态生成，而不是在采集阶段预先切好。上述 YAML 只用于确定机械臂 `jointState` 的名称和 topic；触觉字段仍以本方案规划的 `mesh_motion` 和 `force` 为准，不参考该 YAML 中的 tactile 配置。

### 11.2 逐帧标签规范

每一帧需要保存以下标签字段：

```json
{
  "episode_id": 1,
  "attempt_id": 2,
  "frame_index": 37,
  "timestamp": 1778226902.85,
  "rotation_state": "clockwise",
  "need_recovery": true,
  "failure_reason": "tilted left",
  "recovery_plan": "Regrasp left.",
  "input_recovery_plan": "Regrasp front.",
  "failure_recovery_memory": [
    {
      "attempt_id": 1,
      "recovery_plan": "initial grasp",
      "failure_reason": "tilted right"
    }
  ]
}
```

`rotation_state` 是 Tactile Captioner 的监督标签，只允许：

- `none`
- `clockwise`
- `counterclockwise`

`need_recovery` 是 VLA 的恢复判断监督标签。第一版可以用同一个人工旋转节点初始化 `rotation_state` 和 `need_recovery`，但二者语义不同：`rotation_state` 表示触觉局部旋转证据，`need_recovery` 表示当前上下文下是否应进入恢复流程。

`failure_reason` 是 VLA 的失败诊断标签，只允许：

- `tilted left`
- `tilted right`
- `tilted front`
- `tilted back`

`recovery_plan` 是 Reasoning Mode 的输出标签，只允许：

- `Regrasp left.`
- `Regrasp right.`
- `Regrasp front.`
- `Regrasp back.`

`input_recovery_plan` 是当前 attempt 执行时输入给 Execution Mode 的恢复计划。`attempt1` 的 `input_recovery_plan` 为空；`attempt2` 和 `attempt3` 使用前一轮 Reasoning Mode 生成或人工标注的计划。

`failure_recovery_memory` 表示已经执行并失败的 attempt 历史，第一版只作为 Reasoning Mode 的输入使用。memory item 保持字段名 `recovery_plan`，但在 memory 中它表示该失败 attempt 实际执行时使用的策略：

- `attempt1` 失败后进入 Reasoning Mode 时，memory 追加 `{attempt_id: 1, recovery_plan: "initial grasp", failure_reason: <attempt1 failure_reason>}`。
- `attempt2` 执行时，memory 已包含 `attempt1` 的失败记录；如果 `attempt2` 失败，进入 Reasoning Mode 前再追加 `{attempt_id: 2, recovery_plan: <attempt2 input_recovery_plan>, failure_reason: <attempt2 failure_reason>}`。
- `attempt3` 是设计中的成功 attempt，不再需要 memory；其 Execution 样本只使用第二次 reasoning 输出的 `input_recovery_plan`，`failure_recovery_memory` 置空。

Reasoning Mode 样本发生在失败诊断之后，因此其 memory 需要包含当前刚失败的 attempt，最后一条就是本轮 reasoning 的当前失败上下文。Execution Mode 第一版不使用 memory，避免第三次确定成功的执行样本还携带历史失败上下文。

### 11.3 Rotation State 标注协议

`rotation_state` 的方向统一以左夹爪触觉坐标系为准。也就是说，`clockwise/counterclockwise` 表示在左夹爪标准视角下观察到的局部接触旋转方向，而不是左右两个触觉传感器各自图像坐标中的顺时针或逆时针。

采集时采用双人协作遥操作：

1. 一人负责遥操作机械臂。
2. 一人观察屏幕上的实时触觉旋转方向和力检测图。
3. 每个 attempt 开始前，命令行只设置 `case_id`、本次预期旋转方向 `clockwise/counterclockwise/none`，以及本次前后左右倾斜方向 `left/right/front/back/none`。
4. 当观察到明显旋转趋势后，等待约 `0.5s`，使触觉形变和力场出现足够明确的变化，再标记旋转节点。
5. 离线标注时，旋转节点之前的帧标为 `none`。
6. 离线标注时，旋转节点之后到该 attempt 结束前的帧标为采集前设置的 `clockwise` 或 `counterclockwise`。
7. 遥操作人员继续举起机械臂，并在物体停止转动前结束当前 attempt。

因此，`clockwise/counterclockwise` 的标签语义不是“当前帧刚开始旋转”，而是“截至当前帧，过去一段触觉窗口中已经出现明确、可观测的旋转证据”。这个定义与 Tactile Captioner 读取过去触觉窗口的训练方式保持一致。

由于同一物体旋转在左右夹爪传感器上可能呈现相反方向，标注和可视化界面必须使用左夹爪坐标系下的统一方向。左右夹爪的触觉信号在数据中保留各自原始坐标和通道身份，不对右夹爪触觉信号做镜像或符号变换。

### 11.4 Tactile Captioner 数据读取

Tactile Captioner 训练时不提前把原始数据切成 chunk，而是参考 pi05 加载 action chunk 的方式，对每个可训练 frame 动态读取过去一段固定长度的触觉窗口。

对某一帧 `i`，读取同一 attempt 内 `[i - T + 1, i]` 的触觉序列。窗口重采样或采样后格式为：

```text
mesh_motion: shape=(T, 35, 20, 12)
force: shape=(T, 35, 20, 6)
```

其中 left/right 同一时刻的数据在通道维拼接，格式与第 5 节保持一致。左右夹爪触觉数据按各自原始坐标保留，并通过固定通道顺序区分左右来源；窗口标签使用末帧 `i` 的 `rotation_state`。

触觉窗口必须满足以下约束：

- 窗口不能跨 attempt 边界。
- attempt 开头不足一个完整窗口的样本第一版直接跳过。
- 训练窗口长度应与 `0.5s` 标注延迟和 VLA action chunk 时长匹配。第一版默认设置 `publish_rate=30Hz`、`chunk_size=30`、`action_horizon=30`，因此一个 action chunk 约为 `1.0s`。
- Tactile Captioner 的默认窗口时长也取 `1.0s`；若触觉采样率为 `30Hz`，则 `T=30`。
- 如果后续发现窗口过长导致旋转标签样本中 `none` 占比过高，则增加有效旋转占比过滤规则。

Tactile Captioner 只使用 `mesh_motion`、`force` 和 `rotation_state`，不使用 `need_recovery`、`failure_reason` 或 `recovery_plan`。

### 11.5 VLA LeRobot 数据读取

VLA 训练数据使用 LeRobot 逐帧格式。第一版将每个 attempt 转换为一个独立的 LeRobot episode

每一帧保存以下 LeRobot 字段或等价字段：

- `observation.images.*`
- `observation.state`，由 `arm/jointState/puppetRight/position` 转换得到，作为 VLA 输入的机器人状态。
- `action`，由 `arm/jointState/masterRight/position` 转换得到，作为 action expert 的监督目标。
- `task` 或 `instruction`
- `attempt_id`
- `input_recovery_plan`
- `failure_recovery_memory`
- `rotation_state`
- `tactile_caption`
- `need_recovery`
- `failure_reason`
- `recovery_plan`

LeRobot loader 按 pi05 的方式从当前 frame 读取未来 `action_horizon` 帧作为 action chunk。第一版将训练配置中的 `action_horizon` 设为 `30`，推理客户端对应设置 `chunk_size=30`、`publish_rate=30Hz`。距离 attempt 结束不足 `action_horizon` 的 frame 不参与 action loss，避免 action target 越界或跨 attempt。

VLA 第一版不直接输入 raw tactile tensor，只输入由 `rotation_state` 映射得到的 tactile caption：

| rotation_state | tactile caption |
|---|---|
| `none` | `Tactile: no rotation.` |
| `clockwise` | `Tactile: clockwise rotation.` |
| `counterclockwise` | `Tactile: counterclockwise rotation.` |

attempt 的第一帧或没有上一段触觉反馈的位置，可以使用固定占位文本：

```text
Tactile: no rotation
```

需要新增项目专用 LeRobot DataConfig/transform，参考现有 pi05 数据接口完成：

- 将数据集图像、`arm/jointState/puppetRight/position` 和 `arm/jointState/masterRight/position` repack 到 openpi 需要的 `image`、`state` 和 `actions`，其中 `state=puppetRight position`，`actions=masterRight position`。
- 将 `instruction`、`tactile_caption` 和 `input_recovery_plan` 组合成 Execution Mode prompt。
- 将 `need_recovery` 和必要时的 `failure_reason` 作为 Status/Diagnosis 监督目标。
- 将 failed tactile caption 和包含当前失败 attempt 的 memory 组合成 Reasoning Mode prompt，并将 `recovery_plan` 作为生成目标。

### 11.6 输入与目标防泄漏规则

人工标注的字段需要区分“当前输入”和“训练目标”：

- Execution Mode 中，`input_recovery_plan` 可以作为输入；当前帧的 `failure_reason` 是诊断目标，不能放入输入 prompt。
- Status/Diagnosis 样本中，`need_recovery` 和 `failure_reason` 是监督目标，不能作为输入。
- Reasoning Mode 中，`failure_recovery_memory` 是输入，且最后一条是当前刚失败的 attempt；`recovery_plan` 是生成目标，不能作为输入。
- Recovery Attempt Execution 中，当前 attempt 的 `input_recovery_plan` 是持续条件输入。
- Execution Mode 第一版不输入 `failure_recovery_memory`；该字段只用于 Reasoning Mode。

### 11.7 数据一致性检查

数据转换和训练前需要检查：

- 每个 episode 不超过 3 个 attempt。
- 每个 attempt 内各模态时间戳单调递增，并可按 frame 对齐。
- Tactile Captioner 触觉窗口不跨 attempt。
- rotation 节点前 `rotation_state=none`，节点后到 attempt 结束为人工指定方向。
- Execution Mode 中，`failure_recovery_memory` 应为空或不进入 prompt。
- Reasoning Mode 中，memory 的最后一条必须是当前刚失败的 attempt。
- VLA action chunk 不跨 attempt 边界。
- Execution、Diagnosis 和 Reasoning 输入中没有泄漏目标字段。

## 12. 训练目标

### 12.1 Tactile Captioner 训练目标

Tactile Captioner 采用分类监督目标，不使用语言建模目标。训练重点是让模型根据过去一段固定长度触觉窗口预测局部旋转状态：

- `none`
- `clockwise`
- `counterclockwise`

预测结果再通过固定模板生成 tactile caption，作为 VLA 的文本输入。

### 12.2 VLA Execution Mode 训练目标

Execution Mode 可以包含两个目标：

1. `need_recovery` 分类目标。
2. action chunk 生成目标。

其中 recovery attempt 的 action 监督需要覆盖完整动作前缀：放回物体、回到起始位、根据 `input_recovery_plan` 重新抓取、继续抬升和转移。`input_recovery_plan` 的文本标签不变，仍使用 `Regrasp left/right/front/back.` 四类。

对于触发恢复的样本，还需要训练失败诊断输出：

1. `failure_reason` 自回归文本生成目标。
2. `failure_reason` 只使用 `tilted left/right/front/back` 四种短模板。

### 12.3 VLA Reasoning Mode 训练目标

Reasoning Mode 使用自回归语言建模目标生成 recovery plan。训练时重点让模型学会利用：

- 当前失败触觉 caption。
- 视觉上下文。
- 任务指令。
- 机器人状态。
- failure-recovery memory 中已有尝试及其失败原因。

从而避免重复失败策略，并提出更合理的下一轮恢复计划。

第一版 recovery plan 只使用 `Regrasp left/right/front/back.` 四种短模板。

## 13. 推理流程

完整推理流程如下：

1. 初始化 recovery plan 为空，failure-recovery memory 为空。
2. VLA 在 execution mode 下根据图像、指令、机器人状态和当前 recovery plan 输出 action chunk。
3. 机器人执行 action chunk，并采集触觉形变序列。
4. 将触觉形变序列重采样到固定长度 T。
5. Tactile Captioner 输出 `rotation_state`，并通过固定模板生成 tactile caption。
6. VLA 根据 tactile caption、视觉和状态判断 `need_recovery`。
7. 如果不需要恢复，继续生成下一段 action chunk。
8. 如果需要恢复，VLA 输出 failure reason，并停止继续执行当前原任务轨迹。
9. 将当前失败 attempt 追加到 failure-recovery memory：`attempt1` 写入 `recovery_plan="initial grasp"`，恢复 attempt 写入本次 `input_recovery_plan`。
10. 进入 reasoning mode，VLA 根据失败 caption 和 failure-recovery memory 生成新的 recovery plan。
11. 将 recovery plan 作为后续 recovery attempt 中每个 chunk 的持续条件输入。
12. Recovery attempt 先由 VLA 生成放回物体和回起始位动作，再按 recovery plan 重新抓取并继续任务。
13. 如果恢复失败，重复上述失败诊断、写入 memory 和 reasoning 流程。
14. 如果任务成功或恢复稳定，结束或继续执行原任务。

### 13.1 推理状态机

第一版推理系统维护四种状态：

1. `normal_execution`：无 recovery plan，执行原始抓取-转移任务。
2. `reasoning`：根据失败上下文和 memory 生成新的 recovery plan。
3. `recovery_attempt`：持续使用同一个 recovery plan 执行多个 action chunk；动作顺序为放回物体、回起始位、重新抓取、抬升/转移。
4. `success_or_failure`：任务成功、达到最大恢复次数或人工安全终止。

状态转移规则如下：

- `normal_execution -> reasoning`：最近一个 chunk 的 tactile caption 和视觉状态触发 `need_recovery=true`，`failure_reason` 已生成，并且当前失败 attempt 已写入 memory。
- `reasoning -> recovery_attempt`：Reasoning Mode 输出新的 `recovery_plan`。
- `recovery_attempt -> recovery_attempt`：后续 chunk 未触发新的恢复，继续沿用当前 plan。
- `recovery_attempt -> reasoning`：恢复过程中再次触发 `need_recovery=true`，当前 attempt 记为失败，写入 memory，并生成下一轮 `recovery_plan`。
- `normal_execution/recovery_attempt -> success_or_failure`：任务完成、恢复稳定、达到最大尝试次数或触发安全终止。

### 13.2 最大恢复次数与退出条件

第一版需要设置最大恢复次数 `max_recovery_attempts`。达到该次数后，即使 Reasoning Mode 仍能生成 plan，也停止任务并记录为失败，避免机器人在不稳定状态下无限尝试。

恢复 attempt 的成功判据可以先采用保守定义：连续若干个 action chunk 未触发 `need_recovery=true`，且视觉上物体姿态稳定、机器人仍保持有效夹持，则认为当前恢复有效。若最终完成转移并放置，则整条轨迹记为成功。

### 13.3 Recovery Attempt 中的归位动作

第一版不设置独立 fallback 策略。检测到失败后，系统只完成失败诊断、写入 memory 和 reasoning，不在 reasoning 前执行低级归位动作。

放回物体和回到起始位属于下一次 recovery attempt 的动作前缀，由 VLA Execution Mode 的 action expert 生成。这样不需要额外训练或手写 `return_done` 判据，也避免低级线性插值在机械臂仍抓着物体时执行不可控的任务级归位。



## 14. 初步实验设置

### 14.1 任务

先专注于偏心物体的抓取-转移。目标不是直接预测物体质心，而是让系统通过触觉反馈发现不稳定接触，并经过多步调整找到更稳定的抓取方式。

每次实验包含以下阶段：

1. 机器人从固定或随机初始姿态接近目标物体。
2. 执行抓取并抬升物体。
3. 将物体短距离转移到目标区域。
4. 若出现局部旋转或倾斜，则触发失败诊断和 reasoning。
5. 下一次 recovery attempt 先放回物体并回起始位，再按 `recovery_plan` 重新抓取。
6. 恢复成功后继续转移，恢复失败则进入下一轮恢复或终止。

第一版物体集合应覆盖不同偏心方向，例如左偏、右偏、前偏、后偏，以及少量近似居中的对照物体。训练集和测试集需要划分不同物体实例，避免只记住特定物体外观。

### 14.2 消融实验

为了证明触觉反馈的必要性，后续实验比较：

1. Vision-only VLA：只使用图像、状态和指令。
2. Vision + tactile caption VLA：加入由 `rotation_state` 模板生成的 tactile caption。
3. Vision + raw tactile VLA：直接加入原始触觉形变序列或触觉特征。

重点比较不稳定检测时间、恢复触发是否更早、恢复尝试次数和最终转移成功率。

### 14.3 评价指标

主实验使用以下指标：

- `task_success_rate`：最终完成抓取-转移-放置的比例。
- `recovery_success_rate`：触发恢复后最终重新稳定并完成任务的比例。
- `mean_recovery_attempts`：成功任务中平均恢复尝试次数。
- `first_detection_time`：从抓取或抬升开始到首次触发 `need_recovery=true` 的时间。
- `false_recovery_rate`：稳定样本中错误触发恢复的比例。
- `repeated_failed_plan_rate`：在已有 memory 情况下仍重复选择失败恢复方向的比例。

### 14.4 第一版验收标准

方案进入代码实现前，需要满足以下验收条件：

1. 任取一条采集轨迹，可以按 chunk 对齐图像、状态、动作和触觉序列。
2. 任取一个 chunk，可以生成固定形状的 `mesh_motion` 和 `force` 张量。
3. 任取一个失败 chunk，可以给出 `rotation_state`、tactile caption、`need_recovery` 和 `failure_reason`。
4. 任取一次多轮恢复，可以写出完整 failure-recovery memory。
5. 三个 baseline 的输入输出定义清楚，并能用同一测试集比较。

## 15. 数据采集

第一版的数据使用一个20cm左右的非透明空心长方体，通过在内部的不同位置放置重物来改变重心。第一版计划在0，5，10，15，20cm这几个位置选一个放置。
因为机械臂第一次尝试固定去抓中间位置，每次调整大致移动5cm。这样理论上3次attempt内可以实现稳定抓取。

任务定义是把未知重心的长条物体抓取后移动到盘子里。

通用采集规则：

- 采集前命令行只设置 `case_id`、本次预期旋转方向 `rotation_direction` 和本次前后左右倾斜方向 `tilt_direction`。
- `case_id` 取 `case1/case2/case3`。
- `rotation_direction` 取 `none/clockwise/counterclockwise`，方向定义仍以左夹爪坐标系为准。
- `tilt_direction` 取 `none/left/right/front/back`，用于离线生成或校验 `failure_reason`。
- 采集过程中只额外记录 shift 节点时间戳，也就是观察到明显旋转后约 `0.5s` 的标记点。
- 采集程序不在线设置 `failure_reason`、`recovery_plan`、`input_recovery_plan`、`failure_recovery_memory`。

通用离线标注规则：

- `rotation_state` 根据 `rotation_direction` 和 shift 节点生成：节点前为 `none`，节点后到 attempt 结束为采集前设置的旋转方向；无旋转的成功 attempt 全部为 `none`。
- `need_recovery` 第一版可以用同一个 shift 节点初始化：节点前为 `false`，节点后为 `true`；稳定成功片段为 `false`。
- `failure_reason`、`recovery_plan`、`input_recovery_plan` 和 `failure_recovery_memory` 都在离线阶段根据视频、触觉回放、`tilt_direction` 和 attempt 关系填写。
- Execution Mode 第一版不输入 `failure_recovery_memory`，执行样本中该字段置空。
- 每次失败 attempt 后进入 Reasoning Mode 时，都先把当前失败 attempt 追加到 `failure_recovery_memory`，再把该 memory 作为 Reasoning Mode 输入。第一次 attempt 失败时追加 `{attempt_id: 1, recovery_plan: "initial grasp", failure_reason: <attempt1 failure_reason>}`。
- 恢复成功后的稳定抬升和转移片段要保留下来，作为偏心物体上的 hard negative：`rotation_state=none` 或无明显旋转，`need_recovery=false`。

### case1:重心在中间

横着竖着各采20条（2*20）

一次attempt：
成功抓取。每帧使用完整默认标注：

```json
{
  "rotation_state": "none",
  "need_recovery": false,
  "failure_reason": "",
  "recovery_plan": "",
  "input_recovery_plan": "",
  "failure_recovery_memory": []
}
```

### case2:重心在5和15cm处

四个方向的滑动都要采集，每个方向采集40条。（4*40）

第一次attempt：
采集前命令行设置 `case_id=case2`、本次 `rotation_direction` 和 `tilt_direction`。
在发生旋转0.5s左右打上记号（shift），在旋转停止前终止采集。
离线标注 `failure_reason` 和本轮 Reasoning Mode 的目标 `recovery_plan`。构造 Reasoning Mode 样本时，`failure_recovery_memory` 写为：

```json
[
  {
    "attempt_id": 1,
    "recovery_plan": "initial grasp",
    "failure_reason": "<attempt1 的 failure_reason>"
  }
]
```

第二次attempt：
采集前命令行设置 `case_id=case2`、`rotation_direction=none` 和 `tilt_direction=none`。
从第一次 attempt 失败结束时的状态继续采集。操作者按预定恢复动作先放回物体、回到起始位，再重新抓取并完成任务；采集程序不需要在线写入 `input_recovery_plan`。
离线标注时，将第一次 attempt 失败后对应的 `recovery_plan` 写入第二次 attempt 的 `input_recovery_plan`；第二次 attempt 是成功执行片段，`failure_recovery_memory=[]`。
任务完成前的稳定执行片段保留为 hard negative，用于训练 VLA 不要在恢复成功后继续误触发 recovery。

### case3:重心在0和20cm处。  

四个方向的滑动都要采集，每个方向采集80条。（4*80）

第一次attempt：
采集前命令行设置 `case_id=case3`、本次 `rotation_direction` 和 `tilt_direction`。
在发生旋转0.5s左右打上记号（shift），在旋转停止前终止采集。
离线标注 `failure_reason` 和本轮 Reasoning Mode 的目标 `recovery_plan`。构造 Reasoning Mode 样本时，`failure_recovery_memory` 写为：

```json
[
  {
    "attempt_id": 1,
    "recovery_plan": "initial grasp",
    "failure_reason": "<attempt1 的 failure_reason>"
  }
]
```

第二次attempt：
采集前命令行设置 `case_id=case3`、本次 `rotation_direction` 和 `tilt_direction`。
从第一次 attempt 失败结束时的状态继续采集。操作者按第一次 attempt 失败后预定的恢复动作先放回物体、回到起始位，再重新抓取；采集程序不需要在线写入 `input_recovery_plan`。
在发生旋转0.5s左右打上记号（shift），在旋转停止前终止采集。
离线标注时，将第一次 attempt 失败后对应的 `recovery_plan` 写入第二次 attempt 的 `input_recovery_plan`；第二次 attempt 的 Execution Mode 样本中 `failure_recovery_memory=[]`。同时标注第二次 attempt 的 `failure_reason` 和本轮 Reasoning Mode 的目标 `recovery_plan`。
第二次 attempt 失败后，离线生成用于第三次 Reasoning Mode 的 `failure_recovery_memory`，其中包含第一次失败记录和第二次失败记录。第二次失败记录中，`recovery_plan` 使用第二次 attempt 的 `input_recovery_plan`，`failure_reason` 使用第二次 attempt 输出的 failure reason。

第三次attempt：
采集前命令行设置 `case_id=case3`、`rotation_direction=none` 和 `tilt_direction=none`。
从第二次 attempt 失败结束时的状态继续采集。操作者按第二次 attempt 失败后预定的恢复动作先放回物体、回到起始位，再重新抓取并完成任务；第三次 attempt 是成功恢复 attempt。
离线标注时，将第二次 attempt 失败后对应的 `recovery_plan` 写入第三次 attempt 的 `input_recovery_plan`。第三次 attempt 一定成功，不再需要 memory，执行样本中 `failure_recovery_memory=[]`。

任务完成前的稳定执行片段保留为 hard negative，用于训练 VLA 区分“偏心物体稳定接触”和“仍需恢复”的状态。



Tactile Captioner 的模型、训练脚本和推理封装放在项目级 `src/tactile_vla/` 包中，`data_tools` 只负责离线构建 HDF5 shard 和 index。训练时使用 `train_balanced.npz`，输入只包含 `mesh_motion` 和 `force`，不使用 `force_resultant`：

```python
from tactile_vla.data import TactileCaptionerDataset

ds = TactileCaptionerDataset(
    "/path/with/enough_space/tactile_captioner_data",
    split="train",
    balanced=True,
)
```

推荐训练入口：

```bash
python scripts/train_tactile_captioner.py \
  --dataset-dir data/tactile_captioner_data \
  --run-name tcn_v1_balanced \
  --batch-size 128 \
  --epochs 50 \
  --device cuda
```
