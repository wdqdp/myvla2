# 方案 v4：旋转恢复专项训练与逐步扩展

## 1. V3 问题总结

V3 的总数据量虽然较大，但同时覆盖了多种扰动、多个方向和多种恢复策略。数据被细分到具体的“扰动类型 × 方向 × 恢复距离”后，每个组合中的有效示教数量很少。
V4 不再追求一次学会所有扰动恢复，而是先把旋转恢复做稳定，再按扰动类型逐步扩展。

## 2. V4 核心目标

V4 第一阶段只学习旋转扰动恢复，目标是让模型稳定掌握：

1. 判断当前是否发生需要恢复的旋转；
2. 区分 `right / left / front / back` 四个旋转恢复方向；
3. 在同一方向上区分“轻微”和“正常”两种不同的水平移动距离；
4. 从旋转判断、失败原因、恢复计划到动作执行形成完整闭环；
5. 在学会恢复的同时，保持一次性成功抓取和搬运能力。

V4 的核心原则是：先增加单个能力单元内的数据密度，再增加任务类型。

## 3. 第一阶段的训练范围

### 3.1 本阶段包含

- 无扰动、一次性成功的抓取搬运示教；
- `right / left / front / back` 四个方向的旋转失败 attempt；
- 旋转后使用“轻微”水平移动成功恢复的示教；
- 旋转后使用“正常”水平移动成功恢复的示教；
- Stage A 动作学习和 Stage B 的 `need → failure → plan` 结构化推理学习。

### 3.2 本阶段不包含

- 抓空；
- 接触面积太小；
- 垂直恢复；
- 碰撞、滑移等其他扰动；
- 多种扰动同时出现的复合失败。

这些情况待旋转恢复通过验收后，再一类一类地加入，不在 V4 首轮训练中混合。

## 4. 方向与距离标签

### 4.1 两档移动距离

V4 只训练两档水平恢复距离：

| 中文名称 | 结构化标签 | 用途 |
|---|---|---|
| 轻微 | `slightly` | 小距离修正 |
| 正常 | `moderately` | 正常距离修正 |

距离的中文名称和结构化标签均沿用 V3：“轻微”对应 `slightly`，“正常”对应 `moderately`。

### 4.2 方向语义

旋转失败方向固定为：

```text
right / left / front / back
```

恢复计划的水平方向也使用同一组词表，但必须明确区分：

- `failure_reason` 描述观察到的物体旋转方向；
- `recovery_plan` 描述下一次抓取点应该移动的方向；
- 两者的对应关系由采集标定和人工检查确认，不能根据 episode 编号或 Captioner 的顺、逆时针输出直接推导。

V4 仍可在代码中保留 V3 的完整 grammar 以便后续扩展，但首轮训练 target 只允许四个旋转方向和 `slightly / moderately` 两档距离。

## 5. Prompt 设计

### 5.1 Execution prompt

V4 的 Execution prompt 取消触觉描述，同时删除 V3 末尾的自然语言输出指令。固定格式为：

```text
Mode: execution. Task: <instruction> Recovery plan: <plan>.
```

下列内容不得再出现在 Execution prompt 中：

- tactile caption；
- `Output the next robot action chunk and monitor whether recovery is needed.` 或其他同义末尾指令；
- `case_id`、`attempt_id` 等数据标识。

Stage A、Stage B action replay、强制 recovery plan 消融和真机推理必须使用完全相同的 Execution prompt。

### 5.2 Assessment 和 Reasoning prompt

触觉信息仍然用于失败判断和恢复推理，只从动作生成 prompt 中移除。建议使用：

Assessment：

```text
Mode: tactile assessment. Task: <instruction> <tactile_caption> Recovery plan: <plan>.
```

Reasoning：

```text
Mode: reasoning. Task: <instruction> <failed_tactile_caption> Failure-recovery memory: <memory>.
```

结构化 token 流仍保留 `Answer:` 分隔符。训练、服务端和客户端均从 checkpoint metadata 读取 prompt profile，避免手工配置不一致。旧 V3 checkpoint 未声明新 profile 时继续使用 legacy prompt，不得默认切换。

## 6. 训练路线

### 6.1 阶段一：Stage A 旋转动作训练

Stage A 从原始 base policy 初始化，不继承 V3 多扰动恢复微调带来的混合偏置。训练数据由两部分组成：

- 一次性成功的完整动作；
- 旋转恢复单元中的有效动作。

如果 Stage A 训练结束后人工用手动加入prompt的方式验证真机推理

### 6.2 阶段二：Stage B 旋转判断与计划训练


Stage B 从通过验收的 Stage A checkpoint 初始化，保留 V3 的四任务循环：

```text
action → need → failure → plan
```

其中：

- action replay 使用与 Stage A 完全相同的 indices、norm stats 和 Execution prompt；
- `need=true` 正样本只来自旋转失败的 failure-active 帧；
- `need=false` 覆盖失败前帧、一次性成功 attempt 和成功恢复后的稳定帧；
- `failure` 只训练四个旋转方向，并保持 `grasp_position=appropriate`；
- `plan` 只训练四个方向与轻微/正常两档水平移动；
- 使用 action replay 和 action degradation gate 防止语言微调破坏 Stage A 动作。

Stage B 的评估必须同时报告 need 正/负类 support、need macro-F1、四方向 failure exact match、 plan exact match，以及相对 Stage A baseline 的 action degradation。


## 7. 训练前待确定项

在开始 V4 正式采集和训练前，还需固定以下配置：

- 旋转失败的起始时刻、结束时刻和 `need=true` 窗口标注规则
沿用V3方法
- Stage A/Stage B 的训练 step、评估间隔和 early-stop 条件；
沿用v3


上述数值应在正式训练配置中版本化，不在脚本内使用未记录的默认值。

## 8. V4 正式训练命令

### 8.1 训练前检查

先完成 V4 数据 pipeline，然后生成统一 training index、need 样本和 norm stats，并运行 Stage A/Stage B batch dry-run：

```bash
cd /home/test/qxh/workspace/myvla2

./scripts/run_v4_training_pipeline.sh --execute
```

该脚本只生成训练前产物并执行 dry-run，不会启动正式训练。确认以下产物存在后再运行正式命令：

```text
/data1/outputs/vla/rotation_v4/v4_training_index.json
/data1/outputs/vla/rotation_v4/splits.json
/data1/outputs/vla/rotation_v4/reasoning_manifests/
/data1/outputs/vla/assets/tactile_vla_rotation_v4/norm_stats.json
```

### 8.2 Stage A 正式训练

Stage A 只从原始 `pi05_base` 初始化：

```bash
cd /home/test/qxh/workspace/myvla2/openpi

USE_TF=0 \
PYTHONWARNINGS='ignore:The video decoding and encoding capabilities of torchvision are deprecated:UserWarning' \
uv run python ../scripts/train_vla_stage_a_openpi.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_rotation_v4 \
  --data-profile rotation_v4 \
  --prompt-profile minimal_v1 \
  --index-file /data1/outputs/vla/rotation_v4/v4_training_index.json \
  --split-file /data1/outputs/vla/rotation_v4/splits.json \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --output-dir /data1/outputs/vla/stage_a_action \
  --run-name pi05_delta_tac_rotation_v4 \
  --checkpoint /home/test/.cache/modelscope/hub/models/hairuoliu/pi05_base/params \
  --batch-size 8 \
  --num-workers 0 \
  --num-steps 15000 \
  --action-horizon 30 \
  --action-dim 32 \
  --state-history-len 60 \
  --state-history-dim 7 \
  --history-hidden-dim 256 \
  --use-state-history \
  --paligemma-variant gemma_2b_lora \
  --action-expert-variant gemma_300m_lora \
  --train-lora-only \
  --seed 42
```

训练完成后 Stage B 固定使用：

```text
/data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_v4/15000
```

如需从同一运行目录恢复 Stage A，使用完全相同的命令并在末尾增加 `--resume`；不得更换 profile、index、norm stats、base checkpoint 或训练协议参数。

### 8.3 Stage B 正式训练

```bash
cd /home/test/qxh/workspace/myvla2/openpi

USE_TF=0 \
uv run python ../scripts/train_vla_stage_b_v3.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_rotation_v4 \
  --data-profile rotation_v4 \
  --prompt-profile minimal_v1 \
  --grammar-profile v3_full_v1 \
  --index-file /data1/outputs/vla/rotation_v4/v4_training_index.json \
  --split-file /data1/outputs/vla/rotation_v4/splits.json \
  --reasoning-manifest-dir /data1/outputs/vla/rotation_v4/reasoning_manifests \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --stage-a-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_v4/15000 \
  --output-dir /data1/outputs/vla/stage_b_v4 \
  --run-name pi05_stage_b_rotation_v4 \
  --batch-size 8 \
  --num-workers 0 \
  --num-steps 4000 \
  --lr 1e-4 \
  --eval-interval 500 \
  --save-interval 1000 \
  --keep-period 1000 \
  --action-horizon 30 \
  --action-dim 32 \
  --state-history-len 60 \
  --state-history-dim 7 \
  --state-history-fps 30 \
  --history-hidden-dim 256 \
  --use-state-history \
  --reasoning-window-frames 15 \
  --status-negative-ratio 3.0 \
  --paligemma-variant gemma_2b_lora \
  --action-expert-variant gemma_300m_lora \
  --action-loss-weight 1.0 \
  --need-loss-weight 1.0 \
  --failure-loss-weight 1.0 \
  --plan-loss-weight 1.0 \
  --action-loss-degradation-limit 0.10 \
  --seed 42
```

Stage B 按 `action → need → failure → plan` 循环训练 4000 step，每 500 step 评估。只有 action degradation 不超过 10% 的评估点才可写入：

```text
/data1/outputs/vla/stage_b_v4/pi05_stage_b_rotation_v4/best/
```

如需恢复 Stage B，使用完全相同的命令并增加 `--resume`。

### 8.4 合并 Stage B best checkpoint

Stage B 保存的是 delta checkpoint，部署前必须与 Stage B 训练时 `--stage-a-checkpoint` 记录的同一个 Stage A checkpoint 合并。当前选择 step 15000，因此使用：

```bash
cd /home/test/qxh/workspace/myvla2/openpi

USE_TF=0 python ../scripts/merge_v3_stage_b_delta.py \
  --stage-a-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_v4/15000 \
  --stage-b-delta /data1/outputs/vla/stage_b_v4/pi05_stage_b_rotation_v4/best \
  --output /data1/outputs/vla/stage_b_v4/pi05_stage_b_rotation_v4/merged_best
```

合并脚本会严格检查 Stage A 来源、V4 artifact identity、norm stats SHA256，以及 `best/metrics.json` 中的 10% action degradation gate。已存在合并目录时不会默认覆盖；只在明确需要重新生成时增加 `--overwrite`。

## 9. V4 服务与真机推理启动

### 9.1 部署前 warm-up

完整闭环只能使用 Stage B 的 `merged_best`，不能直接使用 `best/delta_params`。先执行一次只 warm-up 不启动端口的检查：

```bash
cd /home/test/qxh/workspace/myvla2/openpi

USE_TF=0 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
uv run python ../scripts/serve_tactile_vla_policy_v3.py \
  --checkpoint /data1/outputs/vla/stage_b_v4/pi05_stage_b_rotation_v4/merged_best \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --output-action-dim 7 \
  --num-inference-steps 10 \
  --need-recovery-threshold 0.5 \
  --dry-run
```

warm-up 会检查 `rotation_v4`、`minimal_v1`、Stage A 实际 step、V4 artifact identity、norm stats SHA256 和 best metrics，并实际跑通 action、assessment、failure 以及包含 4 对 memory 的 reasoning。输出中应看到 `actions_shape: [30, 7]`、`state_history_shape: [60, 7]` 和 `reasoning_memory_pairs: 4`。

### 9.2 启动 V4 完整 Stage B server

```bash
cd /home/test/qxh/workspace/myvla2/openpi

USE_TF=0 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
uv run python ../scripts/serve_tactile_vla_policy_v3.py \
  --checkpoint /data1/outputs/vla/stage_b_v4/pi05_stage_b_rotation_v4/merged_best \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --host 0.0.0.0 \
  --port 8000 
```

server 会通过 metadata 向客户端声明 `data_profile=rotation_v4`、`prompt_profile=minimal_v1`、H30 action、60×7 state history、最多 4 对 memory 和最多 5 次 attempt。

### 9.3 启动完整真机闭环客户端

先确认 Piper ROS、两路相机和两个触觉节点已经正常启动。在另一个 ROS 终端中运行：

```bash
source /opt/ros/noetic/setup.bash
source /home/test/qxh/workspace/tac_ws/devel/setup.bash
cd /home/test/qxh/workspace/myvla2/openpi

USE_TF=0 \
LD_PRELOAD=/lib/x86_64-linux-gnu/libffi.so.7 \
python inference/agilex/inference/agilex_inference_tactile_vla_sync_single.py \
  --host localhost \
  --port 8000 \
  --expected-data-profile rotation_v4 \
  --instruction "Pick up and transfer the object stably." \
  --captioner_checkpoint /data1/outputs/tactile_captioner/tcn_v3_w30_rotation_head/best.pt \
  --captioner_device auto \
  --tactile_window_size 30 \
  --chunk_size 30 \
  --publish_rate 30 \
  --observation-poll-rate 200 \
  --state-history-len 60 \
  --state-history-fps 30 \
  --recovery-need-ignore-seconds 10.0 \
  --max_attempts 5 \
  --max_publish_step 10000
```

server 和客户端不在同一台机器时，把 `--host localhost` 改为 server 的 IP。`LD_PRELOAD` 只用于解决当前 ROS Python 环境的 `libffi` 冲突；如果该终端不存在此错误可去掉。客户端会从 metadata 自动使用 `minimal_v1`，并用 `--expected-data-profile rotation_v4` 防止误连旧 server。运行时 `s` 表示人工确认成功，`q` 表示安全停止。

### 9.4 Stage A / Stage B action-only 强制恢复消融

测试 Stage A 时启动：

```bash
cd /home/test/qxh/workspace/myvla2/openpi

USE_TF=0 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
uv run python ../scripts/serve_tactile_vla_action_ablation.py \
  --checkpoint-kind stage-a \
  --checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_v4/15000 \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --host 0.0.0.0 \
  --port 8000 \
  --num-inference-steps 10
```

测试合并后 Stage B 的 action expert 时启动：

```bash
cd /home/test/qxh/workspace/myvla2/openpi

USE_TF=0 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
uv run python ../scripts/serve_tactile_vla_action_ablation.py \
  --checkpoint-kind stage-b \
  --checkpoint /data1/outputs/vla/stage_b_v4/pi05_stage_b_rotation_v4/merged_best \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --host 0.0.0.0 \
  --port 8000 \
  --num-inference-steps 10
```

两个消融 server 都使用下面的客户端。`--rotation-direction` 可选 `right` / `left` / `front` / `back`，它会自动生成对应的 moderately failure/plan。A/B 对比必须使用相同方向和相同 `--noise-seed`：

```bash
source /opt/ros/noetic/setup.bash
source /home/test/qxh/workspace/tac_ws/devel/setup.bash
cd /home/test/qxh/workspace/myvla2/openpi

USE_TF=0 \
LD_PRELOAD=/lib/x86_64-linux-gnu/libffi.so.7 \
python inference/agilex/inference/agilex_inference_forced_recovery_ablation.py \
  --host localhost \
  --port 8000 \
  --expected-data-profile rotation_v4 \
  --rotation-direction right \
  --noise-seed 42 \
  --instruction "Pick up and transfer the object stably." \
  --captioner_checkpoint /data1/outputs/tactile_captioner/tcn_v3_w30_rotation_head/best.pt \
  --tactile_window_size 30 \
  --chunk_size 30 \
  --publish_rate 30 \
  --state-history-len 60 \
  --state-history-fps 30 \
  --max_publish_step 10000
```

正常阶段按空格后会丢弃未执行的 NORMAL chunk，重置 history，并进入强制 RECOVERY；`s` 确认成功，`q` 安全停止。`--rotation-direction` 便捷参数只生成 moderately plan。如需强制 slightly 距离，不传该参数，改为显式传入：

```bash
--forced-failure-reason "failure_reason=rotate right,grasp appropriate." \
--forced-recovery-plan "recovery_plan=move horizontally right slightly, move vertically none moderately."
```

Stage A checkpoint 的 step 不再被 server 固定为 10000。如果实际训练选用的不是 15000，应把本节 Stage A 路径替换为实际的数字 step 目录；Stage B 和 `merged_best` 会继续严格检查它与训练/合并配置中记录的 step 完全一致。
