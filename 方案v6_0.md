# V6.1：移除 Action Expert H60 history 的 V5.2 重训练

## 1. 目标与实验边界

V6.1 是一个单变量消融实验：保留 V5.2 的数据、阶段 prompt、H30 target
和训练超参，只移除 Action Expert 中新增的 `60×7` qpos history 编码器。

V6.1 Action Expert 仍然使用 π0.5 原有的当前帧 qpos 离散状态输入，不是删除所有
本体状态。关闭 history 后：

- 模型不创建 `history_gru` / `history_norm` / `history_proj`；
- loader 不再从 LeRobot 读取 H60 窗口；
- 训练和推理不再向 Action Expert 提供 `state_history` 及 mask；
- checkpoint 明确记录 `stage_a_protocol=v6_1_no_state_history`、
  `use_state_history=false`、`state_history_len=0` 和 `history_hidden_dim=0`。

本次不改动 V5.3 `adjustment_end` 分类器的 H30 qpos 特征，也不改夹爪尺度、
delta action、loss 权重、prompt 文本或 `adjustment_end` 切换阈值。

## 2. 数据和训练不变量

V6.1 必须直接使用当前服务器上的 V5.2 adjustment 标签产物：

```text
/data1/qxh/tac_vla/outputs/vla/rotation_v5_adjustment_v2/
```

训练 index 固定为：

```text
/data1/qxh/tac_vla/outputs/vla/rotation_v5_adjustment_v2/
v5_prompt_training_index.json
```

已核验该 index 的关键身份：

```text
schema_version=tactile_vla_v5_adjustment_training_index_v2
data_profile=rotation_phase_v5_adjustment_v2
prompt_profile=phase_v2
experiment_kind=phase_prompt_h30_terminal_hold
action_horizon=30
training_data_hash=18724d2fc58a0e78f96f85b54313b672b093cc8f13d04387f484393514ca1b6f
```

该服务器产物是 V5.2.1 实际训练时使用的旧 v2 版，其 `rexecution_frame`
检测器为 `horizontal_endpoint_stable`。当前代码后来将新建 v2 产物升级为
`sustained_descent_10mm`，但没有改 schema 名。V6.1 为了与 V5.2.1 做单变量
对比，仅对上述已知 `training_data_hash` 及其旧 detector 审计字段开启兼容；
其他 v2 index 仍必须携带新 detector identity。

因此 V6.1 继续使用：

- attempt1 全部为 `execution`；
- attempt2 在 `rexecution_frame` 之前为 `adjustment`，从该帧开始为
  `execution`；
- adjustment H30 跨越边界时使用 V5.2 terminal hold；
- V4 数据划分、action start、H30×32 target、V4 norm stats、LoRA 变体、
  15000 steps 和 seed 42。

本次不重新生成 `rexecution_frame`，也不修改上述 index。这使 H60 history
是 V5.2.1 和 V6.1 之间的主要模型差异。

## 3. 代码修改

### 3.1 Stage A 训练协议

`scripts/train_vla_stage_a_openpi.py` 增加独立的
`v6_1_no_state_history` 协议：

- 只允许它与 `rotation_phase_v5_adjustment_v2` 配对；
- 固定 `--no-use-state-history --state-history-len 0
  --history-hidden-dim 0`；
- 其余超参完全继承 V5.2；
- 仍然必须从 raw `pi05_base` 初始化，不允许从 V5.2.1 checkpoint
  续训；
- resume 同时校验数据 identity、协议名和无 history 模型配置。

旧 V4/V5/V5.2 H60 协议保留，旧 checkpoint 的加载行为不变。

### 3.2 Action-only server

`scripts/serve_tactile_vla_action_ablation.py` 按 checkpoint 中的
`use_state_history` 还原模型：

- H60 checkpoint 继续要求 `60×7` history；
- V6.1 checkpoint 允许 `use_state_history=false`；
- V6.1 服务端在 transform 前丢弃客户端为兼容旧控制流程可能仍
  发来的 history 字段；
- dry-run 的 `state_history_shape` 应为 `null`。

### 3.3 V5.2 真机客户端

`agilex_inference_forced_phase_anlation.py` 根据 server metadata 接受两种合法配对：

- 旧 V5.2：`use_state_history=true` 且 history 为 `60×7@30Hz`；
- V6.1：`stage_a_protocol=v6_1_no_state_history` 且
  `use_state_history=false`。

客户端仍可保留 history buffer 作为 trial 审计日志，但 V6.1 服务端不会将其
送入模型。

## 4. 训练前 dry-run

```bash
cd /home/qxh/workspace/myvla2

CUDA_VISIBLE_DEVICES=0,1,2,3 \
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/train_vla_stage_a_openpi.py \
  --dataset-dir /data1/qxh/tac_vla/tac_data/lerobot_data/tactile_vla_rotation_v4 \
  --index-file /data1/qxh/tac_vla/outputs/vla/rotation_v5_adjustment_v2/v5_prompt_training_index.json \
  --norm-stats-dir /data1/qxh/tac_vla/outputs/vla/assets/tactile_vla_rotation_v4 \
  --output-dir /data1/qxh/tac_vla/outputs/vla/stage_a_action \
  --run-name pi05_delta_tac_rotation_phase_v6_1_no_history \
  --data-profile rotation_phase_v5_adjustment_v2 \
  --prompt-profile phase_v2 \
  --experiment-kind phase_prompt_h30_terminal_hold \
  --checkpoint /home/qxh/.cache/openpi/openpi-assets/checkpoints/pi05_base/params \
  --batch-size 8 \
  --num-steps 15000 \
  --action-horizon 30 \
  --action-dim 32 \
  --no-use-state-history \
  --state-history-len 0 \
  --state-history-dim 7 \
  --history-hidden-dim 0 \
  --paligemma-variant gemma_2b_lora \
  --action-expert-variant gemma_300m_lora \
  --train-lora-only \
  --fsdp-devices 4 \
  --seed 42 \
  --dry-run
```

dry-run 必须满足：

- batch 有当前 `state` 和 `actions=[8,30,32]`；
- batch 不得出现 `state_history` / `state_history_mask`；
- 能分别打印 `execution` 和 `adjustment` 的 `phase_v2` prompt；
- index、terminal-hold target identity 和 V4 norm SHA 校验通过。

## 5. 正式训练命令

```bash
cd /home/qxh/workspace/myvla2

CUDA_VISIBLE_DEVICES=0,1,2,3 \
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/train_vla_stage_a_openpi.py \
  --dataset-dir /data1/qxh/tac_vla/tac_data/lerobot_data/tactile_vla_rotation_v4 \
  --index-file /data1/qxh/tac_vla/outputs/vla/rotation_v5_adjustment_v2/v5_prompt_training_index.json \
  --norm-stats-dir /data1/qxh/tac_vla/outputs/vla/assets/tactile_vla_rotation_v4 \
  --output-dir /data1/qxh/tac_vla/outputs/vla/stage_a_action \
  --run-name pi05_delta_tac_rotation_phase_v6_1_no_history \
  --data-profile rotation_phase_v5_adjustment_v2 \
  --prompt-profile phase_v2 \
  --experiment-kind phase_prompt_h30_terminal_hold \
  --checkpoint /home/qxh/.cache/openpi/openpi-assets/checkpoints/pi05_base/params \
  --batch-size 8 \
  --num-steps 15000 \
  --action-horizon 30 \
  --action-dim 32 \
  --no-use-state-history \
  --state-history-len 0 \
  --state-history-dim 7 \
  --history-hidden-dim 0 \
  --paligemma-variant gemma_2b_lora \
  --action-expert-variant gemma_300m_lora \
  --train-lora-only \
  --fsdp-devices 4 \
  --seed 42
```

输出位置：

```text
/data1/qxh/tac_vla/outputs/vla/stage_a_action/
pi05_delta_tac_rotation_phase_v6_1_no_history/15000
```

如果该 run 正常中断，使用完全相同的命令并在末尾增加 `--resume`。
不得使用 V5.2.1 的 run 目录或 checkpoint 作为 resume 源。

## 6. Action-only 推理验收

先执行 server dry-run：

```bash
cd /home/qxh/workspace/myvla2

CUDA_VISIBLE_DEVICES=0,1,2,3 \
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/serve_tactile_vla_action_ablation.py \
  --checkpoint-kind stage-a \
  --checkpoint /data1/qxh/tac_vla/outputs/vla/stage_a_action/pi05_delta_tac_rotation_phase_v6_1_no_history/15000 \
  --norm-stats-dir /data1/qxh/tac_vla/outputs/vla/assets/tactile_vla_rotation_v4 \
  --num-inference-steps 10 \
  --dry-run
```

验收输出必须包含：

```text
actions_shape=[30,7]
raw_model_actions_shape=[30,32]
state_history_shape=null
```

然后去掉 `--dry-run`，增加 `--host 0.0.0.0 --port 8000` 启动长驻
server。真机客户端仍使用 V5.2 的两阶段按键流程，并固定：

```text
--expected-data-profile rotation_phase_v5_adjustment_v2
```

server metadata 必须同时报告：

```text
stage_a_protocol=v6_1_no_state_history
use_state_history=false
```

## 7. 结果对比

V6.1 应与 V5.2.1 在同一批固定 trial 上比较：

- attempt1 下降期间夹爪是否保持张开；
- attempt2 切回 execution 后，闭合前的下降距离和最低高度；
- 闭合首帧相对 `rexecution_frame` / execution 起点的时延；
- 恢复成功率和 one-shot success 保持率。

该消融只能回答 H60 history 是否影响夹爪时序。如果 V6.1 仍在 attempt2
过早闭合，下一步应优先重新审计 `slightly_lift` 的 `rexecution_frame`，而不能把
结果直接归因于夹爪尺度。
