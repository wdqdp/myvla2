# V7：原生 rexecution 边界的无历史两阶段 VLA

## 1. 目标

使用 `black_box` 数据从原始 `pi05_base` 重新训练 V5.2 形式的两阶段 action 模型：

```text
attempt1：execution
attempt2 [0, rexecution_frame)：adjustment
attempt2 [rexecution_frame, end)：execution
```

本版“两阶段”指 `adjustment/execution` 两种 prompt phase，仅训练 Stage A action 模型。

## 2. 与 V5.2 的区别

- `rexecution_frame` 直接读取：

  ```python
  v4_index["attempt_timing"][attempt_key]["rexecution_frame_index"]
  ```

- 不再通过规则、URDF、phase boundaries 或 override 推导边界。
- 保留 V5.2 的 `phase_v2` prompt 和跨边界 H30 terminal-hold。
- 去掉 H60 state history，只向 pi05 Action Expert 提供当前状态。
- 必须从原始 `pi05_base` 初始化，只训练 LoRA。

版本身份：

```text
data_profile=rotation_phase_v7_adjustment
prompt_profile=phase_v2
experiment_kind=phase_prompt_h30_terminal_hold_native_reexecution
stage_a_protocol=v7_no_state_history
```

## 3. 数据构建

构建器：

```text
scripts/prepare_v7_adjustment_index.py
```

输出目录：

```text
/data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_adjustment/
├── action_phase_manifest.jsonl
└── v7_prompt_training_index.json
```

需要重新生成时执行：

```bash
openpi/.venv/bin/python scripts/prepare_v7_adjustment_index.py --overwrite
```

当前验收结果：

- 210 个 episode、390 个 attempt，其中 180 个 attempt2 均有原生边界；
- action-start 共 210,381 个：execution 163,414，adjustment 46,967；
- 5,220 个跨边界 H30 chunk 使用 terminal-hold，共隔离 78,300 个边界后 action step；
- 沿用 V4 norm stats，SHA256 为 `f7d71f15a9b23b6097958cb718c473ade8545803fe6ea2f58d7c716d76acad8f`。

## 4. 正式训练命令

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 \
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/train_vla_stage_a_openpi.py \
  --dataset-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/lerobot_data/tactile_vla_rotation_v4 \
  --index-file /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_adjustment/v7_prompt_training_index.json \
  --norm-stats-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats \
  --output-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action \
  --run-name pi05_delta_tac_rotation_phase_v7_no_history \
  --data-profile rotation_phase_v7_adjustment \
  --prompt-profile phase_v2 \
  --experiment-kind phase_prompt_h30_terminal_hold_native_reexecution \
  --checkpoint /home/qxh/.cache/openpi/openpi-assets/checkpoints/pi05_base/params \
  --num-steps 15000 \
  --no-use-state-history \
  --state-history-len 0 \
  --state-history-dim 7 \
  --history-hidden-dim 0 \
  --train-lora-only \
  --fsdp-devices 2 \
  --seed 42
```

正常完成后的最终 checkpoint 为：

```text
/data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action/
pi05_delta_tac_rotation_phase_v7_no_history/15000
```

同一训练任务中断后，可以在相同命令末尾增加 `--resume`；不要从 V5/V5.2 checkpoint 续训。

## 5. adjustment_end 多任务训练

分类边界直接使用 V4 index 中的原生 `rexecution_frame_index`，不使用 phase boundaries。仅 attempt2 参与分类：

- 有效区间：`frame <= R`；
- 正样本：`R-10 <= frame <= R`；
- 共47,147个样本，其中正样本1,980个；
- Action Expert 不使用 H60，但分类 prompt 仍保留独立的 `qpos_h30`。

构建数据：

```bash
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/build_v7_adjustment_end_data.py
```

正式训练：

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 \
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/train_vla_adjustment_end_multitask_v7.py \
  --dataset-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/lerobot_data/tactile_vla_rotation_v4 \
  --action-index-file /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_adjustment/v7_prompt_training_index.json \
  --index-file /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_adjustment_end_r10_r0/adjustment_end_training_index.json \
  --norm-stats-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats \
  --stage-a-checkpoint /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action/pi05_delta_tac_rotation_phase_v7_no_history/15000 \
  --output-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/adjustment_end_v7 \
  --run-name pi05_adjustment_end_rotation_v7_multitask_r10_r0_no_history \
  --data-profile rotation_phase_v7_adjustment_end_r10_r0 \
  --prompt-profile phase_change_v1 \
  --experiment-kind adjustment_end_action_multitask_v7_no_history \
  --batch-size 8 \
  --num-steps 8000 \
  --eval-interval 1000 \
  --save-interval 1000 \
  --action-horizon 30 \
  --action-dim 32 \
  --no-use-state-history \
  --state-history-len 0 \
  --state-history-dim 7 \
  --history-hidden-dim 0 \
  --paligemma-variant gemma_2b_lora \
  --action-expert-variant gemma_300m_lora \
  --fsdp-devices 2 \
  --seed 42
```

8000步目录中同时保存 `delta_params/` 和 `full_params/`。默认部署使用自包含模型参数的 `full_params/`；`delta_params/` 用于继续对比和复现。LoRA 保留模块结构，不转换为 dense 权重。

## 6. 部署

原始 Captioner 复制到服务器后先计算 SHA256，然后启动服务：

```bash
CUDA_VISIBLE_DEVICES=0 \
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/serve_tactile_vla_v7.py \
  --adjustment-end-checkpoint /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/adjustment_end_v7/pi05_adjustment_end_rotation_v7_multitask_r10_r0_no_history/8000 \
  --norm-stats-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats \
  --captioner-checkpoint-sha256 <CAPTIONER_SHA256> \
  --checkpoint-load-mode full
```

单臂同步和异步客户端分别为：

```text
openpi/inference/agilex/inference/agilex_inference_forced_phase_anlation_v7.py
openpi/inference/agilex/inference/agilex_inference_forced_phase_anlation_v7_asyn.py
```

两者都不采集或发送 H60；异步 adjustment_end 请求频率不超过7 Hz。



- server：
USE_TF=0 openpi/.venv/bin/python \
    scripts/serve_tactile_vla_action_ablation.py \
    --checkpoint-kind stage-a \
    --checkpoint /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/pi05_delta_tac_rotation_phase_v7_no_history/15000 \
    --norm-stats-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats \
    --num-inference-steps 10 \
    --host 0.0.0.0 \
    --port 8001

- inference

USE_TF=0 \
LD_PRELOAD=/lib/x86_64-linux-gnu/libffi.so.7 \
python inference/agilex/inference/agilex_inference_forced_phase_anlation.py \
  --host localhost \
  --port 8000 \
  --expected-data-profile rotation_phase_v7_adjustment \
  --instruction "Pick up and transfer the object stably." \
  --rotation-direction right \
  --rotation-magnitude moderately \
  --noise-seed 42 \
  --no-captioner \
  --chunk_size 30 \
  --publish_rate 30 \
  --observation-poll-rate 200 \
  --history-freeze-delay-seconds 1.0 \
  --state-history-len 60 \
  --state-history-fps 30 \
  --state-history-max-gap-seconds 0.02 \
  --gripper-min 0.024 \
  --max_publish_step 10000


  重放：
   查看可用 episode：

  cd /home/test/qxh/workspace/myvla2/openpi

  USE_TF=0 \
  LD_PRELOAD=/lib/x86_64-linux-gnu/libffi.so.7 \
  python inference/agilex/inference/agilex_inference_episode_replay_v7.py \
    --list-eligible \
    --noise-seed 42

训练idx：
[31, 33, 34, 36, 37, 38, 39, 41, 42, 43, 44, 45, 46, 48, 49, 50, 51, 52, 53, 54, 55, 56, 58, 59, 60, 61, 62, 63, 65, 66, 67, 68, 71, 72, 73, 74, 76, 77, 78, 79, 80, 82, 83, 84, 85, 87, 88, 89, 92, 93, 95, 96, 98, 99, 100, 102, 103, 104, 105, 106, 107, 108, 109, 110]


  真实机械臂发布示例：

  USE_TF=0 \
  LD_PRELOAD=/lib/x86_64-linux-gnu/libffi.so.7 \
  python inference/agilex/inference/agilex_inference_episode_replay_v7.py \
    --episode-id 50 \
    --host localhost \
    --port 8000 \
    --noise-seed 42 \
    --execute \
    --gripper-min 0.024 \
    --publish-rate 30