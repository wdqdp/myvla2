# V7：原生 rexecution 边界的无历史两阶段 VLA

## 1. 目标

使用 `black_box` 数据从原始 `pi05_base` 重新训练 V5.2 形式的两阶段 action 模型：

```text
attempt1：execution
attempt2 [0, rexecution_frame)：adjustment
attempt2 [rexecution_frame, end)：execution
```

本版“两阶段”指 `adjustment/execution` 两种 prompt phase，仅训练 Stage A action 模型。

## 2. 与 V5.2 的区别

- `rexecution_frame` 直接读取：

  ```python
  v4_index["attempt_timing"][attempt_key]["rexecution_frame_index"]
  ```

- 不再通过规则、URDF、phase boundaries 或 override 推导边界。
- 保留 V5.2 的 `phase_v2` prompt 和跨边界 H30 terminal-hold。
- 去掉 H60 state history，只向 pi05 Action Expert 提供当前状态。
- 必须从原始 `pi05_base` 初始化，只训练 LoRA。

版本身份：

```text
data_profile=rotation_phase_v7_adjustment
prompt_profile=phase_v2
experiment_kind=phase_prompt_h30_terminal_hold_native_reexecution
stage_a_protocol=v7_no_state_history
```

## 3. 数据构建

构建器：

```text
scripts/prepare_v7_adjustment_index.py
```

输出目录：

```text
/data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_adjustment/
├── action_phase_manifest.jsonl
└── v7_prompt_training_index.json
```

需要重新生成时执行：

```bash
openpi/.venv/bin/python scripts/prepare_v7_adjustment_index.py --overwrite
```

当前验收结果：

- 210 个 episode、390 个 attempt，其中 180 个 attempt2 均有原生边界；
- action-start 共 210,381 个：execution 163,414，adjustment 46,967；
- 5,220 个跨边界 H30 chunk 使用 terminal-hold，共隔离 78,300 个边界后 action step；
- 沿用 V4 norm stats，SHA256 为 `f7d71f15a9b23b6097958cb718c473ade8545803fe6ea2f58d7c716d76acad8f`。

## 4. 正式训练命令

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 \
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/train_vla_stage_a_openpi.py \
  --dataset-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/lerobot_data/tactile_vla_rotation_v4 \
  --index-file /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_adjustment/v7_prompt_training_index.json \
  --norm-stats-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats \
  --output-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action \
  --run-name pi05_delta_tac_rotation_phase_v7_no_history \
  --data-profile rotation_phase_v7_adjustment \
  --prompt-profile phase_v2 \
  --experiment-kind phase_prompt_h30_terminal_hold_native_reexecution \
  --checkpoint /home/qxh/.cache/openpi/openpi-assets/checkpoints/pi05_base/params \
  --num-steps 15000 \
  --no-use-state-history \
  --state-history-len 0 \
  --state-history-dim 7 \
  --history-hidden-dim 0 \
  --train-lora-only \
  --fsdp-devices 2 \
  --seed 42
```

正常完成后的最终 checkpoint 为：

```text
/data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action/
pi05_delta_tac_rotation_phase_v7_no_history/15000
```

同一训练任务中断后，可以在相同命令末尾增加 `--resume`；不要从 V5/V5.2 checkpoint 续训。
