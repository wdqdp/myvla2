# V8.3

V8.3 以 V8.2 的 G/A/S/C 边界、静止帧过滤和 V7.4 H30 拼接为基础，只处理
`small_grasp` 的短下降监督。

## 短下降时间归一化

满足以下全部条件的 attempt2 使用插值 action target：

- task 为 `small_grasp`；
- adjustment prompt 的 recovery plan 为 `move vertically down moderately`；
- 下降长度 `S-A+1 < 20`；
- `S < R`，不允许 target 进入 rexecution 阶段。

对每个此类 attempt，重建起点 `p ∈ [G-9,G]` 的十个 H30：

```text
target = action[p...G] + Resample(action[A...S], 30 - (G-p+1))
```

`Resample` 在时间轴上对原始 7D action 轨迹线性插值，首尾严格等于真实 A、S
动作。因此每条强化 H30 有 20–29 帧连续下降；不包含 `(G,A)` 或 `(S,C)` 静止帧，
也不使用 terminal hold。若这些起点此前因 V8.2 的 post-stop 限制被过滤，V8.3
重新启用它们，因为新 target 在 S 结束。

其余任务、长下降 (`S-A+1 >= 20`) 和非聚焦起点均保留 V8.2 原始监督。

生成索引：

```bash
openpi/.venv/bin/python scripts/prepare_v8_3_adjustment_index.py
```

输出：

```text
/data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v8_3_adjustment/v8_3_prompt_training_index.json
```

## Stage-A 训练

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 \
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/train_vla_stage_a_openpi.py \
  --dataset-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/lerobot_data/tactile_vla_rotation_v4 \
  --index-file /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v8_3_adjustment/v8_3_prompt_training_index.json \
  --norm-stats-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats \
  --output-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action \
  --run-name pi05_delta_tac_rotation_phase_v8_3_no_history \
  --data-profile rotation_phase_v8_3_adjustment \
  --prompt-profile phase_v2 \
  --experiment-kind phase_prompt_h30_short_small_grasp_descent_time_warp \
  --checkpoint /home/qxh/.cache/openpi/openpi-assets/checkpoints/pi05_base/params \
  --batch-size 8 \
  --num-steps 15000 \
  --action-horizon 30 \
  --action-dim 32 \
  --no-use-state-history \
  --state-history-len 0 \
  --state-history-dim 7 \
  --history-hidden-dim 0 \
  --paligemma-variant gemma_2b_lora \
  --action-expert-variant gemma_300m_lora \
  --train-lora-only \
  --fsdp-devices 2 \
  --seed 42
```
