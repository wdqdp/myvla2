# V2 方案：H30 本体状态记忆 VLA

## 1. 版本定位

V2 是在 V1 真机闭环方案上增加“近期本体状态记忆”的版本，当前对应 Git 分支：

```text
feature/v2-state-memory-h30
```

核心配置：

- 基础模型：OpenPI π0.5（pi05）。
- action chunk：30 步。
- action 发布频率：30 Hz，一个 chunk 约为 1 秒。
- 当前本体状态：puppet qpos `[7]`。
- 历史本体状态：最近 60 帧 puppet qpos `[60,7]`。
- 历史时间网格：30 Hz，覆盖 `(60-1)/30=1.967` 秒。
- 历史编码器：masked GRU，`7 -> 256 -> 1024`。
- Stage A 训练目标：只使用 flow-matching action loss，不加辅助进度标签。
- Stage B：保留 V1 的 `need_recovery` / `failure_reason` / `recovery_plan` 三个分类头。

V2 的直接目标是缓解 V1 的动作进度歧义：当前单帧图像和 qpos 很相似时，模型可以通过过去约 2 秒的 qpos 变化判断机械臂刚刚在上升、下降、横移、静止还是重新夹取，从而减少 recovery 轨迹回到前缀的概率。

## 2. 整体数据流

```text
raw topic 数据
  ↓ 同步与转换
HDF5：每帧保存 puppet qpos、master action、图像、触觉和 timestamp
  ↓
LeRobot：observation.state 仍为单帧 [7]，不在数据文件中重复存储 60 帧窗口
  ↓ 训练 Dataset 使用 negative delta_timestamps 查询
current_state [7] + state_history [60,7] + state_history_mask [60]
  ↓
π0.5 prefix：图像 + 当前 state + prompt
π0.5 action expert：noisy action + flow timestep + history condition
  ↓
30 步 delta action
  ↓ server 反归一化并加回当前 puppet qpos
30 步、7 维 absolute joint target
```

这种做法保持 raw HDF5 和 LeRobot 数据格式通用；“历史窗口长度”是模型训练配置，不是数据集存储格式。

## 3. 输入与 prompt

### 3.1 Execution Mode

Stage A 和 Stage B status 分支使用 Execution prompt：

```text
Mode: execution.
Task: <instruction>
Tactile: <no rotation / clockwise rotation / counterclockwise rotation>.
Recovery plan: <none / Regrasp left/right/front/back.>.
Case: <case_id>.
Attempt: <attempt_id>.
Output the next robot action chunk and monitor whether recovery is needed.
```

输入包含：

- front image、left wrist image；
- 当前 puppet qpos `[7]`；
- 历史 puppet qpos `[60,7]` 及 mask `[60]`；
- instruction、tactile caption、`input_recovery_plan`、case 和 attempt。
/res

### 3.2 Reasoning Mode

Stage B recovery-plan 分支使用 Reasoning prompt：

```text
Mode: reasoning.
Task: <instruction>
Tactile: <failed tactile caption>.
Failure-recovery memory: <previous failed attempts>.
Case: <case_id>.
Failed attempt: <attempt_id>.
Choose the next recovery plan.
```

`failure_recovery_memory` 是运行时显式维护并每次重新发送的文本历史，例如：

```json
[
  {"attempt_id": 1, "recovery_plan": "initial grasp", "failure_reason": "tilted left"},
  {"attempt_id": 2, "recovery_plan": "Regrasp left.", "failure_reason": "tilted front"}
]
```

它只用于 Reasoning Mode 选择下一个 recovery plan，不输入 Stage A Execution action，也不是模型隐状态。

V2 的 `state_history` 和 `failure_recovery_memory` 是两种不同的记忆：

- `state_history`：稠密、连续、约 2 秒，直接影响 action expert。
- `failure_recovery_memory`：跨 attempt 的稀疏文本摘要，只影响 recovery-plan 分类。

## 4. 离线历史状态构造

LeRobot 中每帧仍只保存当前 `observation.state=[7]`。Dataset 加载样本 `t` 时配置：

```python
delta_timestamps["observation.state"] = [-59/30, -58/30, ..., -1/30, 0]
```

得到：

```text
current_state       = state_history[-1]       # [7]
state_history                                # [60,7]
state_history_mask                           # [60]
```

约束：

- 历史不跨 LeRobot episode，即不跨 attempt。
- attempt 开头不足 60 帧时，LeRobot 重复最早帧补齐。
- 补齐位置的 mask 为 false，masked GRU 不用这些伪历史更新 hidden state。
- `state_history` 和当前 state 都是 puppet qpos，复用 norm stats 中 `state` 的 quantile 统计。
- 动作监督仍为 `master[t:t+30] - puppet[t]`，前 7 维有效，再 pad 到 action dim 32。

## 5. 历史编码与注入方式

V2 在 `Pi0` 中新增：

```text
state_history [B,60,7]
       ↓ masked GRUCell（7 -> 256）
last valid hidden [B,256]
       ↓ LayerNorm
       ↓ Linear（256 -> 1024，小幅随机初始化）
history_cond [B,1024]
```

π0.5 action expert 原来用 flow timestep 生成 `time_cond`。V2 改为：

```text
adarms_cond = time_cond + history_cond
```

然后用 AdaRMSNorm 条件化 action expert。该设计的特点：

- 历史不增加视觉/文本 prefix token，不改变 VLM 的 token 长度。
- 历史直接条件化生成动作的 action expert。
- 同一 chunk 的 10 次 flow-matching 积分只计算一次 `history_cond`。
- Stage A 不增加 phase/progress 分类头，历史编码器只通过 action loss 学习。
- Stage B 使用 prefix-only feature，因此当前三个辅助头不直接使用 `history_cond`。

Stage A 采用 LoRA 训练：原有 backbone 按 pi05 LoRA freeze filter 训练，新增的 `history_gru` / `history_norm` / `history_proj` 保持可训练。基础 pi05 checkpoint 不含 `history_*` 参数，weight loader 只允许这些新参数和 LoRA 参数缺失，其他结构仍严格校验。

## 6. Stage A 与 Stage B

### 6.1 Stage A：动作生成

```text
front + wrist image + current state + 60-frame state history
+ execution prompt
-> 30 x 7 absolute joint targets（server 输出）
```

训练内部预测 30 步 delta action，只优化 flow-matching action loss。

### 6.2 Stage B：恢复判断与计划

Stage B 冻结 Stage A backbone，在 pi05 prefix 的 mask-mean pooled feature 上训练三个 MLP 分类头：

- `need_recovery`：二分类。
- `failure_reason`：`tilted left/right/front/back` 四分类。
- `recovery_plan`：`Regrasp left/right/front/back.` 四分类。

Status 样本使用 Execution prompt，训练前两个头；Reasoning 样本使用 Reasoning prompt，只训练 recovery-plan 头。Reasoning 触发帧扩展到 `t..t+10`，用于覆盖真机检测时刻在失败附近波动的情况。

Stage B 只保存 heads，部署时必须同时加载与之匹配的 Stage A checkpoint。

## 7. 在线历史构造

真机 `/puppet/joint_right` 约为 200 Hz，不能直接取最近 60 条 topic，否则只覆盖约 0.3 秒，与训练的约 2 秒不一致。

V2 在 joint callback 中持续缓存 `(ROS timestamp, qpos)`。对与当前图像同步的 puppet state 时间 `t`，构造：

```text
t-59/30, t-58/30, ..., t-1/30, t
```

然后为每个目标时间选择 200 Hz 缓冲中时间距离最近的 qpos。实际实现是基于时间戳的最近邻重采样，不是对 qpos 数值做线性插值，也不是固定每 6 条消息取一条。

具体约束：

- 在线缓冲保留约 2.47 秒。
- 最近 qpos 距目标时间超过 20 ms 时，对应 mask 置 false。
- 最后一帧强制使用与图像同步的当前 puppet state，mask 为 true。
- 每个新 attempt 开始时清空历史，不把上一次失败的稠密 qpos 带入下一 attempt。
- `--state-history-fps 30` 是模型历史时间网格；`--publish_rate 30` 是 action 发布频率，两者相互独立。

## 8. 在线恢复流程

```text
获取同步图像、当前 qpos、约 2 秒 qpos 历史和触觉 caption
  ↓ Execution 请求
actions[30,7] + need_recovery + failure_reason
  ├─ need_recovery=false：以 30 Hz 开环发布完整 30 步，然后重新观察
  └─ need_recovery=true：记录失败 memory，停止当前 attempt
                         ↓ Reasoning 请求
                     选择下一 recovery plan
                         ↓
                     开始新 attempt
```

当前第一版保留完整 30 步开环执行，不做 chunk overlap 或 temporal ensemble。

对 attempt 2 及之后的归位阶段，前 `recovery_tactile_ignore_seconds` 强制使用 `Tactile: no rotation.`，防止归位旋转被误判为夹取失败。该时间按已发布 action 步数换算，不是按程序墙钟时间计算。

## 9. Expanded 数据与当前产物

当前正式 V2 使用扩展数据：

```text
dataset: /data1/tac_data/lerobot_data/tactile_vla_expanded
split:   /data1/outputs/vla/indices/splits_h30_state_memory_expanded.json
index:   /data1/outputs/vla/indices/vla_indices_h30_state_memory_expanded.json
norm:    /data1/outputs/vla/assets/tactile_vla_h30_state_memory_expanded
Stage A: /data1/outputs/vla/stage_a_action/pi05_delta_tac_h30_state_memory_expanded/15000
Stage B: /data1/outputs/vla/stage_b_heads/pi05_heads_jax_h30_state_memory_expanded/best/head_params.npz
```

数据概况：

- 320 个原始 episode，680 个 attempt，395614 帧。
- 按原始 episode 分成 train/val/test = 256/32/32，同一原始 episode 下的 attempt 不跨 split。
- H30 execution 样本：train/val/test = 303282/38916/33696。
- Stage B reasoning 样本：train/val/test = 3223/429/308。
- val/test 的 recovery-plan 四个方向都有样本。

当前 Stage A 配置为 15000 steps、batch 8、LoRA + 可训练 history encoder。Stage B 已使用该 15000-step backbone 训练 2 epochs，best 验证集上三个分支的 macro-F1 均为 1.0。该结果反映离线标签可分，不等价于真机恢复成功率。

## 10. 复现命令

命令均在下列目录执行：

```bash
cd /home/test/qxh/workspace/myvla2/openpi
```

### 10.1 重建 index/split 与 norm stats

```bash
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/prepare_vla_data.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_expanded \
  --base-split-file /data1/outputs/vla/indices/splits_h30_state_memory.json \
  --split-file /data1/outputs/vla/indices/splits_h30_state_memory_expanded.json \
  --index-file /data1/outputs/vla/indices/vla_indices_h30_state_memory_expanded.json \
  --output-dir /data1/outputs/vla/indices \
  --reasoning-augment-after-frames 10 \
  --action-horizon 30

UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/compute_vla_norm_stats.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_expanded \
  --split-file /data1/outputs/vla/indices/splits_h30_state_memory_expanded.json \
  --output-dir /data1/outputs/vla/assets/tactile_vla_h30_state_memory_expanded \
  --action-horizon 30
```

### 10.2 Stage A

```bash
CUDA_VISIBLE_DEVICES=0 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/train_vla_stage_a_openpi.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_expanded \
  --split-file /data1/outputs/vla/indices/splits_h30_state_memory_expanded.json \
  --index-file /data1/outputs/vla/indices/vla_indices_h30_state_memory_expanded.json \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_h30_state_memory_expanded \
  --output-dir /data1/outputs/vla/stage_a_action \
  --run-name pi05_delta_tac_h30_state_memory_expanded \
  --checkpoint /home/test/.cache/modelscope/hub/models/hairuoliu/pi05_base/params \
  --action-horizon 30 \
  --num-steps 15000 \
  --batch-size 8 \
  --num-workers 2 \
  --overwrite
```

已有完整 checkpoint 后续训同一 run 用 `--resume`；不要同时使用 `--overwrite`。

### 10.3 Stage B

```bash
CUDA_VISIBLE_DEVICES=0 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/train_vla_stage_b_heads.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_expanded \
  --split-file /data1/outputs/vla/indices/splits_h30_state_memory_expanded.json \
  --index-file /data1/outputs/vla/indices/vla_indices_h30_state_memory_expanded.json \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_h30_state_memory_expanded \
  --backbone-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_h30_state_memory_expanded/15000 \
  --output-dir /data1/outputs/vla/stage_b_heads \
  --run-name pi05_heads_jax_h30_state_memory_expanded \
  --action-horizon 30 \
  --batch-size 8 \
  --num-workers 2 \
  --epochs 2
```

### 10.4 Server

```bash
CUDA_VISIBLE_DEVICES=4 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache uv run python \
  ../scripts/serve_tactile_vla_policy.py \
  --stage-a-checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_h30_state_memory_expanded/15000 \
  --stage-b-heads /data1/outputs/vla/stage_b_heads/pi05_heads_jax_h30_state_memory_expanded/best/head_params.npz \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_h30_state_memory_expanded \
  --state-history-fps 30 \
  --port 8000
```

首次检查可在命令末尾加 `--dry-run`，应看到 `actions_shape=[30,7]` 且 metadata 中 `use_state_history=true`。

### 10.5 真机 client

```bash
cd /home/test/qxh/workspace/myvla2/openpi

LD_PRELOAD=/lib/x86_64-linux-gnu/libffi.so.7 \
python inference/agilex/inference/agilex_inference_tactile_vla_sync_single.py \
  --host localhost \
  --port 8000 \
  --instruction "Pick up and transfer the object stably." \
  --captioner_checkpoint /data1/outputs/tactile_captioner/tcn_v1_balanced/best.pt \
  --state-history-fps 30 \
  --recovery-tactile-ignore-seconds 3.0 \
  --max_publish_step 10000 \
  --chunk_size 30
```

`LD_PRELOAD` 用于解决当前 conda inference 环境与 ROS `cv_bridge` 的 libffi 冲突。只做观测链路检查时添加 `--no-publish --max_publish_step 5`。

## 11. V2 已完成的工程修改

- OpenPI `Observation` 、`Pi0Config` 和 `Pi0` 支持 `state_history` 及 mask。
- Dataset 按 30 Hz 动态查询 60 帧历史，不改变 LeRobot 存储格式。
- state history 使用当前 state 的 norm stats 独立归一化。
- Stage A/B 训练、checkpoint 加载和 server 模型配置支持 history encoder。
- server 校验 checkpoint 中是否真实存在 `history_*` 参数，避免静默按 V1 模型加载。
- client/server 会校验 history 长度、维度和 fps 是否一致。
- 真机端支持 200 Hz ROS qpos 缓存到 30 Hz 模型网格的基于时间戳重采样。
- 非第一次 attempt 的归位前几秒可强制忽略触觉旋转。
- 增加 raw episode 复制、重编号和校验脚本，并对 expanded 数据重建 split/index/norm。
- state-history padding/mask、在线重采样、异常时间间隔、归一化、freeze filter 和 checkpoint loader 都有对应测试。

