# 方案 V5 第一阶段：验证阶段 Prompt 有效性

## 1. 目标和实验边界

V5 第一阶段只回答一个问题：在观测、history、H30 target、采样分布和训练协议均不改变时，把旋转恢复过程拆成三个 action prompt，是否能降低动作语义歧义并改善真机恢复。

固定身份如下：

```text
data_profile=rotation_phase_v5
prompt_profile=phase_v1
experiment_kind=phase_prompt_only
```

本轮只训练 Stage A，不训练 Stage B，不增加或实现阶段判别头，也不实现自动阶段状态机。只有离线 prompt 有效性和真机配对验收均通过后，才进入自动阶段判别头开发。

数据选择完全复用 V4：

- 直接读取 V4 `selection.json`，selection hash 固定为 `7047235cd323f2af9a3adb0c3a72663d20ea73179b0c185c37b884c7d88ce711`；
- 仅包含 `one_success`、`moderate_lift` 和 `slightly_lift`；
- 沿用 V4 对 slightly 样本中“调整后仍同方向旋转”的排除结果；
- 固定 448 个 episode、776 个 attempt，以及 V4 原 train/val/test episode 划分；
- 不根据 V5 标签重新选择或划分 episode；
- 明确不加入 `no_grasp`、`small_grasp`、抓空、面积过小、垂直恢复或其他非旋转扰动。

V5 不重建 LeRobot，不改变 V4 parquet。V4 的 execution indices、H30 target 数据源、60×7 history、norm stats 和 action sampler 顺序都必须逐项保持一致。

## 2. 阶段定义

逐帧阶段固定为：

```text
execution  = 正常抓取、抬升和搬运
reposition = 放下并松开物体，使物体回到原位置
adjustment = 按 recovery plan 调整并重新抓取
```

所有已有 attempt1 均标记为 `execution`。attempt2 使用 raw HDF5 中的夹爪和左右触觉 Fz 自动检测两个边界。

## 3. Raw/LeRobot 严格对齐

标注器同时读取：

- V4 LeRobot 的 global index、episode/attempt/frame identity、`ros_timestamp`、caption 和 instruction；
- raw HDF5 的 `arm/jointStatePosition/puppetRight[:,6]`；
- raw HDF5 的 `arm/jointStatePosition/masterRight[:,6]`；
- raw HDF5 的 `tactile/force_resultant/left[:,2]`；
- raw HDF5 的 `tactile/force_resultant/right[:,2]`；
- raw HDF5 的 `timestamp`。

每个 attempt 必须满足：profile、LeRobot 和 raw 的帧数完全相同，LeRobot `frame_index` 精确覆盖 `0..T-1`，timestamp 在 `1e-9` 绝对误差内逐帧一致。缺少任一 Fz/夹爪/timestamp 数据集、shape 不合法或出现非有限值时立即拒绝。

哈希只覆盖实际参与标签的五条 raw stream，并同时记录真实 HDF5 dataset name、shape、dtype 和绝对路径；不会把未用于标签的大体积图像/mesh 数据混入标签源身份。

## 4. 自适应接触力和拒绝规则

每个 attempt 独立处理，不使用固定 `0.095` 夹爪阈值或固定牛顿阈值。

1. 分别用 puppet/master 夹爪的 P5/P95 计算 `[0,1]` 相对张开进度。
2. 对两条相对进度取平均，并精确选择最张开的 10% 帧。
3. 用这些帧左右 Fz 的 median 估计 `left_baseline/right_baseline`。
4. 计算：

   ```text
   contact_force = abs(Fz_left-left_baseline) + abs(Fz_right-right_baseline)
   ```

5. 在无接触样本上计算 median、MAD 和 `noise_sigma=1.4826*MAD`：

   ```text
   force_off_threshold = noise_median + 6*noise_sigma
   contact_reference   = attempt contact_force 的 Q75
   separation_score    = (contact_reference-force_off_threshold)/noise_sigma
   force_on_threshold  = force_off_threshold
                         + max(0.10*(contact_reference-force_off_threshold),
                               6*noise_sigma)
   ```

`contact_reference <= force_off_threshold`、`separation_score < 6` 或 on threshold 不再留有稳定接触区间时，自动标签必须拒绝，不能回退到夹爪距离。左右受力不均仍可由合力规则识别；Fz 常量偏置由左右独立 baseline 消除。

## 5. 边界检测

attempt2 起始 30 帧内必须出现至少 15 帧的稳定接触窗口。

松开边界要求：

- `contact_force < force_off_threshold` 至少连续 10 帧；
- 窗口起点前 30 帧内，puppet 或 master 的相对张开进度至少增加 0.10；
- `release_frame` 是稳定 force-off 窗口首帧。

重新抓取边界要求：

- release 后 `contact_force > force_on_threshold` 至少连续 15 帧；
- 接触窗口起点前 30 帧内，puppet 或 master 的相对闭合进度至少增加 0.10；
- `regrasp_frame` 是 15 帧确认窗口末帧，而不是窗口首帧。

最多 2 帧的孤立碰撞尖峰会在 force-off run 中去抖；它不会制造第二个松开边界。瞬时失力不能满足 10 帧 force-off，瞬时碰撞也不能满足 15 帧 force-on，因此均不能触发阶段切换。

Captioner 的 `area` 只记录为 release/regrasp 附近的辅助置信度，不参与硬判定。

标签闭区间固定为：

```text
[0, release_frame]             -> reposition
(release_frame, regrasp_frame] -> adjustment
(regrasp_frame, end]           -> execution
```

找不到边界、初始接触不稳定、分布不可分、趋势不足、边界逆序或其他低置信情况一律拒绝。人工修正写入版本化 `phase_boundary_overrides.json`，且只能修正 attempt2。`release_frame` 和 `regrasp_frame` 兼容两种输入：整数表示 attempt 内从 0 开始的局部帧号；带小数的数值表示人工标注的大致 raw timestamp，构建脚本会映射到该 attempt 中时间最接近的帧。整数若超出帧范围也按 timestamp 解释，因而允许填写整秒 Unix 时间。时间戳必须位于该 attempt 的时间范围内，且解析后的 release 必须早于 regrasp。`reason` 和 `reviewer` 为可选审计信息，建议正式数据保留。

人工时间标注示例（两个时间不能相同）：

```json
{
  "schema_version": "tactile_vla_v5_phase_boundary_overrides_v1",
  "version": 1,
  "overrides": [
    {
      "episode_id": 51,
      "attempt_id": 2,
      "release_frame": 1785658883.897981,
      "regrasp_frame": 1785658888.231314
    }
  ]
}
```

`phase_boundaries.jsonl` 会保留输入时间，同时记录 `resolved_release_frame`、`resolved_regrasp_frame`、匹配到的 raw timestamp 和时间误差。

截至 2026-08-25，以下 17 个自动检测拒绝的 attempt2 已完成人工审核，并写入仓库内 v1 override 文件：

```text
51/2, 62/2, 63/2, 69/2, 70/2, 84/2,
117/2, 118/2, 120/2, 136/2, 171/2, 289/2,
341/2, 381/2, 473/2, 474/2, 476/2
```

主要拒绝原因是 force-off/force-on 时，夹爪的主要开合动作已经落在严格的前 30 帧窗口之外。正式产物已校验为 448 个 episode、776 个 attempt，边界来源为 368 个 attempt1 execution、391 个自动标签和 17 个 override；`no_grasp=0`、`small_grasp=0`。

## 6. Sidecar 和 V5 index

默认输出目录：

```text
/data1/outputs/vla/rotation_v5/
├── phase_boundaries.jsonl
├── action_phase_manifest.jsonl
├── phase_boundary_overrides.json
├── phase_label_summary.json
└── v5_prompt_training_index.json
```

`phase_boundaries.jsonl` 每个 V4 attempt 一行，记录 raw stream hash、自动阈值、边界、检测诊断、caption 辅助信息和 override provenance。

`action_phase_manifest.jsonl` 与 V4 execution indices 一一对应，记录 global/frame identity、phase 和 `chunk_phase_pure`。`chunk_phase_pure=false` 只用于将跨阶段 H30 从主要离线 prompt 评估中分离；训练时不删样本、不截断 target、不做 terminal hold。

`v5_prompt_training_index.json` 必须校验并绑定：

- V4 selection/profile/split/index 的路径和 SHA256；
- 448 episode、776 attempt 和三个允许任务；
- V4/V5 execution indices 的 count、顺序和 SHA256 完全相同；
- V4 parquet identity、global/frame identity 和 H30 source identity 完全相同；
- V4 norm `norm_stats.json` SHA256 完全相同；
- raw 标签源、boundary、override 和 action phase manifest 的完整哈希；
- `no_grasp=0`、`small_grasp=0`。

## 7. Prompt-only Stage A

`phase_v1` 的三个 action prompt 都不包含触觉描述：

```text
Mode: execution. Task: <instruction>.
Mode: reposition. Task: <instruction>. Description: Put the object back to its original position.
Mode: adjustment. Task: <instruction>. Recovery plan: <plan>.
```

构造器统一处理句号，instruction 已有句号时不会产生重复标点。

Stage A loader 按 global index 查询 action phase sidecar，只替换 prompt：

- 不阶段均衡采样；
- 不复制、增删或重排 V4 action 样本；
- 不修改 H30 target；
- 不处理跨阶段 H30；
- history 仍为 60×7@30Hz；
- 继续读取 `/data1/outputs/vla/assets/tactile_vla_rotation_v4` 的 norm stats。

训练协议固定为：

```text
batch size=8
steps=15000
lr=5e-5
final lr=5e-7
transition steps=7000
save interval=1000
keep period=5000
H30
action dim=32
history=60x7@30Hz
LoRA only=true
seed=42
base checkpoint=raw pi05_base
```

任何 profile、prompt、experiment kind、seed、checkpoint 或协议偏差都拒绝训练和 resume。dry-run 会分别打印 execution/reposition/adjustment 的真实 prompt 与 `chunk_phase_pure`。

## 8. 人工阶段消融推理

只新增并修改：

```text
openpi/inference/agilex/inference/agilex_inference_forced_phase_anlation.py
```

原 `agilex_inference_forced_recovery_ablation.py` 行为保持不变。action-only server 仅增加 V5 Stage A artifact/profile 校验，V4 serving 行为不变。

固定按键：

```text
1 = 选择 execution
2 = 选择 reposition
3 = 选择 adjustment
s = 使用已选择阶段推理并执行下一 chunk
SPACE = 停止当前 execution chunk，开始新 attempt，重置 history，进入 reposition
q = 停止
```

运行语义：

- 初始阶段为 execution；
- 每个完整 chunk 的最后一个 action 发布后，继续采集 1 s 的关节反馈，再锁定 history；
- 人工暂停期间只锁定 history，不锁定图像和当前 qpos；
- 暂停期间 `1/2/3` 只更新下一阶段选择，不恢复 history、不请求模型、不执行动作；
- 按 `s` 后等待一组时间晚于按键时刻的实时图像/qpos，与已锁定 history 组合后发起下一次请求；
- reposition→adjustment→execution 切换不重置 history；
- SPACE 只在 execution 有效，并保留原强制恢复的“丢弃旧 chunk、新 attempt、单帧 history”语义；
- 暂停和选阶段期间的 callbacks 不得刷新模型 history；按 `s` 后 history 才重新锚定并恢复，当前请求使用锁定的 history 和实时 observation；
- websocket 请求的 `mode` 始终是 `execution`，阶段只通过 prompt 表达；
- V4 `minimal_v1` 下 reposition 和 adjustment 使用相同旧 recovery prompt；
- V5 `phase_v1` 下使用三个独立 prompt；
- 支持 right/left/front/back 和 moderately/slightly；
- action noise 由 `(noise_seed, phase, phase_chunk_index)` 唯一确定，相同三元组在 V4/V5 间完全一致。

## 9. 离线反事实评估

对同一 val observation、60×7 history、H30 target 和 folded noise，分别输入正确 phase prompt 和另外两个错误 prompt。主要报告只使用 `chunk_phase_pure=true`；跨阶段 H30 单独计数和报告，但不改变训练数据。

每行结果包含：

```text
true_phase
phase_losses={execution,reposition,adjustment}
v4_loss
chunk_phase_pure
```

验收聚合器固定检查：

- execution/reposition/adjustment 三个阶段的平均 `min(wrong loss)-correct loss > 0`；
- 每个阶段正确 prompt 胜率 `>=60%`；
- 正确 prompt 总体 action loss 相对 V4 退化 `<=10%`；
- 三个阶段必须都有非零 pure-chunk support。

## 10. 真机配对验收

旋转恢复配对测试：

```text
4 个方向 × {moderately, slightly} × 每条件 5 次
```

one-success 对照各 10 次。V4/V5 使用相同条件、人工 phase、chunk index 和 noise seed，记录：

- 成功率；
- 反复动作 chunk 数；
- 人工纠错次数；
- 各阶段耗时。

只有同时满足以下条件，才继续实现自动阶段判别头：

1. 离线三个阶段的 prompt margin 和胜率全部通过；
2. 正确 prompt 相对 V4 action loss 退化不超过 10%；
3. 真机成功率提高至少 10 个百分点，或反复动作减少至少 30%；
4. one-success 成功率下降不超过 10 个百分点。

## 11. 执行顺序

数据产物和 Stage A 训练已完成。完整的复现命令为：

```bash
cd /home/test/qxh/workspace/myvla2

PYTHONPATH=src /home/test/anaconda3/envs/openpi/bin/python \
  scripts/prepare_v5_phase_prompt_index.py \
  --overrides-file configs/rotation_v5/phase_boundary_overrides.json

PYTHONPATH=src /home/test/anaconda3/envs/openpi/bin/python \
  scripts/train_vla_stage_a_openpi.py \
  --dataset-dir /data1/tac_data/lerobot_data/tactile_vla_rotation_v4 \
  --index-file /data1/outputs/vla/rotation_v5/v5_prompt_training_index.json \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --data-profile rotation_phase_v5 \
  --prompt-profile phase_v1 \
  --experiment-kind phase_prompt_only \
  --run-name pi05_delta_tac_rotation_phase_v5 \
  --num-steps 15000 \
  --seed 42
```

当前最终 checkpoint 为：

```text
/data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_phase_v5/15000
```

其 `config.json` 已核对为 `rotation_phase_v5 + phase_v1 + phase_prompt_only`，action 为 H30×32，history 为 60×7，seed 为 42。V5 只推理 Stage A，不得把 `--checkpoint-kind` 改成 `stage-b`，norm stats 仍指向 V4 目录。

### 11.1 推理代码链路

真机人工阶段消融由两个进程组成：

```text
scripts/serve_tactile_vla_action_ablation.py
    -> 加载 V5 Stage A、V4 norm stats，返回 H30 action

openpi/inference/agilex/inference/agilex_inference_forced_phase_anlation.py
    -> 采集真机 observation/history，人工选 phase，发送 prompt 并执行 chunk
```

客户端连接后会先校验 server metadata。只有同时满足以下条件才会执行：

- `action_only_ablation=true`；
- `data_profile=rotation_phase_v5`；
- `prompt_profile=phase_v1`；
- action noise 形状为 30×32，输出 action 形状为 30×7；
- history 为 60×7@30 Hz。

所有 websocket 请求的 `mode` 仍固定为 `execution`。Server 不读取独立 phase 字段，三个阶段的唯一模型输入差异是 prompt。

V5 训练 `config.json` 包含 405,105 条逐 action phase lookup，文件约 178 MB。这些训练期 sidecar 不是推理输入，action-only server 只在 websocket metadata 中发送 profile、形状、训练步数和关键 identity hash 等精简摘要，不会把整个 lookup 发给客户端或复制进 trial 日志。

### 11.2 终端 A：先验证 checkpoint，再启动 server

在可见 NVIDIA GPU 的训练/推理环境中先运行一次 dry-run：

```bash
cd /home/test/qxh/workspace/myvla2/openpi

USE_TF=0 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
uv run python ../scripts/serve_tactile_vla_action_ablation.py \
  --checkpoint-kind stage-a \
  --checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_phase_v5/15000 \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --num-inference-steps 10 \
  --dry-run
```

warm-up 必须输出以下形状后才能连真机：

```text
raw_model_actions_shape=[30, 32]
actions_shape=[30, 7]
action_noise_shape=[30, 32]
state_history_shape=[60, 7]
```

验证通过后去掉 `--dry-run` 启动长驻 server：

```bash
cd /home/test/qxh/workspace/myvla2/openpi

USE_TF=0 \
UV_CACHE_DIR=/home/test/qxh/workspace/myvla2/.uv-cache \
uv run python ../scripts/serve_tactile_vla_action_ablation.py \
  --checkpoint-kind stage-a \
  --checkpoint /data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_phase_v5/15000 \
  --norm-stats-dir /data1/outputs/vla/assets/tactile_vla_rotation_v4 \
  --host 0.0.0.0 \
  --port 8000 \
  --num-inference-steps 10
```

首次 JAX warm-up 完成且终端出现 `Serving action-only ablation` 后，再启动客户端。

### 11.3 终端 B：启动 V5 真机客户端

下面是 `right + moderately` 的一次正式 trial。`--trial-id` 必须每次唯一，已存在的目录会被拒绝：

```bash
source /opt/ros/noetic/setup.bash
source /home/test/qxh/workspace/tac_ws/devel/setup.bash
cd /home/test/qxh/workspace/myvla2/openpi

USE_TF=0 \
LD_PRELOAD=/lib/x86_64-linux-gnu/libffi.so.7 \
python inference/agilex/inference/agilex_inference_forced_phase_anlation.py \
  --host localhost \
  --port 8000 \
  --expected-data-profile rotation_phase_v5 \
  --instruction "Pick up and transfer the object stably." \
  --rotation-direction right \
  --rotation-magnitude moderately \
  --noise-seed 42 \
  --no-captioner \
  --chunk_size 30 \
  --publish_rate 30 \
  --observation-poll-rate 200 \
  --history-freeze-delay-seconds 1.0 \
  --state-history-len 60 \
  --state-history-fps 30 \
  --state-history-max-gap-seconds 0.02 \
  --gripper-min 0.024 \
  --max_publish_step 10000
```

`--gripper-min` 是必填的真机安全参数，单位为米。客户端先从模型输出的第 7 维减去
`--gripper_offset`（默认 `0.001`），再对最终发布值执行
`max(gripper_min, action[6] - gripper_offset)`；因此偏移不会把最终命令重新降到安全下限以下。
每条 `action_publish` 日志都会记录偏移后的原值、下限值以及本帧是否发生截断。

对 raw episode 1--40 的 attempt1 统计时，以稳定 Fz 接触、puppet 夹爪被物体撑在
`0.035--0.045 m` 且夹爪速度不超过 `0.0001 m/frame` 的连续窗口作为稳定抓持段。
episode19 的 Fz 接触分布不可分、也没有可用的抓持平台，拒绝纳入数值统计；其余 39 个
episode 共 11234 个稳定抓持帧中，action 第 7 维的 P5/P25/P50/P75/P95 分别约为
`0.0200/0.0225/0.0242/0.0269/0.0319 m`。因此首轮推荐 `l=0.024 m`，它接近示教中位值，
比按 P5 取 `0.020 m` 更偏向保护物体。该值会截断约 43.6% 的稳定抓持示教帧，所以正式
批量测试前必须先低速做抓稳/打滑检查；若 `0.024 m` 明显夹不稳，再按预先记录的规则降到
`0.022 m`，而不是在同一配对实验中临时改变。V4/V5 配对 trial 必须使用相同的
`--gripper-min` 和 `--gripper_offset`。

V5 prompt 不使用触觉描述，所以主要 prompt-only 试验使用 `--no-captioner`，避免引入无关的 captioner 延迟。如需额外记录触觉 caption，可删除 `--no-captioner` 并增加：

```bash
--captioner_checkpoint /data1/outputs/tactile_captioner/tcn_v3_w30_rotation_head/best.pt \
--captioner_device auto \
--tactile_window_size 30
```

即使启用 captioner，caption 也只进入 trial 日志，不会进入 V5 的三个 action prompt。正式配对中 V4/V5 必须使用相同的 captioner 设置。

### 11.4 真机按键时序

客户端启动后的推荐操作顺序为：

1. 确认环境安全后按 Enter，程序立即以 `execution` 请求并执行第 0 个 chunk，初始 chunk 不需按 `s`。
2. 一个完整 chunk 结束后，程序继续记录 1 s 关节反馈，然后锁定 history 并进入人工暂停。如要继续正常执行，按 `1` 选择 `execution`，再按 `s`；程序会在 `s` 后重新采集实时图像和 qpos。
3. 需开始人工强制恢复时，在 `execution` 执行中或 chunk-end 暂停时按 SPACE。程序会丢弃未执行的 execution action、开始新 attempt、将 history 重置为当前单帧，然后立即以 `reposition` 执行第一个恢复 chunk。
4. `reposition` chunk 结束后会再次暂停。若物体尚未放回并松开，按 `2` 后按 `s` 继续 reposition；已完成时按 `3` 后按 `s` 进入 adjustment。
5. `adjustment` 使用命令行方向和幅度自动生成的 recovery plan。调整和重新抓取尚未完成时用 `3` + `s` 继续；稳定重抓后用 `1` + `s` 返回 execution。
6. `reposition -> adjustment -> execution` 的切换不重置 history。任意暂停期间都可按 `q` 停止；动作执行中按 `q` 也会在下一次按键轮询时安全停止。

关键约束：

- `1/2/3` 只在 chunk-end 暂停时有效，只修改“下一 chunk”的 phase，必须再按 `s` 才会请求和执行。
- SPACE 只在当前 phase 为 `execution` 时有效。
- 暂停和选 phase 期间不刷新 history。`--history-freeze-delay-seconds 1.0` 表示最后一个 action 后先记录 1 s history 再锁定；按 `s` 后程序采集时间晚于按键的实时 observation，用实时图像/qpos 和锁定 history 发起当前 server 请求。
- 不要增加 `--use_actions_interpolation`，否则会改变 30 Hz 的原始 action 时序并破坏 V4/V5 配对。

### 11.5 四方向、幅度和 V4/V5 配对

`--rotation-direction` 支持 `right/left/front/back`，`--rotation-magnitude` 支持 `moderately/slightly`。两个参数会生成合法的 failure reason 和 recovery plan；其中 failure reason 只写入审计日志，不进入 action prompt。

V4/V5 每个配对 trial 必须保持：

- 相同环境条件、方向、幅度、instruction 和 `--noise-seed`；
- 相同的 SPACE 触发时机、phase 切换顺序和每个 phase 的 chunk 数；
- 相同的 `chunk_size=30`、发布频率和 captioner 设置。

noise 由 `(noise_seed, phase, phase_chunk_index)` 唯一决定。因此只有人工 phase 路径和 chunk 数一致时，V4/V5 才使用逐 chunk 相同 noise。对比 V4 时仍使用本节的 forced-phase 客户端，但 server 改为 V4 Stage A checkpoint，客户端改为：

```bash
--expected-data-profile rotation_v4
```

此时 server metadata 会让客户端自动使用 V4 `minimal_v1`；`reposition` 和 `adjustment` 仍共用旧 recovery prompt。不要用原 forced-recovery 客户端与 V5 做这一配对，否则人工停顿和 phase 路径不一致。

one-success 对照中不按 SPACE，始终使用 `1` + `s` 继续 execution。客户端参数校验仍要求提供方向/计划，因此为 V4/V5 配对传入相同的任意固定 `--rotation-direction` 和 `--rotation-magnitude` 即可；它们不会进入 execution prompt。

### 11.6 日志和结果记录

默认 trial 目录为：

```text
/home/test/qxh/workspace/myvla2/outputs/runtime/forced_phase_ablation/<trial-id>/
├── events.jsonl
├── trigger_front.png  # 发生 SPACE 时
└── trigger_left.png   # 发生 SPACE 时
```

`events.jsonl` 会记录 checkpoint/metadata、完整 prompt、phase 选择和切换、history 锁定时间/qpos、按 `s` 后的实时 observation 时间/qpos、noise SHA256、raw/transformed action、发布反馈及推理耗时。两张 trigger 图像在 SPACE 开始新 attempt 时保存；一个 trial 建议只触发一次 SPACE，否则后一次 trigger 图像会覆盖前一次。真机汇总还需按第 10 节另行记录最终成功/失败、反复动作 chunk、人工纠错次数和各阶段耗时。

### 11.7 离线 gate 与真机验收顺序

当前 `scripts/evaluate_v5_phase_prompt_counterfactual.py` 是验收聚合器，输入必须是模型侧已生成的逐样本 loss JSONL：

```bash
cd /home/test/qxh/workspace/myvla2

PYTHONPATH=src /home/test/anaconda3/envs/openpi/bin/python \
  scripts/evaluate_v5_phase_prompt_counterfactual.py \
  --loss-jsonl /data1/outputs/vla/rotation_v5/v5_phase_prompt_val_losses.jsonl \
  --output /data1/outputs/vla/rotation_v5/v5_phase_prompt_counterfactual_summary.json \
  --require-pass
```

必须明确：当前仓库只有上述聚合器，尚没有实现从 V4/V5 checkpoint 生成 `v5_phase_prompt_val_losses.jsonl` 的模型侧 evaluator。因此在该 evaluator 补齐并且离线 gate 通过以前，可用上述真机流程做安全的 exploratory trial，但不能声明 V5 已通过最终验收。

正式顺序仍是：生成离线 loss JSONL → 聚合 gate 通过 → 按第 10 节完成 V4/V5 真机配对。任何 gate 不通过都停留在 prompt 验证阶段，不得开始 Stage B 或阶段判别头开发。
