# 方案 V5：基于阶段判别的旋转恢复训练

## 1. 背景与目标

V4 已经能够学习旋转恢复动作，但整个 attempt2 使用同一种 execution prompt，归位、水平调整和恢复后的正常搬运共享同一动作分布。相似姿态可能对应不同的下一步动作，容易造成阶段歧义；模型一旦做出反复动作，还会产生训练中没有见过的 state history，并进一步放大错误。

V5 将原执行流程拆成三个有序阶段：

1. `execution`：正常抓取、抬升和搬运；
2. `reposition`：将机械臂放下并第一次松开夹爪，把物体归位；
3. `adjustment`：抬起机械臂、按 recovery plan 移动、放下并重新抓住物体。

重新抓稳后回到 `execution`。V5 的目标是让动作模型在每个 prompt 下只学习一种动作语义，并由独立阶段判别头和受约束状态机决定何时切换 prompt。

V5 从 V4 已发布提交 `1ece3b7` 开始开发，数据和模型产物必须使用独立的 V5 profile、目录和配置哈希，不覆盖 V4。

## 2. 可行性结论

当前 V4 LeRobot 数据包含 408 条 attempt2。只读检查表明：

- 408/408 均能从第 7 维从臂夹爪状态和主臂动作中找到第一次稳定张开；
- 408/408 均能在第一次张开后找到重新闭合、抓住物体的稳定窗口；
- 触觉 caption 不能单独作为阶段边界：部分样本在夹爪完全张开时仍连续预测 `area=medium`，重新接触附近也可能出现 `none/medium` 抖动；
- 因此采用“夹爪状态和动作主判、触觉复核、异常样本人工 override”的规则。

仅增加一个基于现有 pooled VLM feature 的三分类头还不够。当前 VLM prefix 只包含图像和 prompt，60×7 state history 只进入 action expert。V5 的阶段头必须显式融合 VLM 特征和 state-history encoder 特征，才能利用夹爪状态、运动方向和最近动作轨迹。

## 3. 数据范围与版本

### 3.1 数据范围

V5 沿用 V4 的数据选择和 episode split：

- `one_success`；
- `moderate_lift`；
- `slightly_lift`；
- 继续排除 V4 已排除的 slight 同方向旋转失败子类；
- train/val/test 中的 episode 不重新划分，避免与 V4 对比时发生数据迁移。

V5 不修改 raw data、HDF5 或 V4 LeRobot parquet。新的阶段边界、逐帧阶段标签和动作窗口作为 sidecar manifest/index 保存，并绑定 V4 selection、split、parquet 和 caption 的 SHA256。

版本名称固定为：

```text
data_profile: rotation_phase_v5
prompt_profile: phase_v1
phase_schema: tactile_vla_rotation_phase_v5_v1
```

默认产物目录：

```text
/data1/outputs/vla/rotation_v5/
/data1/outputs/vla/assets/tactile_vla_rotation_v5/
/data1/outputs/vla/stage_a_action/pi05_delta_tac_rotation_v5/
/data1/outputs/vla/stage_b_v5/pi05_stage_b_rotation_v5/
```

### 3.2 阶段类别

类别 ID 和字符串必须固定，checkpoint、server 和客户端均从 metadata 读取并校验：

```text
0 = execution
1 = reposition
2 = adjustment
```

## 4. attempt2 阶段边界标签

### 4.1 第一次松开边界

将“夹爪第一次松开”定义为第一次稳定完全张开，而不是刚开始张开。

`release_frame` 取最早满足下列条件的连续 5 帧窗口起点：

```text
observation.state[:, 6] >= 0.095
action[:, 6]            >= 0.095
```

### 4.2 重新抓稳边界

在 `release_frame` 后搜索最早满足下列条件的连续 15 帧窗口，并将窗口起点记为 `regrasp_frame`：

```text
median(observation.state[:, 6]) <= 0.060
ptp(observation.state[:, 6])    <= 0.003
median(observation.state[:, 6] - action[:, 6]) >= 0.003
至少 5/15 帧 tactile_caption 的 area != none
```

其中状态与动作的稳定差值表示从臂夹爪被物体阻挡，而主臂仍在给出更小的闭合目标。

### 4.3 逐帧标签

attempt2 固定按以下闭区间语义打标签：

```text
[0, release_frame]                    -> reposition
(release_frame, regrasp_frame]        -> adjustment
(regrasp_frame, attempt_end]          -> execution
```

其他 attempt：

- one_success attempt1 全部标记为 `execution`；
- 恢复任务 attempt1 全部标记为 `execution`，包括 failure-active 观测；阶段描述的是当前行为，不替代 need/failure 标签；
- attempt2-only 失败数据仍按相同规则生成三个阶段标签。

两个边界前后各 5 帧标记 `phase_label_confident=false`。这些帧保留阶段身份和时序评估用途，但不进入 phase-head 分类训练。

### 4.4 自动标注的拒绝条件

遇到以下任一情况时脚本必须拒绝自动放行：

- 找不到 release 或 regrasp；
- `release_frame >= regrasp_frame`；
- 任一阶段长度不足以形成有效动作或评估样本；
- 边界持续时间属于配置定义的异常值；
- 触觉复核失败；
- 同一 attempt 存在无法消歧的多个候选窗口。

人工修正写入版本化 `phase_boundary_overrides.json`，记录 episode、attempt、两个边界、修改原因和审核人。override 文件 SHA256 必须写入 V5 profile 和 checkpoint identity。

## 5. 动作数据重新构造

### 5.1 动作监督范围

V5 动作监督只使用成功动作：

- one_success 的完整 attempt1；
- 成功 attempt2 的 reposition；
- 成功 attempt2 的 adjustment；
- 成功 attempt2 重新抓稳后的 execution。

失败 attempt1、失败 attempt2 和 attempt2-only 失败数据可以训练 phase、need、failure 和 plan，但不能进入 action index，避免把导致失败或重复旋转的动作重新教给动作模型。

### 5.2 H30 阶段边界处理

每个 action sample 仍输出 H30，但真实 target 不得跨阶段：

- 当前阶段内的部分使用原始真实动作；
- 超出阶段终点的剩余位置，重复该阶段最后一个真实动作；
- manifest 显式记录 `real_action_steps`、`padded_action_steps`、阶段起止帧和 terminal frame；
- 下游严格验证 H30 中不存在下一阶段动作。

这种 terminal hold 使阶段头尚未完成连续确认时，机械臂停在“夹爪已打开”或“已经抓稳”的安全边界，而不是继续生成下一阶段动作。

### 5.3 History 对齐

- attempt2 起点与真机新 attempt 一致，只保留起点 qpos 作为第一个有效 history frame；
- reposition→adjustment 和 adjustment→execution 时不清空 history；
- 阶段起点的 60×7 history 自然包含上一阶段末尾动作；
- 人工暂停或推理延迟期间不得刷新 history；
- train、dry-run、server warm-up 和真机客户端均校验相同的 30 Hz、60×7 和 mask 语义。

### 5.4 采样与 norm stats

统一 index 保留所有唯一 action sample，不复制 frame。Stage A batch sampler 按下面比例采样：

```text
execution : reposition : adjustment = 1 : 1 : 1
```

execution 内部再按下面比例采样：

```text
one_success : 成功 attempt2 后半段 = 1 : 1
```

norm stats 从 V5 train action index 重新计算：

- 只统计真实动作位置；
- 不统计 terminal hold 补齐副本；
- 不复用 V4 norm stats；
- summary 记录每个阶段的真实动作数、补齐数和最终 norm SHA256。

## 6. V5 Prompt

新增 `phase_v1`，不修改 `legacy` 和 `minimal_v1` 的行为。

### 6.1 Execution

```text
Mode: execution. Task: <instruction>.
```

### 6.2 Reposition

```text
Mode: reposition. Task: <instruction> Description: Put the object back to its original position.
```

### 6.3 Adjustment

```text
Mode: adjustment. Task: <instruction> Recovery plan: <plan>.
```

以上三个动作 prompt 均不包含触觉描述。构造器必须规范句号，禁止因 instruction 已带句号而产生重复标点。

### 6.4 Phase assessment

阶段判别和 need/failure 共用不泄漏阶段答案的 assessment prompt：

```text
Mode: tactile assessment. Task: <instruction> <tactile_caption> Recovery plan: <plan>.
```

该 prompt 中不得出现 `execution`、`reposition`、`adjustment` 当前标签字段。reasoning prompt、完整 V3 failure/recovery grammar 和最多 4 对 memory 的规则保持 V4 行为。

## 7. 模型修改

### 7.1 PhaseHead

在现有 Stage B wrapper 中新增三分类 `PhaseHead`：

1. 对 PaliGemma 图像和 assessment prompt 的 prefix output 做 masked mean pooling；
2. 使用 `backbone.encode_state_history(observation)` 编码归一化的 60×7 history；
3. 分别 LayerNorm 和线性投影后拼接；
4. 经过 GELU、dropout 和三分类线性层输出 `phase_logits[B,3]`。

Stage B 中 history GRU/projection 保持冻结，避免 phase loss 直接改变 action expert 的 history conditioning。可训练参数包括：

- 原 Stage B PaliGemma LoRA；
- need head；
- phase head。

### 7.2 Assessment 前向

一次 assessment prefix 编码同时返回：

```text
need_recovery_logits
phase_logits
failure_reason generation cache
```

phase transform 必须保留并归一化 state history。缺少 history、mask、norm stats 或维度不为 60×7 时立即报错，不允许回退到纯图文阶段判别。

### 7.3 Checkpoint 与 metadata

Stage B delta、resume、merge 和 merged checkpoint identity 新增：

- `phase_head` 参数；
- phase 类别及顺序；
- phase manifest/index/config hash；
- phase prompt profile；
- assessment stride、确认次数和置信度阈值；
- V5 norm stats SHA256。

V5 delta 只能与其 config 中记录的同一个 V5 Stage A checkpoint 合并。V4 checkpoint 不得改名冒充 V5。

## 8. 训练协议

### 8.1 Stage A

V5 Stage A 从原始 `pi05_base` 初始化，不继承 V4 Stage A 或 merged_best：

```text
batch size:              8
steps:                   15000
learning rate:           5e-5
final learning rate:     5e-7
transition steps:        7000
save interval:           1000
keep period:             5000
action horizon:          30
action dim:              32
state history:           60x7 @ 30 Hz
LoRA only:               true
```

Stage A action loader 根据每行 phase 构造对应 prompt，并使用 V5 phase-bounded H30 target。

### 8.2 Stage B

Stage B 改为五任务循环：

```text
action -> need -> phase -> failure -> plan
```

固定协议：

```text
total steps:             5000
updates per task:        1000
batch size:              8
learning rate:           1e-4
eval interval:           500
save interval:           1000
keep period:             1000
```

phase train sampler 三类等量，并在方向、slightly/moderately、成功/失败和 attempt 布局上分层。val/test 保留完整序列用于阶段转换延迟评估。

每个评估点报告：

- action 总体及 execution/reposition/adjustment 分阶段 loss；
- 相对 Stage A baseline 的 action degradation；
- need class support 和 macro-F1；
- phase 每类 precision/recall/F1、macro-F1 和 confusion matrix；
- 四方向 failure exact match；
- 四方向、两种距离 plan exact match；
- 10步采样、连续两次确认下的阶段转换延迟。

best 首先要求 action degradation `<=10%`，再按以下四项平均值选择：

```text
need macro-F1
phase macro-F1
failure exact match
plan exact match
```

## 9. Server 与真机状态机

### 9.1 Server metadata

V5 server 必须声明：

```text
data_profile=rotation_phase_v5
prompt_profile=phase_v1
phase_classes=[execution,reposition,adjustment]
phase_assessment_stride=10
phase_confirmations=2
phase_confidence_threshold=0.6
```

assessment response 增加：

```text
phase
phase_probs
phase_logits
```

### 9.2 受约束 FSM

客户端只允许以下转换：

```text
execution
  -> need=true -> reasoning -> new attempt -> reposition

reposition
  -> 连续两次 adjustment 概率 >= 0.6 -> adjustment

adjustment
  -> 连续两次 execution 概率 >= 0.6 -> execution
```

运行规则：

- 每次 action inference 仍生成 H30，但只执行前 10 步，然后重新 assessment；
- execution→reposition 只能由 need/failure/reasoning 和新 attempt 触发；
- classifier 不得触发逆向转换或跳过 adjustment；
- reposition/adjustment 阶段忽略 need-recovery 输出；
- 低置信度或预测当前阶段时保持当前状态；
- 连续确认计数在预测不一致时清零；
- 单阶段持续超过 30 秒仍未合法转换时停止动作发布并要求人工检查；
- failure 后清空 history；阶段内部转换不清空 history。

所有日志记录 assessment timestamp、当前 FSM 状态、phase probs、确认计数、动作 prompt、recovery plan、已执行步数和最终转换原因。

## 10. 测试与验收

### 10.1 数据测试

- 408 条 attempt2 均获得顺序合法且确定性的 release/regrasp 边界；
- override、异常拒绝、低置信区和 source SHA 校验；
- split 与 V4 完全一致且无 episode 泄漏；
- 失败 attempt 不进入 action index；
- 成功 attempt2 三个阶段均有 action support；
- H30 不包含下一阶段真实动作，补齐部分只等于 terminal action；
- norm stats 不统计补齐副本；
- attempt2 起点和阶段切换附近 history 与真机规则一致。

### 10.2 模型与训练测试

- `phase_v1` 三种动作 prompt 和 assessment prompt 快照；
- assessment prompt 不泄漏 phase label；
- legacy/minimal checkpoint 行为不变；
- phase head 输出 `[B,3]` 并真实使用 history；
- 缺失 history、mask、norm 或 identity 时拒绝训练和 serve；
- Stage B 严格执行五任务循环和 5000 step；
- resume、best、merge 和 serve 校验 V5 phase identity；
- dry-run 打印三个 action batch 和 need/phase/failure/plan batch 的真实 prompt、H30、60×7 history、phase target 和补齐信息。

### 10.3 离线验收

目标指标：

```text
phase macro-F1 >= 0.90
三个 phase 均有非零 support
FSM 边界转换延迟 P95 <= 20 frames
action degradation <= 10%
```

还必须检查分阶段 action loss，不能只用总体平均掩盖某一阶段退化。

### 10.4 真机验收

依次验证：

- one_success 正常抓取搬运；
- right/left/front/back moderately 恢复；
- right/left/front/back slightly 恢复；
- 完整 `execution -> reposition -> adjustment -> execution` 闭环；
- 阶段边界附近模型先稳定保持，再由 FSM 切换 prompt；
- 不出现 reposition 与 adjustment 的反复切换；
- 新 attempt 才清空 history，阶段内 history 连续。

V5 首轮仍只验收旋转恢复，不扩展抓空、面积过小、垂直恢复或其他扰动。
