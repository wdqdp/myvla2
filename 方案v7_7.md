# V7.7：Action + Phase Assessment + Recovery Reasoning 多任务训练

## 1. 目标与范围

V7.7 从 V7.4 Stage-A 最终 checkpoint 初始化，在一个模型中联合训练以下五个任务：

1. `action`：生成 H30 action chunk。
2. `adjustment_end`：调整阶段是否已结束。
3. `need_recovery`：执行阶段是否需要恢复。
4. `failure_reason`：需要恢复时，生成结构化失败原因。
5. `recovery_plan`：根据失败触觉和 `Failure-recovery memory` 生成结构化恢复计划。

`need_recovery` 仍为 V4 的二分类 MLP；`failure_reason` 和 `recovery_plan` 仍为 V4 的 PaliGemma 受约束因果文本生成，不新建独立线性分类头。`adjustment_end` 保留 V7.6 的二分类 MLP、标注规则和 H100/11 点历史。

V7.7 的模型结构是：

```text
共享 PaliGemma prefix encoder / LM decoder
├── adjustment_end_head: prefix mean pooling -> MLP -> 2 logits
├── need_recovery_head: prefix mean pooling -> MLP -> 2 logits
├── failure_reason: 共享 LM decoder + failure grammar
└── recovery_plan: 共享 LM decoder + recovery grammar

Action Expert
└── action: H30 x 32，对外取前 7 维
```

## 2. Prompt 协议

### 2.1 Phase 共享 prompt

`adjustment_end`、`need_recovery` 和 `failure_reason` 必须使用字节级一致的原始 prompt 模板：

```text
Mode: phase
Task: <instruction>
Touch: <当前触觉描述>
Recovery plan: <当前 recovery_plan>
State history H100 sampled to 11 points: <离散后的 11x7 qpos>
```

约束：

- 不在末尾增加“请判断阶段”、“输出失败原因”等补充文字。
- `<当前触觉描述>` 原样使用 Captioner 输出；如果值本身为 `Touch[...]`，则该行是 `Touch: Touch[...]`，训练与部署不得分别做字符串清洗。
- attempt1 没有恢复计划时，`Recovery plan` 固定写 `none`。
- attempt2 及更后续 attempt 使用当前真正在执行的结构化 recovery plan。
- 11x7 qpos 使用 compact JSON，不加空格，每个数值是当前 norm stats 下的 256-bin 离散 ID。
- H100 包含当前帧 `p`。
- 分类头不使用 Action Expert 的连续 state history；H100 只作为文本 token 进入 PaliGemma。

为了在 `need_recovery=true` 后直接复用同一份 KV cache 生成 `failure_reason`，三个任务必须使用同一个 structured tokenizer 前缀：

```text
Task: <上述 phase prompt>, State: <当前 7 维离散 qpos>;
Answer: 
```

V7.6 `adjustment_end` 原来通过普通 `TokenizePrompt` 使用 `Action:` 后缀。V7.7 为了共享 failure 生成的 KV cache，必须统一改为上述 `Answer:` 前缀。V7.7 不加载 V7.6 的模型权重；`adjustment_end_head` 在 V7.4 Stage-A 基础上随机初始化并从头训练。V7.6 仅用于定义 adjustment 的数据构造规则和对比评估基线。

### 2.2 Plan prompt

`recovery_plan` 完全沿用 V4 `minimal_v1` reasoning prompt：

```text
Mode: reasoning. Task: <instruction> <failed_tactile_caption> Failure-recovery memory: <memory>.
```

`memory` 中每项格式为：

```text
recovery_plan=<上一次执行的计划>; failure_reason=<该次的失败原因>
```

多项使用 ` | ` 分隔。第一次失败时：

```text
recovery_plan=initial plan; failure_reason=<failure_reason>
```

plan 训练和推理同样使用 structured `Answer:` 前缀，但它的 prompt 已经与 phase prompt 不同，因此不能复用 phase/failure 的 KV cache，必须单独 prefill。

### 2.3 Action prompt

Action 数据和 prompt 完全沿用 V7.4/V7.6，不因 V7.7 的 phase assessment prompt 而改变：

- `execution` 和 `adjustment` 使用 V7.4 阶段 prompt。
- H30 target 使用 V7.4 的跨边界与静止段处理。
- Action Expert 不使用连续 state history，即 `state_history_len=0`。
- 训练监督仍是 delta action，模型内部为 H30x32，有效机器人维度为 7。

## 3. 数据构建

### 3.1 统一身份和分割

V7.7 不重新分割 episode，必须沿用 V4/V7.6 的 train/val/test episode 划分。构建器必须校验：

- V4 selection hash、V7.4 action index selection hash 和 V7.6 adjustment index selection hash 完全一致。
- 一个 original episode 只能属于一个 split。
- manifest 的 `(global_index, episode_id, attempt_id, frame_index)` 与 LeRobot 实际帧一致。
- prompt 中不得包含 `episode_id`、`attempt_id`、`frame_index`、目标 label 等数据身份字段。
- 所有 source JSON/JSONL、Parquet 身份、norm stats、Captioner summary 和 base checkpoint config 写入 SHA256。

### 3.2 H100/11 点构建

对 phase prompt 的当前帧 `p`，先按真实时间顺序构建同一 episode 内截止当前帧的最近 100 帧：

```text
episode_timeline[current_position-99 : current_position+1]
```

再均匀取 11 个时间位置，最后一点必须是 `p`。H100 允许跨 attempt，但绝对不允许跨 episode。

- 对 `adjustment_end`，完全沿用 V7.6 的 idle 区间扰动、`k=0:1:2` 的 17:2:1 分配、`l>k*10` 约束和 seed 42。
- 对 `need_recovery/failure_reason`，不应用 adjustment 阶段的 idle/AG 过滤，直接对真实时间线均匀采样。
- 例如 attempt2 的当前帧之前不足 99 帧时，直接从同 episode 的 attempt1 尾部继续向前取；更后续 attempt 按相同规则使用前面 attempt 的真实历史。
- 只有当前帧位于整个 episode 的前 100 帧、整个 episode 历史仍不足时，才使用该 episode 第 0 帧 qpos 做左侧 padding。
- episode timeline 必须依据原始 timestamp/global index 排序，并校验 attempt 切换处的时间单调性，不能仅用会在每个 attempt 重置的局部 `frame_index` 向前索引。
- adjustment idle 扰动只作用于当前 attempt 的 `[gripper_motion_stop, arm_adjustment_start]` 区间；H100 中来自前一 attempt 的帧作为普通历史帧，不应被该 idle 规则删除。
- manifest 同时保存 11 个 source `(attempt_id, frame_index, global_index, timestamp)`、离散后 11x7 数值和 episode 内有效历史长度，用于审计跨 attempt 采样。

### 3.3 Action 数据

不生成新 action 标签，直接引用：

```text
outputs/rotation_v7_4_adjustment/v7_4_prompt_training_index.json
```

训练加载器必须复用 V7.6 的 V7.4 action replay 通路，不能从 V4 旧 action index 重新取样。

### 3.4 adjustment_end 数据

记：

```text
A = arm_adjustment_start
S = arm_adjustment_stop
p = 当前帧
```

事实样本完全沿用 V7.6：

```text
p in [A, S-11]     -> false
p in [S-10, S+10]  -> true
```

反事实 recovery plan 样本也完全沿用 V7.6：

- 原 `moderately` 改为 `slightly`：`S_s=floor((A+S)/2)`，`[A,S_s-1]` 为 false，`[S_s,S]` 为 true。
- 原 `slightly` 改为 `moderately`：`[A,S]` 全为 false。
- 反事实 pair 的图像、触觉、当前 qpos、H100 采样点和 idle 扰动必须完全相同，只允许 recovery plan 和对应 label 改变。
- 沿用 V7.6 训练集 `slightly -> moderately` 困难负样本缩减和最终 1:2 正负比。

V7.7 应重建 adjustment manifest 中的 prompt，使其符合新的共享 `Mode: phase` 模板；除 prompt/token length 外，必须逐行校验其物理身份、采样帧、label、pair 关系和 V7.6 一致。

V7.6 已固定的数量应保持：

| split | false | true | total |
|---|---:|---:|---:|
| train | 10490 | 5245 | 15735 |
| val | 1950 | 660 | 2610 |
| test | 1565 | 629 | 2194 |

### 3.5 need_recovery 数据

标注沿用 V4，但重新设计负样本的组成，不再使用 V4 的 hard-negative-first 策略。

正样本：

```text
失败 attempt 中 p >= shift_frame_index 的 failure-active 帧
label = true
```

负样本候选：

1. `pre_failure_hard_negative`：失败 attempt 的 `p < shift_frame_index`。
2. `one_success_easy_negative`：一次成功 attempt 的整个 attempt，在完整时间区间内均匀采样。
3. `successful_recovery_easy_negative`：成功恢复 episode 的 attempt2，只从 `[gripper_close_start, attempt最后一帧]` 内均匀采样。

每个 split 独立构建，总负正比固定为 3:1，三类负样本在负类内的比例尽可能接近 1:1:1，seed=42。设正样本数为 `P`，则负样本总目标为 `3P`：

1. 先对三类各分配约 `P` 个名额。
2. 如果某类候选少于其名额，保留该类的全部候选。
3. 将缺额尽可能均匀地重新分配给仍有剩余候选的类别，保持总负样本数为 `3P`。
4. 不进行有放回采样；只有三类候选合计不足 `3P` 时才报错。
5. 每类内对指定时间区间做确定性均匀采样；随机打破同余选择和后续训练打乱使用 seed 42。

在三类候选都充足时，每类负样本数都等于 `P`；候选不平衡时允许偏离 1:1:1，但必须在 summary 中记录目标数、候选数、实际选中数和缺额重分配结果。

总数目标保持：

| split | false | true | total |
|---|---:|---:|---:|
| train | 39147 | 13049 | 52196 |
| val | 4626 | 1542 | 6168 |
| test | 4830 | 1610 | 6440 |

每条样本加入按 3.2 构造的 H100/11 点历史，并使用 2.1 的 phase prompt。二分类标签为：

```text
need_recovery_label: 0=false, 1=true
```

### 3.6 failure_reason 数据

失败原因目标和时间范围沿用 V4。记 `F=shift_frame_index`：

```text
训练：p in [F, F+14]
验证/测试主指标：p = F+14
```

建议 manifest 在 val/test 仍保留完整 15 帧，用于评估异步检测的时序稳定性；主 exact-match 指标仍只统计 `F+14`，与 V4 可比。

当前 black_box 的物理样本数量为：

| split | 失败 attempt | 15 帧 manifest | V4 主评估样本 |
|---|---:|---:|---:|
| train | 144 | 2160 | 2160 |
| val | 18 | 270 | 18 |
| test | 18 | 270 | 18 |

每条样本使用与 need 完全相同的 phase prompt 和 H100 构造。对于同一帧的 need-positive/failure 样本，构建器必须校验：

```text
need_prompt_bytes == failure_prompt_bytes
```

训练 target 是完整的结构化文本，例如：

```text
failure_reason=rotate right,grasp appropriate.
```

使用 V4 `v3_full_v1` failure grammar 做 token-level 约束交叉熵。当前 black_box 真实 target 只有 `right/left + grasp appropriate`；grammar 中其他合法值只是为后续扩展保留，不能将它们的评估支持记为有真实训练样本。

### 3.7 recovery_plan 数据

plan 数据范围、memory 构造和 target 沿用 V4：

- 当前 observation 取失败 attempt 的 `[F,F+14]` 帧。
- 观测图像、当前 qpos 和 failed tactile caption 必须来自同一真实帧。
- target recovery plan 来自同 episode 的相邻下一个真实 attempt。
- `moderately` target 只允许 `memory_length=1`。
- `slightly` target 允许 `memory_length=2/3/4`。
- memory 为 V4 `rotation_distance_v1` 合成链，seed=42；末项 failure 必须是当前真实 failure，不伪造 donor episode 身份。
- target 使用 V4 `v3_full_v1` recovery grammar 训练受约束 token 交叉熵。

当前 V4 artifact 数量为：

| split | 完整 manifest | V4 训练/主评估实际使用 |
|---|---:|---:|
| train | 3840 | 3840 |
| val | 480 | 34（只取 frame_offset=14） |
| test | 480 | 33（只取 frame_offset=14） |

训练 target 例如：

```text
recovery_plan=move horizontally right moderately, move vertically none moderately.
```

当前 black_box 真实 target 只覆盖 `left/right x slightly/moderately`，垂直固定为 `none moderately`。完整 grammar 中未出现的方向、程度和垂直计划不能当作已训练能力。

plan 不使用 phase H100 prompt，也不使用连续 state history；它只使用 V4 reasoning prompt、当前 7 维 qpos 和当前图像。

## 4. 训练方案

### 4.1 初始化和参数更新

V7.7 必须直接从 V7.4 Stage-A 的 `15000` checkpoint 初始化：

```text
/data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action/pi05_delta_tac_rotation_phase_v7_4_no_history/15000
```

初始化规则：

- 加载 V7.4 Stage-A 的 action model、PaliGemma 及已有 LoRA 权重。
- 不加载 V7.6 checkpoint 中的任何权重。
- `adjustment_end_head` 随机初始化。
- `need_recovery_head` 随机初始化。
- `failure_reason` 和 `recovery_plan` 没有独立参数头。

训练参数：

```text
可训练：PaliGemma LoRA + adjustment_end_head + need_recovery_head
冻结：Action Expert + PaliGemma non-LoRA
```

`failure_reason/recovery_plan` 没有独立参数头，它们的 loss 通过共享 PaliGemma LM decoder 更新 PaliGemma LoRA。

### 4.2 任务循环

固定五任务循环：

```text
action -> adjustment_end -> need_recovery -> failure_reason -> recovery_plan
```

建议正式训练 `20000 steps`，必须在 5-task cycle 边界保存 checkpoint，每个任务获得 4000 个 update。其中 adjustment_end 的 update 数与 V7.6 的 8000-step 两任务训练保持一致；action 则从 V7.4 Stage-A 基线继续参与多任务训练。

五个 loss 初始权重均设为 1.0：

```text
action diffusion loss
adjustment_end softmax cross entropy
need_recovery softmax cross entropy
failure_reason constrained token cross entropy
recovery_plan constrained token cross entropy
```

如果后续调整 loss 权重，必须新建版本，不能在 V7.7 同名 run 中静默修改。

### 4.3 可恢复训练和 checkpoint

- 每个 sampler 使用独立 seed/cursor，resume 后必须继续原数据流。
- checkpoint step 必须被 5 整除，防止 resume 后任务循环错位。
- 保存 optimizer state、loop RNG、model RNG 和所有可训练参数。
- 每个正式 checkpoint 导出 `delta_params` 和可直接部署的 `full_params`。
- resume 时校验五个数据流的 artifact hash、prompt helper identity、class/grammar 和 base V7.4 Stage-A checkpoint identity。

### 4.4 统一 phase prefill 接口

V7.7 服务端使用一个统一的 phase prefill 接口：

```python
phase_prefill(
    observation,
    active_head="need_recovery",
    generation_grammar=failure_grammar,
)
```

`active_head` 只能是 `need_recovery` 或 `adjustment_end`，决定本次请求实际执行并返回哪个分类头。未激活的分类头不计算 logits，也不得出现在响应中。

执行阶段固定调用：

```python
phase_prefill(
    observation,
    active_head="need_recovery",
    generation_grammar=failure_grammar,
)
```

一次 PaliGemma prefix forward 同时产生：

```text
need_recovery_logits
failure 首 token logits
PaliGemma KV cache
prefix_mask
semantic_position
```

- `need_recovery=false`：立即释放本次 KV cache，不生成 failure。
- `need_recovery=true`：服务端必须先立即向客户端推送 need=true 的 `phase_decision` 事件，使客户端中断当前 action chunk；然后不重新编码图像、phase prompt 或 H100，直接从首 token logits 和同一份 KV cache 因果续写 `failure_reason`。
- need 判断和条件 failure 生成属于同一个服务端请求，但通过同一 WebSocket 的两个事件分步返回：先返回 need 决策，后返回 failure 文本。客户端不发起第二次 failure 请求，服务端也不向客户端传输或暴露 KV cache ID。
- failure 生成完成后释放该 cache。

调整阶段固定调用：

```python
phase_prefill(
    observation,
    active_head="adjustment_end",
    generation_grammar=None,
)
```

此时只返回 `adjustment_end_logits`，不计算 `need_recovery_logits`，不准备 failure 首 token logits，也不保留文本生成用 KV cache。

## 5. 推理输出协议

### 5.1 Action

```json
{
  "actions": "float32[30,7]",
  "policy_timing": {"infer_ms": 0.0}
}
```

### 5.2 执行阶段 phase assessment

执行阶段只激活 `need_recovery_head`。`adjustment_end` 字段不计算、不返回；`failure_reason` 只在 need 为 true 时继续生成。

need=false：

```json
{
  "event": "phase_decision",
  "request_id": "phase-123",
  "phase": "execution",
  "need_recovery": false,
  "need_recovery_probs": [0.99, 0.01],
  "policy_timing": {"infer_ms": 0.0}
}
```

need=true 时，同一 `request_id` 先返回决策事件：

```json
{
  "event": "phase_decision",
  "request_id": "phase-123",
  "phase": "execution",
  "need_recovery": true,
  "need_recovery_probs": [0.01, 0.99],
  "policy_timing": {"infer_ms": 0.0}
}
```

客户端收到该事件后立即停止并丢弃当前 H30 的剩余 action。服务端继续使用同一份 KV cache 生成 failure，完成后再推送：

```json
{
  "event": "failure_reason",
  "request_id": "phase-123",
  "phase": "execution",
  "failure_reason": "failure_reason=rotate right,grasp appropriate.",
  "policy_timing": {"decode_ms": 0.0}
}
```

`need_recovery_probs` 固定顺序是 `[P(false), P(true)]`。沿用 V4 输出协议，`failure_reason` 只返回 grammar 约束后的最终文本，不返回 `failure_reason_probs`。need 和条件 failure 仍在同一次 server request 中完成，客户端不再发起第二次 failure request。

### 5.3 调整阶段 phase assessment

调整阶段只激活 `adjustment_end_head`，不计算、不返回 need/failure：

```json
{
  "event": "phase_decision",
  "request_id": "phase-456",
  "phase": "adjustment",
  "adjustment_end": false,
  "adjustment_end_probs": [0.998, 0.002],
  "policy_timing": {"infer_ms": 0.0}
}
```

`adjustment_end_probs` 固定顺序是 `[P(false), P(true)]`。

### 5.4 Recovery plan

failure 生成完成后，先用本次实际执行的 plan 和 failure 更新 memory：

```json
{
  "recovery_plan": "initial plan",
  "failure_reason": "failure_reason=rotate right,grasp appropriate."
}
```

然后使用 V4 reasoning prompt 单独 prefill 并生成 plan：

```json
{
  "recovery_plan": "recovery_plan=move horizontally right moderately, move vertically none moderately.",
  "policy_timing": {"infer_ms": 0.0}
}
```

沿用 V4 输出协议，不返回 `recovery_plan_probs`。

## 6. 异步部署状态机

phase assessment 使用独立异步 worker，无论当前是 execution 还是 adjustment 都保持运行，但每个阶段只激活对应输出：

```text
execution
  -> 异步 phase prefill
  -> need_recovery_head
       -> false: 继续 execution
       -> true:
            立即推送 phase_decision(need=true)
            -> 客户端中断并丢弃当前 execution H30 的剩余 action
            -> 进入安全保持状态
            -> 复用同一 phase prefill KV cache
            -> failure_reason 因果解码
            -> 更新 Failure-recovery memory
            -> 用 reasoning prompt 重新 prefill
            -> recovery_plan 因果解码
            -> 清空过期 action/phase 结果
            -> 切换到 adjustment

adjustment
  -> 异步 phase prefill
  -> adjustment_end_head
       -> false: 继续 adjustment
       -> true:
            立即中断并丢弃当前 adjustment H30 的剩余 action
            -> 进入安全保持状态
            -> 清空过期 action/phase 结果
            -> 切换到 execution
```

异步正确性要求：

- action 和 phase assessment 使用独立 WebSocket/请求通路。
- 同一时刻最多一个 phase request in flight，避免判断乱序。
- 每个请求携带 `attempt_id`、`generation`、`phase`和开始 action step，但这些不进入 prompt。
- 返回时若 attempt/generation/phase 已变更，立即丢弃旧结果。
- need=true 或 adjustment_end=true 的决策事件优先级高于 action 发布循环；客户端必须在下一个控制周期前设置 `stop_latched=true`。
- `stop_latched=true` 时，立即丢弃当前 chunk 中未发布的所有 action，使所有在途 action future 失效，并按控制器安全停止/保持接口持续发布 hold 命令。不得等待当前 H30 执行完。
- need=true 时不得等待 `failure_reason` 或 `recovery_plan` 生成完成才停止；诊断和 plan 推理期间保持 `stop_latched=true`。
- adjustment_end=true 时同样立即丢弃剩余 adjustment action；只有切换到 execution 且收到新的有效 execution chunk 后才允许解除 hold。
- action 结果和 phase 结果使用独立的 generation/request ID：停止时可以使 action generation 立即失效，但仍允许同一 phase `request_id` 的后续 failure 事件被接收。
- need=true 的 failure 必须使用该次 need 请求的同一观测和 KV cache，不能用更新一帧重新推理。
- need 与 failure 是同一个 server request 内的两阶段事件；不实现客户端二次请求或跨请求 KV-cache registry。
- plan 使用同一次失败观测的 failed tactile caption，但因 memory/prompt 改变而单独 prefill。
- 在同一 episode 内，H100 rolling buffer 在 attempt 切换和 execution/adjustment phase 切换时都不清空；只在新 episode/新任务会话开始时清空。
- 真机会话刚启动、rolling buffer 尚不足 100 帧时，使用本 episode 收到的第一帧 qpos 左填充，与训练端的 episode 起点规则一致。
- 阶段切换时增加 generation 并清空 action chunk、temporal smoothing 和 phase future。
- 对 need 和 adjustment_end 分别配置判断频率、冷却时间和阈值；阈值从 checkpoint metadata 读取，不在客户端静默写死。

## 7. 评估与验收

### 7.1 离线指标

Action：

- 相对 V7.4 Stage-A base 的 action validation loss degradation。
- execution/adjustment 分阶段 loss。

Adjustment end：

- V7.6 的 AUROC、AUPRC、阈值下 precision/recall/early-FPR。
- factual/counterfactual 分组指标和 magnitude pair ranking。
- H30 endpoint 时序模拟。

Need recovery：

- accuracy、macro-F1、false-positive rate 和 recall。
- failure-active 起点相对延迟。
- attempt 级连续输出稳定性。
- 按 `pre_failure_hard_negative`、`one_success_easy_negative` 和 `successful_recovery_easy_negative` 分别统计 false-positive rate。
- 每个 split 审计总负正比以及三类负样本的实际比例，并记录偏离 1:1:1 的原因。

Failure reason：

- exact match、按方向的 exact match。
- `[F,F+14]` 每个 frame offset 的准确率。
- 只在 need=true 时执行的级联准确率。

Recovery plan：

- 完整字符串 exact match。
- horizontal direction accuracy 和 magnitude accuracy。
- 按 memory length 1/2/3/4 分组统计。
- 用真实 failure target 构建 prompt 的 teacher-forced 结果。
- 用 failure 模型预测值更新 memory 后的端到端结果。

### 7.2 必须通过的回归检查

- V7.7 action loss 不超过预设的 V7.4 Stage-A degradation gate。
- adjustment-end 的阈值指标不低于 V7.6 可接受标准。
- 同一 need/failure 物理帧生成完全相同的 phase prefix token。
- need=false 的 server 响应不包含 failure 字段。
- execution 响应不包含 adjustment-end 字段。
- adjustment 响应不包含 need/failure 字段。
- need=true 时 failure 解码复用同一 KV cache，不发生第二次 phase prefill。
- need=true 的 `phase_decision` 必须在 `failure_reason` 事件之前到达客户端，并且客户端在下一控制周期前进入 hold。
- 在 H30 的任意位置注入 need=true 或 adjustment_end=true 后，该位置之后的 chunk action 均不得再发布。
- true 决策前已发起但后到达的 action 响应必须因 action generation 过期而被丢弃。
- need 诊断/plan 期间必须持续 hold；adjustment_end=true 后必须持续 hold，直到对应新 phase 的首个有效 action chunk 就绪。
- execution 请求不计算或返回 adjustment-end logits；adjustment 请求不计算或返回 need logits。
- need=false 和 failure 解码完成后均不保留该次 phase KV cache。
- 跨 attempt H100 样本的 11 个 source key 与 episode 真实时间线一致，不跨 episode，且最后一点是当前帧。
- 同一回放序列在训练构建器和部署 rolling buffer 中生成的 H100/11 离散结果必须完全一致。
- need 数据的总负正比是 3:1，三类负样本已按候选容量做最小偏离的 1:1:1 分配，且不存在重复采样。
- plan 在 memory 更新后发生一次新 reasoning prefill。
- 所有 failure/plan 输出必须属于 checkpoint 记录的 grammar。

## 8. 计划的实现文件

本版本建议新建，不覆盖 V7.6 文件：

```text
src/tactile_vla/vla/v7_7_phase_prompt.py
src/tactile_vla/vla/v7_7_multitask_data.py
src/tactile_vla/vla/v7_7_multitask_model.py
src/tactile_vla/vla/v7_7_multitask_evaluation.py
scripts/build_v7_7_multitask_data.py
scripts/train_vla_multitask_v7_7.py
scripts/serve_tactile_vla_v7_7.py
openpi/inference/agilex/inference/agilex_inference_v7_7_asyn.py
tests/test_v7_7_multitask_data.py
tests/test_v7_7_multitask_model.py
tests/test_v7_7_async_inference.py
```

数据构建默认输出：

```text
outputs/rotation_v7_7_multitask/
├── adjustment_end_manifest.jsonl
├── need_recovery_manifest.jsonl
├── failure_reason_manifest.jsonl
├── recovery_plan_manifest.jsonl
├── v7_7_multitask_training_index.json
├── summary.json
└── artifact_hashes.json
```

## 9. 实现顺序

1. 先实现并锁定 phase prompt helper 及 token identity 测试。
2. 构建四个分类/推理 manifest 和统一 training index，输出类别、时间边界、H100、token length 和哈希统计。
3. 人工抽查共享 phase prompt、H100 和 plan memory。
4. 实现 V7.7 模型包装、V7.4 Stage-A checkpoint 加载和两个分类头的随机初始化。
5. 实现五任务循环，先跑 5-step dry run，再跑 10-step save/restore smoke test。
6. 跑完整离线评估和 action/adjustment 回归 gate。
7. 最后实现 server 和异步真机状态机。

## 10. 已实现入口与命令

构建数据：

```bash
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/build_v7_7_multitask_data.py
```

默认产物已经生成到：

```text
/data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_7_multitask
```

四卡正式训练：

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 \
OPENPI_DATA_HOME=/home/qxh/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/train_vla_multitask_v7_7.py \
  --dataset-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/lerobot_data/tactile_vla_rotation_v4 \
  --index-file /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v7_7_multitask/v7_7_multitask_training_index.json \
  --norm-stats-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats \
  --stage-a-checkpoint /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/stage_a_action/pi05_delta_tac_rotation_phase_v7_4_no_history/15000 \
  --output-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/multitask_v7_7 \
  --run-name pi05_rotation_v7_7_five_task_h100_no_history \
  --data-profile rotation_phase_v7_7_five_task_h100 \
  --prompt-profile phase_v7_7_h100_11_answer \
  --grammar-profile v3_full_v1 \
  --paligemma-variant gemma_2b_lora \
  --action-expert-variant gemma_300m_lora \
  --no-use-state-history \
  --fsdp-devices 2 \
  --num-steps 10000 \
  --eval-interval 5000 \
  --save-interval 5000 \
  --seed 42 \
```

其中 `H100 sampled to 11 points` 由数据 manifest 写入 phase prompt；
`--no-use-state-history --state-history-len 0 --history-hidden-dim 0` 表示 Action Expert 本身仍是 V7.4 的无历史结构，二者不要混淆。
V7.7 的 `adjustment_loss_weight=1.0` 由训练入口固定设置，当前不是命令行参数。

训练 checkpoint 同时导出 `delta_params` 和可直接部署的 `full_params`。

服务端推理命令（可传数字 step 目录或其 `full_params` 子目录）：

```bash
CUDA_VISIBLE_DEVICES=0 \
OPENPI_DATA_HOME=/home/test/.cache/openpi \
USE_TF=0 \
openpi/.venv/bin/python scripts/serve_tactile_vla_v7_7.py \
  --checkpoint /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/pi05_rotation_v7_7_five_task_h100_no_history/10000 \
  --norm-stats-dir /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats \
  --host 0.0.0.0 \
  --port 8001 \
  --need-recovery-threshold 0.5 \
  --adjustment-end-threshold 0.8 
```

其中两个 `0.5` 是当前 checkpoint metadata 中的默认阈值；完成 validation 阈值选择后，应将这两项改为选定值。
不写这两个参数时，server 会读取 checkpoint `config.json` 中的阈值。

服务启动后可检查：

```bash
curl --fail http://127.0.0.1:8000/healthz
```

真机异步推理命令：

```bash
LD_PRELOAD=/lib/x86_64-linux-gnu/libffi.so.7 \
python openpi/inference/agilex/inference/agilex_inference_v7_7_asyn.py \
  --host localhost \
  --port 8001 \
  --instruction "Pick up and transfer the object stably." \
  --noise-seed 42 \
  --norm-stats-file /data1/qxh/tac_vla_new/tac_data/demon_data/black_box/outputs/rotation_v4/norm_stats/norm_stats.json \
  --captioner_checkpoint /data1/outputs/tactile_captioner/tcn_v3_w30_rotation_head/best.pt \
  --need-recovery-rate-hz 3 \
  --adjustment-end-rate-hz 3 \
  --publish_rate 30 \
  --max-attempts 5 \
  --gripper-min 0.035 \
  --classification-gripper-open-threshold 0.0985 \
  --classification-gripper-open-value 0.0995
```

默认为人工按 Enter 后开始；如果需要启动后立即运行，增加 `--start-immediately`。
运行时 `s` 表示人工确认任务成功，`q` 表示安全退出。
该入口不再需要人工传入 rotation direction/magnitude；need=true 后由 failure 和 plan 头自动生成 recovery plan。
连续 H100 来自 ROS qpos callback 的 30 Hz buffer，execution/adjustment 和 attempt 切换均不清空；Action Expert 仍不接收 dense state-history tensor。
Action 请求按 `noise_seed + phase + chunk index` 生成确定性 `[30,32]` noise，server 会强制校验该输入，从而保持 V7.4 action 部署的可复现性。

实现状态：数据构建、五任务训练、双分类头、共享 need/failure KV prefill、流式两事件服务协议、连续 H100、立即停止 gate 和可执行的 ROS 异步真机入口均已落地。
